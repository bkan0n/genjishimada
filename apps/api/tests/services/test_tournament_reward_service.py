"""Unit tests for the tournament reward service.

Wave-0 scaffold. The reward service (08-02) and the outbox reset sweep (08-03)
fill this in. The placeholder below keeps the module collectible so downstream
plans' `<verify>` commands have a target.
"""

import pytest

pytestmark = [pytest.mark.domain_tournaments]


def test_reward_service_scaffold_collects() -> None:
    """Placeholder so the module collects; replaced in 08-02/08-03."""
    assert True
