---
phase: 11-tournament-verification-flow
plan: 04
type: execute
wave: 4
depends_on: ["11-02", "11-03"]
files_modified:
  - apps/api/routes/v3/tournaments.py
  - apps/api/services/tournament_service.py
  - libs/sdk/src/genjishimada_sdk/tournaments.py
  - scripts/seed-tournament-local.sh
  - apps/api/tests/services/test_tournament_service.py
  - apps/api/tests/integration/test_tournaments_integration.py
  - apps/api/tests/integration/test_tournament_rewards.py
  - apps/api/tests/repository/tournaments/test_cycle_transitions.py
autonomous: true
requirements: [SUB-01]
user_setup: []

must_haves:
  truths:
    - "D-05: POST /api/v3/tournaments/cycles/{cycle_id}/submit no longer exists and no code path writes a tournament completion without verification"
    - "D-05: cross_write_to_core + create_tournament_completion are PRESERVED (reused by the PB path); only the verification-skipping route + XP-on-submit are removed"
    - "SC-4: the bypass endpoint 404s and deleted symbols are not importable; the seed script uses a normal completion POST"
  artifacts:
    - path: "apps/api/routes/v3/tournaments.py"
      provides: "bypass submit route removed"
      contains: "TournamentsController"
    - path: "scripts/seed-tournament-local.sh"
      provides: "normal completion POST instead of bypass submit POST"
      contains: "completions"
  key_links:
    - from: "test suite"
      to: "absence of POST /cycles/{id}/submit"
      via: "old endpoint 404 + removed-symbol import guard"
      pattern: "submit"
---

<objective>
Remove the verification-skipping bypass (D-05) now that 11-02/11-03 provide the verified path. Delete the `POST /api/v3/tournaments/cycles/{cycle_id}/submit` route, the bypass `TournamentService.submit_completion` method (with its XP-on-submit), and the now-unused `TournamentCompletionCreateRequest` from the SDK; repoint the seed script to a normal completion POST; rewrite every submit-asserting test. PRESERVE `cross_write_to_core` and `create_tournament_completion` (still used by the PB path) and `award_participation`/`publish_xp_events` (now called from verification).

Purpose: SC-4 — no code path writes a tournament completion without verification.
Output: Bypass removed, dependents fixed, regression tests proving 404 + non-importable symbols.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-tournament-verification-flow/11-RESEARCH.md
@.planning/phases/11-tournament-verification-flow/11-PATTERNS.md
@.planning/phases/11-tournament-verification-flow/11-02-SUMMARY.md
@.planning/phases/11-tournament-verification-flow/11-03-SUMMARY.md

<interfaces>
<!-- Verified this session. Re-grep ALL definitions before deleting (P1: Python keeps the LAST definition; line numbers drift). -->

Bypass to remove:
- routes/v3/tournaments.py: `@litestar.post(path="/cycles/{cycle_id:int}/submit", opt={"required_scopes": {"tournaments:write"}})` → submit_completion (decorator ~L460-463, handler ~L465), body TournamentCompletionCreateRequest, returns TournamentCompletionResponse, calls tournament_service.submit_completion(cycle_id, data) ~L485, catches CycleNotFoundError/CycleNotActiveError/SlowerTimeError.
- tournament_service.py: bypass submit_completion (~L497-584): fetch_cycle; fetch_user_completion → SlowerTimeError keep-fastest gate; create_tournament_completion; cross_write_to_core; award_participation on first completion; publish_xp_events post-commit.

PRESERVE (do NOT delete):
- tournaments_repository.py cross_write_to_core (L930), create_tournament_completion (L885) — PB-path reuse (P9/A4).
- tournament_reward_service.py award_participation, publish_xp_events — now called from verify paths (11-02/11-03).
- tournament_service.py verify_tournament_completion / reject_tournament_completion (added in 11-03) — keep.

SDK:
- TournamentCompletionCreateRequest (tournaments.py L331) + __all__ entry L17 — remove ONLY after no caller remains (grep first).
- TournamentCompletionResponse (L347) — KEEP. TournamentCompletionCreatedEvent (extended in 11-01) — KEEP. TournamentVerificationChangedEvent — KEEP.

scripts/seed-tournament-local.sh: header L16 + usage hint L206 reference POST /api/v3/tournaments/cycles/{id}/submit. Replace with a normal completion POST on the cycle's map.

Tests hitting the bypass submit (rewrite the submit-asserting cases): tests/services/test_tournament_service.py, tests/integration/test_tournaments_integration.py, tests/integration/test_tournament_rewards.py, tests/repository/tournaments/test_cycle_transitions.py. (Bot tests handled in 11-05.) Known flakes/stale (NOT regressions): test_difficulty_exact_filter, TestFetchMapsFilterCategory under -n 4, is-True/False assertions vs check_active_cycle_for_category (int|None).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete the bypass route + service method; preserve PB-path helpers</name>
  <files>apps/api/routes/v3/tournaments.py, apps/api/services/tournament_service.py</files>
  <read_first>
    - apps/api/routes/v3/tournaments.py (re-grep the submit decorator + handler; confirm exact current line range before deleting — P1)
    - apps/api/services/tournament_service.py (re-grep submit_completion; confirm it is the bypass and NOT verify_tournament_completion from 11-03; confirm cross_write_to_core/create_tournament_completion are repo calls, untouched)
    - apps/api/repository/tournaments_repository.py (confirm cross_write_to_core L930 + create_tournament_completion L885 remain — PRESERVED)
    - .planning/phases/11-tournament-verification-flow/11-PATTERNS.md (§tournament_service.py REMOVE bypass; §routes REMOVE bypass; P9)
  </read_first>
  <action>
    Remove the bypass `POST /cycles/{cycle_id:int}/submit` route handler from routes/v3/tournaments.py (decorator + handler body + its try/except). Remove the bypass `submit_completion` method from tournament_service.py (the body: fetch_cycle, SlowerTimeError keep-fastest gate, create_tournament_completion + cross_write_to_core + award_participation-on-submit + publish_xp_events). Do NOT remove: the repo's cross_write_to_core/create_tournament_completion (PB-path reuse), the reward service's award_participation/publish_xp_events (verify-path use), or the 11-03 verify_tournament_completion/reject_tournament_completion methods. If SlowerTimeError is now unused, leave its definition unless grep proves zero references — note in SUMMARY. Repo-wide grep for any remaining caller of the removed route/method before finishing; use `from e` if any translation remains.
  </action>
  <verify>
    <automated>cd /Users/nebula/Documents/coding/parkour/genji/genjishimada && ! grep -rn "cycles/{cycle_id:int}/submit\|cycles/{cycle_id}/submit" apps/api/routes/ ; grep -c "async def cross_write_to_core\|async def create_tournament_completion" apps/api/repository/tournaments_repository.py</automated>
  </verify>
  <acceptance_criteria>
    - No `.../cycles/{cycle_id}/submit` route remains in apps/api/routes/ (grep returns nothing).
    - The bypass `submit_completion` is gone from tournament_service.py; `verify_tournament_completion` still present (grep count 1).
    - `grep -c "async def cross_write_to_core\|async def create_tournament_completion" apps/api/repository/tournaments_repository.py` == 2 (preserved).
    - `award_participation` + `publish_xp_events` still present in tournament_reward_service.py.
    - No remaining caller of the removed route/method under apps/api (grep clean).
    - `cd apps/api && uv run ruff check routes/v3/tournaments.py services/tournament_service.py` clean.
  </acceptance_criteria>
  <done>Bypass route + service method gone; PB-path + verify-path helpers preserved; no dangling callers.</done>
</task>

<task type="auto">
  <name>Task 2: Remove unused SDK CreateRequest, repoint seed script, rewrite submit-asserting tests</name>
  <files>libs/sdk/src/genjishimada_sdk/tournaments.py, scripts/seed-tournament-local.sh, apps/api/tests/services/test_tournament_service.py, apps/api/tests/integration/test_tournaments_integration.py, apps/api/tests/integration/test_tournament_rewards.py, apps/api/tests/repository/tournaments/test_cycle_transitions.py</files>
  <read_first>
    - libs/sdk/src/genjishimada_sdk/tournaments.py (TournamentCompletionCreateRequest L331 + __all__ L17 — grep repo-wide for callers BEFORE removing)
    - scripts/seed-tournament-local.sh (the bypass POST + header L16 + hint L206)
    - apps/api/tests/services/test_tournament_service.py, tests/integration/test_tournaments_integration.py, tests/integration/test_tournament_rewards.py, tests/repository/tournaments/test_cycle_transitions.py (the submit-asserting cases)
    - apps/api/tests/integration/test_tournaments_integration.py (how the integration client POSTs a normal completion — the replacement path)
    - .planning/phases/11-tournament-verification-flow/11-VALIDATION.md (SC-4 validation: old endpoint 404s; no unverified tournament write)
    - .planning/phases/11-tournament-verification-flow/11-PATTERNS.md (§tests; §scripts)
  </read_first>
  <action>
    1. SDK: grep repo-wide for `TournamentCompletionCreateRequest`. If the only remaining references are its definition + __all__ entry, remove both; otherwise fix callers first. Run `just fix` after the SDK edit (workspace package). KEEP TournamentCompletionResponse, TournamentCompletionCreatedEvent, TournamentVerificationChangedEvent.
    2. Seed script: replace the bypass `POST /api/v3/tournaments/cycles/{id}/submit` with a normal completion POST on the active cycle's map (the verified auto-detect path); update header comment (L16) + usage hint (L206) to describe the new flow. Keep runnable via bash.
    3. Tests: rewrite every submit-asserting case to use the new verified path. Add an SC-4 regression test asserting the old endpoint returns 404 (route absent) and that `TournamentCompletionCreateRequest` (if removed) is not importable. Replace any test asserting a tournament completion was written verified-on-submit with one asserting it is written unverified and only becomes verified after the verify endpoint/OCR. Do NOT write is-True/False assertions against check_active_cycle_for_category (int|None). Run targeted with -p no:xdist (mind known flakes).
  </action>
  <verify>
    <automated>cd /Users/nebula/Documents/coding/parkour/genji/genjishimada && just fix >/dev/null 2>&1 ; uv run --directory apps/api pytest tests/services/test_tournament_service.py tests/integration/test_tournaments_integration.py tests/integration/test_tournament_rewards.py tests/repository/tournaments/test_cycle_transitions.py --no-testmon -p no:xdist -q 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "TournamentCompletionCreateRequest" apps/ libs/ scripts/` shows no usage outside (removed) definition (or, if kept, a documented remaining caller).
    - The seed script no longer POSTs the bypass submit endpoint (`grep -n "cycles/.*/submit" scripts/seed-tournament-local.sh` returns nothing).
    - An SC-4 test asserts the old endpoint 404s and (if removed) the CreateRequest import fails.
    - All four rewritten test files pass under `-p no:xdist --no-testmon` (excluding the documented pre-existing flakes).
    - `cd apps/api && uv run ruff check` clean for edited files.
  </acceptance_criteria>
  <done>SDK CreateRequest removed (or documented), seed script repointed, all submit-asserting tests rewritten and green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → tournament API | removing the bypass closes an unauthenticated-of-verification write path; the only remaining write is the verified pipeline |
| seed script → API | local seeding must not reintroduce an unverified write |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-14 | Elevation of Privilege | residual bypass write path | mitigate | route + service method deleted; SC-4 test asserts 404 + no code path writes a tournament completion without verification |
| T-11-15 | Tampering | accidental deletion of preserved PB-path helpers | mitigate | acceptance grep asserts cross_write_to_core + create_tournament_completion remain (count 2); verify_tournament_completion remains |
| T-11-16 | Repudiation | seed script reintroducing unverified writes | mitigate | seed repointed to the normal verified completion POST; grep asserts no submit-endpoint reference remains |
| T-11-SC | Tampering | npm/pip/cargo installs | mitigate | no package installs in this plan; no install task present |
</threat_model>

<verification>
- `! grep -rn "cycles/.*/submit" apps/api/routes/ scripts/` (no bypass references).
- `grep -c "async def cross_write_to_core\|async def create_tournament_completion" apps/api/repository/tournaments_repository.py` == 2.
- `uv run --directory apps/api pytest tests/services/test_tournament_service.py tests/integration/test_tournaments_integration.py tests/integration/test_tournament_rewards.py tests/repository/tournaments/test_cycle_transitions.py --no-testmon -p no:xdist -q` green (excl. documented flakes).
- `cd apps/api && uv run ruff check routes/v3/tournaments.py services/tournament_service.py` clean.
</verification>

<success_criteria>
- D-05: bypass route + service method removed; PB-path helpers + verify-path methods preserved.
- SC-4: old endpoint 404s; removed symbols not importable; seed script + tests reflect the verified path.
</success_criteria>

<output>
Create `.planning/phases/11-tournament-verification-flow/11-04-SUMMARY.md` when done. Record: whether TournamentCompletionCreateRequest + SlowerTimeError were removed or retained (with caller evidence), and the new seed-script flow.
</output>
