---
phase: 11-tournament-verification-flow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - libs/sdk/src/genjishimada_sdk/tournaments.py
  - libs/sdk/src/genjishimada_sdk/auth.py
  - apps/api/events/schemas.py
  - apps/api/repository/tournaments_repository.py
autonomous: true
requirements: [SUB-01]
must_haves:
  truths:
    - "D-01: get_active_cycle_by_map_id(map_id) returns the active cycle row for a tournament map, enabling auto-detect on normal submit"
    - "D-04: set_tournament_verified(tournament_completion_id) exists so both verify paths can mark a tournament row verified"
    - "D-04: TournamentVerificationChangedEvent and an extended TournamentCompletionCreatedEvent are defined as the contracts for the non-PB tournament verification surface"
    - "tournaments:verify scope is registered in the AuthScope enum so the new verify endpoints can be guarded"
  artifacts:
    - path: "libs/sdk/src/genjishimada_sdk/tournaments.py"
      provides: "Extended TournamentCompletionCreatedEvent + new TournamentVerificationChangedEvent + __all__ entry"
      contains: "class TournamentVerificationChangedEvent"
    - path: "libs/sdk/src/genjishimada_sdk/auth.py"
      provides: "TOURNAMENTS_VERIFY scope enum member"
      contains: "TOURNAMENTS_VERIFY"
    - path: "apps/api/events/schemas.py"
      provides: "TournamentOcrVerificationRequestedEvent for the in-process OCR variant"
      contains: "class TournamentOcrVerificationRequestedEvent"
    - path: "apps/api/repository/tournaments_repository.py"
      provides: "get_active_cycle_by_map_id + set_tournament_verified + fetch_tournament_completion"
      contains: "async def get_active_cycle_by_map_id"
  key_links:
    - from: "apps/api/repository/tournaments_repository.py"
      to: "tournaments.cycles"
      via: "SELECT WHERE map_id=$1 AND status='active' (idx_cycles_map_id)"
      pattern: "get_active_cycle_by_map_id"
    - from: "apps/api/repository/tournaments_repository.py"
      to: "tournaments.completions"
      via: "UPDATE ... SET verified=$2 WHERE id=$1"
      pattern: "set_tournament_verified"
---

<objective>
Define every NEW contract Phase 11 builds against: SDK event structs, the new auth scope, the in-process OCR event schema, and the three new tournament repository lookups. This is the Wave 0 "interface contracts first" plan — downstream plans (11-02 service/endpoints, 11-03 completions hooks, 11-04 bot) consume these symbols directly, no codebase scavenger hunt.

Purpose: Establish stable contracts so Wave 2/3 plans implement against them in parallel.
Output: SDK struct/event + scope additions, one event schema, three repo methods. No business logic, no wiring.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-tournament-verification-flow/11-CONTEXT.md
@.planning/phases/11-tournament-verification-flow/11-RESEARCH.md
@.planning/phases/11-tournament-verification-flow/11-PATTERNS.md

<interfaces>
<!-- Existing contracts extracted from codebase. Use directly. -->

AuthScope enum — libs/sdk/src/genjishimada_sdk/auth.py:16-28 (StrEnum):
  MAPS_READ="maps:read" ... COMPLETIONS_VERIFY="completions:verify"
  TOURNAMENTS_READ="tournaments:read", TOURNAMENTS_WRITE="tournaments:write"
  (NO tournaments:verify yet — this plan adds it; __all__ already exports AuthScope.)

tournaments.py SDK (libs/sdk/src/genjishimada_sdk/tournaments.py):
  - __all__ is a sorted tuple (~L8-31) — add new event name preserving alphabetical order.
  - TournamentCompletionCreatedEvent at ~L441 currently carries only completion_id, cycle_id (slim).
  - TournamentCompletionResponse at ~L347 (KEEP).
  - TournamentCompletionCreateRequest at ~L331 (do NOT delete here — 11-02 removes after bypass gone).
  - Event types live in the "# Event types (RabbitMQ)" section (~L399).
  Analog event shape: genjishimada_sdk/completions.py CompletionCreatedEvent / VerificationChangedEvent.

events/schemas.py — OcrVerificationRequestedEvent at L12 (msgspec.Struct):
  fields mirror (completion_id, user_id, code, time, screenshot). Analog for the tournament variant.

tournaments_repository.py existing convention (re-grep line numbers at edit time — P1 drift):
  - check_active_cycle_for_category (~L257): fetchrow + _get_connection(conn) + $N params.
  - fetch_user_completion (~L1055): SELECT ... ORDER BY verified DESC, time ASC LIMIT 1.
  - create_tournament_completion (~L885): plain INSERT ... RETURNING *, error-translates UniqueViolation/FK → domain.
  - All methods: `*, conn: Connection | None = None` keyword-only; `_conn = self._get_connection(conn)`.
  - Index idx_cycles_map_id exists (migration 0020:72). UNIQUE on tournaments.completions is (cycle_id,user_id,inserted_at).
  - tournaments.completions columns: id, cycle_id, user_id, map_id, time numeric(10,2), screenshot, video, verified bool, completion bool, inserted_at. NO verified_by/verified_at.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add tournaments:verify scope + SDK events</name>
  <files>libs/sdk/src/genjishimada_sdk/auth.py, libs/sdk/src/genjishimada_sdk/tournaments.py, apps/api/events/schemas.py</files>
  <read_first>
    - libs/sdk/src/genjishimada_sdk/auth.py (full — AuthScope StrEnum + __all__)
    - libs/sdk/src/genjishimada_sdk/tournaments.py L1-40 (__all__) and L399-end (event section, TournamentCompletionCreatedEvent)
    - libs/sdk/src/genjishimada_sdk/completions.py (CompletionCreatedEvent, VerificationChangedEvent as the event template)
    - apps/api/events/schemas.py (OcrVerificationRequestedEvent)
  </read_first>
  <action>
    In auth.py: add enum member `TOURNAMENTS_VERIFY = "tournaments:verify"` to AuthScope, placed adjacent to the existing TOURNAMENTS_READ/TOURNAMENTS_WRITE members (per D-05/security — the new tournament verify endpoints in 11-02 are guarded by this scope). AuthScope is already in __all__; no __all__ change needed.

    In tournaments.py: (a) EXTEND the existing TournamentCompletionCreatedEvent (currently completion_id, cycle_id) to also carry user_id: int, time: float, video: str | None, screenshot: str — so the bot Accept/Reject consumer (11-04) renders without a round-trip (RESEARCH §New events, "extend for fewer round-trips"). (b) ADD new `class TournamentVerificationChangedEvent(Struct)` with fields tournament_completion_id: int, cycle_id: int, user_id: int, verified: bool, time: float — mirror the plain-Struct shape of completions.VerificationChangedEvent. Place both in the "# Event types (RabbitMQ)" section. (c) Add "TournamentVerificationChangedEvent" to the sorted __all__ tuple preserving alphabetical order. Do NOT touch TournamentCompletionCreateRequest/Response here (11-02 owns removal).

    In events/schemas.py: ADD `class TournamentOcrVerificationRequestedEvent(Struct)` mirroring OcrVerificationRequestedEvent but with fields tournament_completion_id: int, cycle_id: int, user_id: int, code: str, time: float, screenshot: str (in-process event for the non-PB OCR variant consumed by 11-03's tournament OCR listener).

    Run `just fix` (or `just sync`) after editing the SDK so API + bot resolve the workspace package (MEMORY: ModuleNotFoundError fix). Match msgspec Struct field conventions (Python 3.10+ union `str | None`, no Optional).
  </action>
  <verify>
    <automated>cd /Users/nebula/Documents/coding/parkour/genji/genjishimada && uv run --directory apps/api python -c "from genjishimada_sdk.tournaments import TournamentVerificationChangedEvent, TournamentCompletionCreatedEvent; from genjishimada_sdk.auth import AuthScope; from apps.api.events.schemas import TournamentOcrVerificationRequestedEvent; assert AuthScope.TOURNAMENTS_VERIFY == 'tournaments:verify'; assert {'user_id','time','video','screenshot'} <= set(TournamentCompletionCreatedEvent.__struct_fields__)" 2>&1 || uv run --directory libs/sdk python -c "from genjishimada_sdk.tournaments import TournamentVerificationChangedEvent; from genjishimada_sdk.auth import AuthScope; assert AuthScope.TOURNAMENTS_VERIFY=='tournaments:verify'"</automated>
  </verify>
  <acceptance_criteria>
    - AuthScope.TOURNAMENTS_VERIFY resolves to "tournaments:verify".
    - TournamentCompletionCreatedEvent carries user_id, time, video, screenshot (in addition to completion_id, cycle_id).
    - TournamentVerificationChangedEvent importable with the 5 named fields; present in __all__ in sorted order.
    - TournamentOcrVerificationRequestedEvent importable from events/schemas.py.
    - `just lint-sdk` passes; `just fix` run so API/bot resolve the SDK.
  </acceptance_criteria>
  <done>New scope + two event additions + one in-process event schema exist, import cleanly, and lint passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Tournament repository lookups for verification</name>
  <files>apps/api/repository/tournaments_repository.py, apps/api/tests/repository/tournaments/test_verification_lookups.py</files>
  <read_first>
    - apps/api/repository/tournaments_repository.py: check_active_cycle_for_category (~L257), fetch_user_completion (~L1055), create_tournament_completion (~L885), and the BaseRepository _get_connection usage at the top of any method.
    - apps/api/migrations/0020_tournaments.sql L59-99 (cycles + completions columns, idx_cycles_map_id)
    - apps/api/tests/repository/tournaments/ (existing fixtures: cycle factory, completion factory) — reuse them; do not invent new DB setup
  </read_first>
  <behavior>
    - get_active_cycle_by_map_id: given an ACTIVE cycle pinned to map_id M, returns a row (id, category_id, map_id, status='active') for M; returns None for a map with no active cycle; returns None when the only cycle for M is status != 'active' (pending/finalizing/completed).
    - set_tournament_verified: flips tournaments.completions.verified to TRUE for the given id and returns {id, cycle_id, user_id, time}; returns None for a non-existent id; calling twice is harmless (still verified=TRUE).
    - fetch_tournament_completion: returns the full tournament completion row dict for an id; None if absent.
  </behavior>
  <action>
    Add three methods to TournamentRepository, each `*, conn: Connection | None = None` keyword-only, `_conn = self._get_connection(conn)`, asyncpg `$N` positional params only (never f-string SQL — CLAUDE.md):

    1. `async def get_active_cycle_by_map_id(self, map_id: int, *, conn=None) -> dict | None` — `SELECT id, category_id, map_id, status FROM tournaments.cycles WHERE map_id = $1 AND status = 'active' LIMIT 1`; return `dict(row) if row else None`. Relies on existing idx_cycles_map_id (D-01: auto-detect keys off the active cycle's map_id).

    2. `async def set_tournament_verified(self, tournament_completion_id: int, verified: bool = True, *, conn=None) -> dict | None` — `UPDATE tournaments.completions SET verified = $2 WHERE id = $1 RETURNING id, cycle_id, user_id, time`; return `dict(row) if row else None` (D-04: both verify paths mark the tournament row verified). The table has no verified_by/verified_at columns — do not reference them.

    3. `async def fetch_tournament_completion(self, tournament_completion_id: int, *, conn=None) -> dict | None` — `SELECT * FROM tournaments.completions WHERE id = $1`; return `dict(row) if row else None` (used by the verify endpoint service + bot detail fetch fallback).

    Add Google-style docstrings. Place near the other completion methods (after create_tournament_completion / fetch_user_completion).
  </action>
  <verify>
    <automated>cd /Users/nebula/Documents/coding/parkour/genji/genjishimada && uv run --directory apps/api pytest tests/repository/tournaments/test_verification_lookups.py -v -p no:xdist</automated>
  </verify>
  <acceptance_criteria>
    - test_verification_lookups.py covers: active-cycle hit, non-active cycle miss, no-cycle miss for get_active_cycle_by_map_id; verify-flip + non-existent-id None + double-call idempotent for set_tournament_verified; row + None for fetch_tournament_completion.
    - All three methods accept `conn=` and use `$N` positional params.
    - Tests green via the listed command.
  </acceptance_criteria>
  <done>Three repo methods exist with tests proving active-cycle lookup, verified flip, and completion fetch behave per the behavior block.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SDK consumers (API + bot) | Struct field contracts shared across processes; field drift breaks decode |
| client → API (future endpoints) | tournaments:verify scope string must match enum + guard exactly |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Tampering | get_active_cycle_by_map_id / set_tournament_verified SQL | mitigate | asyncpg `$1`/`$2` positional params only; never f-string SQL (CLAUDE.md) |
| T-11-02 | Elevation of Privilege | tournaments:verify scope string | mitigate | Register canonical "tournaments:verify" in AuthScope so 11-02 guards bind to a single source of truth; superuser-only/bot-token grant enforced in guard |
| T-11-03 | Information disclosure | extended TournamentCompletionCreatedEvent carries user_id/time/video/screenshot | accept | Same fields already cross the bus for completions; consumed only by the verification channel (bot), not user-facing broadcast |
| T-11-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (RESEARCH: zero external packages added) |
</threat_model>

<verification>
- `just lint-sdk` and `just lint-api` pass.
- `uv run --directory apps/api pytest tests/repository/tournaments/test_verification_lookups.py -v -p no:xdist` green.
- `from genjishimada_sdk.auth import AuthScope; AuthScope.TOURNAMENTS_VERIFY` resolves.
</verification>

<success_criteria>
All Phase 11 NEW contracts (scope, 2 SDK events, 1 in-process event schema, 3 repo lookups) exist, import cleanly, lint clean, and the repo methods have passing behavior tests. No business logic or wiring added.
</success_criteria>

<output>
Create `.planning/phases/11-tournament-verification-flow/11-01-SUMMARY.md` when done. Record: final field lists for both events, the exact repo method signatures, and confirm `just fix` was run so API+bot resolve the SDK.
</output>
