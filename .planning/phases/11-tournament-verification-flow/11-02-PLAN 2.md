---
phase: 11-tournament-verification-flow
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - apps/api/services/tournament_service.py
  - apps/api/routes/v3/tournaments.py
  - infra/rabbitmq/definitions.json
  - libs/sdk/src/genjishimada_sdk/tournaments.py
  - scripts/seed-tournament-local.sh
autonomous: true
requirements: [SUB-01]
must_haves:
  truths:
    - "D-05: POST /api/v3/tournaments/cycles/{cycle_id}/submit no longer exists and the bypass TournamentService.submit_completion is deleted — no code path writes a tournament completion without verification"
    - "D-04: PATCH /api/v3/tournaments/completions/{id}/verify marks a tournament completion verified via TournamentService.verify_tournament_completion (single-writer; bot calls this, never writes DB)"
    - "D-06: participation XP is granted inside verify_tournament_completion (not on submission), idempotent via the xp_grants UNIQUE(cycle_id,user_id,reason) ledger"
    - "D-04: the two new tournament queues api.tournament.completion.created and api.tournament.verification.changed exist in definitions.json WITH DLQs (Phase 09-03 pattern)"
    - "D-03: cross_write_to_core's PB-gated CTE is PRESERVED (only the bypass route + XP-on-submit are removed)"
  artifacts:
    - path: "apps/api/services/tournament_service.py"
      provides: "verify_tournament_completion (+reject); bypass submit_completion removed"
      contains: "async def verify_tournament_completion"
    - path: "apps/api/routes/v3/tournaments.py"
      provides: "PATCH .../completions/{id}/verify and /reject endpoints (scope tournaments:verify); bypass submit route removed"
      contains: "verify"
    - path: "infra/rabbitmq/definitions.json"
      provides: "2 new tournament queues + 2 DLQs"
      contains: "api.tournament.verification.changed"
  key_links:
    - from: "apps/api/routes/v3/tournaments.py"
      to: "TournamentService.verify_tournament_completion"
      via: "PATCH /tournaments/completions/{id}/verify handler"
      pattern: "verify_tournament_completion"
    - from: "apps/api/services/tournament_service.py"
      to: "tournaments.completions + xp_grants ledger"
      via: "set_tournament_verified + award_participation in one transaction"
      pattern: "set_tournament_verified"
---

<objective>
Build the tournament-native verification surface API-side and remove the bypass. Add TournamentService.verify_tournament_completion (sets verified, grants participation XP idempotently, publishes TournamentVerificationChangedEvent), wire the verify/reject endpoints, delete the verification-skipping bypass (D5), and declare the two new RabbitMQ queues with DLQs. Preserve cross_write_to_core for the PB path (D4a, owned by 11-03).

Purpose: Give the bot (11-04) and the completions OCR path (11-03) a single verified-write entry point and close the bypass.
Output: New service verify/reject methods, two endpoints, definitions.json queues, bypass deletion, seed-script fix.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-tournament-verification-flow/11-CONTEXT.md
@.planning/phases/11-tournament-verification-flow/11-RESEARCH.md
@.planning/phases/11-tournament-verification-flow/11-PATTERNS.md
@.planning/phases/11-tournament-verification-flow/11-01-SUMMARY.md

<interfaces>
<!-- From 11-01 (consume directly) + existing code. Re-grep line numbers at edit time — P1 drift. -->

From 11-01:
  - tournament_repo.set_tournament_verified(tc_id, verified=True, *, conn=None) -> dict|None ({id,cycle_id,user_id,time})
  - tournament_repo.fetch_tournament_completion(tc_id, *, conn=None) -> dict|None
  - SDK TournamentVerificationChangedEvent(tournament_completion_id, cycle_id, user_id, verified, time)
  - AuthScope.TOURNAMENTS_VERIFY = "tournaments:verify"

Existing tournament_service.py (TournamentService(BaseService)):
  - __init__ at ~L63: (pool, state, tournament_repo, reward_service=None). reward_service injected by DI (provide_tournament_service ~L629). provide_tournament_reward_service exists.
  - BYPASS submit_completion at ~L497-584 (REMOVE): fetch_cycle→CycleNotFound/CycleNotActive (~L522), fetch_user_completion→SlowerTimeError keep-fastest (~L531-537), create_tournament_completion (~L541), cross_write_to_core (~L551), award_participation on first completion (~L573), publish_xp_events post-commit (~L580-582).
  - PRESERVE cross_write_to_core (repo ~L930) — 11-03 PB path uses it.

reward_service.award_participation(cycle, user_id, conn) at tournament_reward_service.py:136 → _grant_xp(reason_key="participation"); idempotent via claim_xp_grant ON CONFLICT (cycle_id,user_id,reason) DO NOTHING. publish_xp_events at :122 flushes deferred XpGrantEvent post-commit.

Existing routes/v3/tournaments.py:
  - class TournamentsController(litestar.Controller) ~L54: tags, path="/tournaments", dependencies dict of Provide(provide_*).
  - BYPASS submit route at ~L443-466: @post("/cycles/{cycle_id:int}/submit", opt={"required_scopes":{"tournaments:write"}}) → submit_completion(cycle_id, tournament_service, data: TournamentCompletionCreateRequest) -> TournamentCompletionResponse, catches CycleNotFound/CycleNotActive/SlowerTimeError. (REMOVE.)
  - Verify-route TEMPLATE (completions): routes/v3/completions.py:124 @patch(path="/{record_id:int}/verify", opt={"required_scopes":{"completions:verify"}}).

publish_message guard (services/base.py): api.tournament.* NOT in IGNORE_IDEMPOTENCY → MUST pass idempotency_key. Use f"tournament:verify:{tc_id}".

definitions.json queue block template (existing api.tournament.cycle_started + .dlq) — each main queue: x-dead-letter-exchange "", x-dead-letter-routing-key "<name>.dlq", x-queue-type classic; plus standalone <name>.dlq with x-queue-type classic. (Phase 09-03 pattern, commits 4c31fa4/33059e7.)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: verify_tournament_completion + reject service methods + endpoints</name>
  <files>apps/api/services/tournament_service.py, apps/api/routes/v3/tournaments.py, apps/api/tests/services/test_tournament_service.py</files>
  <read_first>
    - apps/api/services/tournament_service.py: __init__ (~L63), submit_completion (~L497-584) for the transaction + publish_xp_events pattern, get_config for service-method shape.
    - apps/api/services/completions_service.py: verify_completion (~L575-652) as the structural template (acquire conn + transaction, update, publish event, post-commit flush).
    - apps/api/services/tournament_reward_service.py: award_participation (L136), publish_xp_events (L122).
    - apps/api/routes/v3/tournaments.py: controller header (~L54-63), an existing @patch/@post handler with domain→CustomHTTPException translation.
    - apps/api/services/exceptions/tournaments.py: existing TournamentsError subclasses (for a TournamentCompletionNotFoundError if not present).
  </read_first>
  <behavior>
    - verify_tournament_completion(tc_id): when the tournament row exists → sets verified=TRUE, grants participation XP for (cycle_id,user_id) exactly once even if called twice, publishes TournamentVerificationChangedEvent on api.tournament.verification.changed (skipped under X-PYTEST-ENABLED), returns the verified row. When tc_id absent → raises TournamentCompletionNotFoundError (→ 404 at route).
    - reject_tournament_completion(tc_id): leaves verified=FALSE (no-op flip / explicit set FALSE), no XP, returns the row; absent id → 404. (Open Q1 resolved: keep verified=FALSE — simplest, stays below verified on the board.)
    - Calling verify twice grants XP once (ledger UNIQUE) and is otherwise harmless.
  </behavior>
  <action>
    In tournament_service.py ADD `async def verify_tournament_completion(self, tournament_completion_id: int) -> dict` mirroring completions_service.verify_completion structure: `async with self._pool.acquire() as conn, conn.transaction():` → `fetch_tournament_completion` (raise TournamentCompletionNotFoundError if None) → `set_tournament_verified(tc_id, True, conn=conn)` → if reward_service present, `await self.reward_service.award_participation(cycle_row, user_id, conn)` collecting deferred events (D-06; idempotent via ledger — never branch on "already granted", Pitfall P5) → publish TournamentVerificationChangedEvent(tournament_completion_id, cycle_id, user_id, verified=True, time) via `self.publish_message(routing_key="api.tournament.verification.changed", data=..., idempotency_key=f"tournament:verify:{tc_id}")` → after commit `await self.reward_service.publish_xp_events(...)` (existing post-commit flush pattern at submit_completion ~L580-582). ADD `async def reject_tournament_completion(self, tournament_completion_id: int) -> dict` → fetch (404 if absent) → `set_tournament_verified(tc_id, False, conn=conn)` → return row; no XP, no verify event.

    Add `TournamentCompletionNotFoundError(TournamentsError)` to services/exceptions/tournaments.py + barrel __init__ if not already present (mirror existing aliased-barrel convention).

    In routes/v3/tournaments.py ADD two handlers on TournamentsController, mirroring the completions verify decorator: `@patch(path="/completions/{tournament_completion_id:int}/verify", opt={"required_scopes": {"tournaments:verify"}})` → `verify_tournament_completion` and `@patch(path="/completions/{tournament_completion_id:int}/reject", opt={"required_scopes": {"tournaments:verify"}})` → `reject_tournament_completion`. Typed `:int` path param (V5 input validation). Catch TournamentCompletionNotFoundError → CustomHTTPException 404. Return the verified/rejected row (reuse an existing tournament response struct or a minimal dict response consistent with controller conventions).
  </action>
  <verify>
    <automated>cd /Users/nebula/Documents/coding/parkour/genji/genjishimada && uv run --directory apps/api pytest tests/services/test_tournament_service.py -v -p no:xdist -k "verify or reject"</automated>
  </verify>
  <acceptance_criteria>
    - verify_tournament_completion sets verified=TRUE, grants participation XP once across double-call (assert one ledger row), publishes TournamentVerificationChangedEvent (mock/skip publish), 404s on absent id.
    - reject_tournament_completion leaves verified=FALSE, grants no XP, 404s on absent id.
    - Endpoints registered at PATCH /tournaments/completions/{id}/verify and /reject with scope tournaments:verify, typed :int path param.
    - `just lint-api` passes.
  </acceptance_criteria>
  <done>Tournament verify/reject service methods + endpoints exist, are scope-guarded, grant XP idempotently on verify, and have passing unit tests.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Remove the bypass (D5) + declare new queues</name>
  <files>apps/api/routes/v3/tournaments.py, apps/api/services/tournament_service.py, libs/sdk/src/genjishimada_sdk/tournaments.py, infra/rabbitmq/definitions.json, scripts/seed-tournament-local.sh</files>
  <read_first>
    - apps/api/routes/v3/tournaments.py: bypass @post submit route (~L443-466) — re-grep exact start/end at edit time (P1).
    - apps/api/services/tournament_service.py: bypass submit_completion (~L497-584) — re-grep exact bounds.
    - libs/sdk/src/genjishimada_sdk/tournaments.py: TournamentCompletionCreateRequest (~L331) + its __all__ entry.
    - infra/rabbitmq/definitions.json: the api.tournament.cycle_started queue + .dlq blocks (copy exactly).
    - scripts/seed-tournament-local.sh: the bypass POST to /cycles/{id}/submit (header ~line 16; the actual POST call).
    - grep for any other caller of submit_completion / TournamentCompletionCreateRequest / cross_write_to_core before deleting (P1).
  </read_first>
  <action>
    D-05 coordinated deletion (re-grep ALL definitions first — P1):
    - Delete the bypass route `@post("/cycles/{cycle_id:int}/submit" ...)` handler from TournamentsController.
    - Delete `TournamentService.submit_completion` (the whole bypass method). PRESERVE `cross_write_to_core` in the repo and PRESERVE `create_tournament_completion` — 11-03's PB path needs them (Pitfall P9/A4). Do NOT remove award_participation from the reward service (D-06 moved it into verify_tournament_completion in Task 1; the PB path in 11-03 also calls it).
    - Remove `TournamentCompletionCreateRequest` from tournaments.py SDK + its `__all__` entry (no path uses it after the bypass is gone — confirm via grep). KEEP TournamentCompletionResponse and TournamentCompletionCreatedEvent.
    - In definitions.json add 4 entries mirroring the cycle_started block exactly: queues `api.tournament.completion.created` (+ `api.tournament.completion.created.dlq`) and `api.tournament.verification.changed` (+ `api.tournament.verification.changed.dlq`). Each main queue: durable true, auto_delete false, arguments {x-dead-letter-exchange "", x-dead-letter-routing-key "<name>.dlq", x-queue-type "classic"}; each DLQ: durable true, x-queue-type "classic". Keep JSON valid (commas/order). Tournament XP stays on api.xp.grant — do NOT add api.tournament.xp.grant.
    - In seed-tournament-local.sh replace the bypass POST to `/cycles/{id}/submit` with a normal completion POST on the cycle's map (POST /api/v3/completions/ with the cycle map code) so local seeding still produces tournament-relevant runs through the new auto-detect path. Update the header comment (line ~16).
  </action>
  <verify>
    <automated>cd /Users/nebula/Documents/coding/parkour/genji/genjishimada && python3 -c "import json; d=json.load(open('infra/rabbitmq/definitions.json')); n=[q['name'] for q in d['queues']]; assert 'api.tournament.completion.created' in n and 'api.tournament.completion.created.dlq' in n and 'api.tournament.verification.changed' in n and 'api.tournament.verification.changed.dlq' in n, n" && ! grep -rn "cycles/{cycle_id:int}/submit\|cycles/{cycle_id}/submit" apps/api/routes/v3/tournaments.py && ! grep -rn "def submit_completion" apps/api/services/tournament_service.py && ! grep -rn "TournamentCompletionCreateRequest" apps/api libs/sdk --include="*.py" | grep -v test