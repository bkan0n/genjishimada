---
phase: 11-tournament-verification-flow
plan: 03
type: execute
wave: 3
depends_on: ["11-01", "11-02"]
files_modified:
  - apps/api/services/completions_service.py
  - apps/api/events/completions.py
autonomous: true
requirements: [SUB-01]
must_haves:
  truths:
    - "D-01: a normal completion on the active cycle's map is auto-detected (get_active_cycle_by_map_id) and considered for that tournament with no separate submit step"
    - "D-04a: a new-PB run on the active map inserts the tournament row + sets core.completions.tournament_completion_id in the SAME submit transaction; verify_completion marks the linked tournament row verified + grants participation XP (no second embed)"
    - "D-04b: a slower-than-PB run on the active map is caught from the 0017 speed trigger (asyncpg CheckViolationError), inserted into tournaments.completions, and routed to tournament verification (OCR variant for no-video, mod queue for video)"
    - "D-07: on an active tournament map a valid slower-than-PB run is accepted (recorded, pending verification) instead of raising SlowerThanPendingError / hitting the speed trigger; non-tournament maps keep the existing HTTP 400 unchanged"
    - "D-06: participation XP for the PB path is granted inside verify_completion, idempotent via the xp_grants ledger"
    - "D-03: no slower-than-PB row is inserted into core.completions (guaranteed by the 0017 trigger + PB-gated path)"
  artifacts:
    - path: "apps/api/services/completions_service.py"
      provides: "auto-detect tournament branch in submit_completion; PB tournament side-effect in verify_completion; attempt_tournament_auto_verify_async OCR variant"
      contains: "get_active_cycle_by_map_id"
    - path: "apps/api/events/completions.py"
      provides: "tournament.ocr.requested in-process listener"
      contains: "tournament.ocr.requested"
  key_links:
    - from: "apps/api/services/completions_service.py submit_completion"
      to: "tournaments.completions + core.completions.tournament_completion_id"
      via: "active-cycle branch inside the existing conn.transaction()"
      pattern: "get_active_cycle_by_map_id"
    - from: "apps/api/services/completions_service.py verify_completion"
      to: "TournamentService.set_tournament_verified + award_participation"
      via: "in-service side-effect resolving cycle from code (event has no map_id)"
      pattern: "set_tournament_verified"
    - from: "apps/api/services/completions_service.py non-PB catch"
      to: "tournament.ocr.requested / api.tournament.completion.created"
      via: "CheckViolationError branch → tournament insert → OCR variant or mod queue"
      pattern: "CheckViolationError"
---

<objective>
Wire tournament logic into the shared completion hot path. Auto-detect active-cycle runs (D1), branch PB vs non-PB at the insert site, set the PB tournament side-effect inside verify_completion (D4a), catch the 0017 speed trigger to record + route non-PB runs to tournament verification (D4b/D7), and add the tournament OCR variant + in-process listener. This is the highest-risk plan (shared submit path) — TDD-gated with explicit regression behavior.

Purpose: Make verified tournament-relevant runs reach the board via normal submission, without breaking normal (non-tournament) submission.
Output: completions_service auto-detect + PB side-effect + non-PB catch + OCR variant; one new in-process listener.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-tournament-verification-flow/11-CONTEXT.md
@.planning/phases/11-tournament-verification-flow/11-RESEARCH.md
@.planning/phases/11-tournament-verification-flow/11-PATTERNS.md
@.planning/phases/11-tournament-verification-flow/11-01-SUMMARY.md
@.planning/phases/11-tournament-verification-flow/11-02-SUMMARY.md

<interfaces>
<!-- Consume from 11-01/11-02 + existing code. Re-grep line numbers at edit time — P1 drift. -->

CONFIRMED P7 (speed-trigger exception): 0017/0012 trigger does `RAISE EXCEPTION ... USING ERRCODE = '23514'` (CHECK violation). insert_completion (completions_repository.py:1551-1559) catches ONLY UniqueViolationError + ForeignKeyViolationError — it does NOT catch CheckViolationError, so the trigger surfaces from insert_completion as a RAW asyncpg.exceptions.CheckViolationError. The D4b catch must branch ONLY on CheckViolationError (import from asyncpg.exceptions) and let Unique/FK propagate. SlowerThanPendingError is the SEPARATE explicit pending-faster precheck raised BEFORE insert (submit_completion ~L439-440) — do NOT conflate.

Existing completions_service.py (re-grep bounds):
  - submit_completion (~L413-501): map-exists check (~L430-432); `async with self._pool.acquire() as conn, conn.transaction():` (~L434); pending precheck → SlowerThanPendingError (~L439-440); insert_completion (~L442-444); get_suspicious_flags (~L447); no-video + not suspicious → `request.app.emit("completion.ocr.requested", OcrVerificationRequestedEvent(...), svc=self, users=..., notifications=...)` then early return (~L478-492); else publish CompletionCreatedEvent on api.completion.submission (~L494-501).
  - verify_completion (~L575-652): check_completion_exists (~L600), fetch_completion_for_moderation (~L604 → user_id/code/old_time/old_verified), update_verification (~L612), quest progress, publish VerificationChangedEvent(completion_id, verified, verified_by, reason) on api.completion.verification (~L639-651). EVENT HAS NO map_id (P8).
  - attempt_auto_verify_async (~L281-412): OCR HTTP block (~L307-332): hostname switch, POST http://{host}:8000/extract json={image_url, code, time, names}, msgspec.json.decode(..., OcrResponse), 3-way match (~L330-332); on match verify_completion_with_pool(completion_id) (~L340); on mismatch publish FailedAutoverifyEvent + CompletionCreatedEvent (~L345-366). idempotency key shape ~L305.
  - __init__ (~L86): (pool, state, completions_repo) — NO tournament dep yet. provide_completions_service (~L922): (state, completions_repo).

From 11-01:
  - tournament_repo.get_active_cycle_by_map_id(map_id, *, conn) -> dict|None (id, category_id, map_id, status)
  - tournament_repo.set_tournament_verified(tc_id, verified=True, *, conn) -> dict|None
  - SDK TournamentOcrVerificationRequestedEvent(tournament_completion_id, cycle_id, user_id, code, time, screenshot) in events/schemas.py
From 11-02:
  - tournament_service.verify_tournament_completion(tc_id) (terminal for OCR-match on non-PB)
  - queue api.tournament.completion.created (mod review for non-PB video), api.tournament.verification.changed
  - SDK TournamentCompletionCreatedEvent(completion_id, cycle_id, user_id, time, video, screenshot)

Existing repo: create_tournament_completion(cycle_id, user_id, map_id, time, screenshot, video, *, conn) -> row (RETURNING *); cross_write_to_core PRESERVED (PB-gated CTE). insert_completion(code, user_id, time, screenshot, video, *, conn) -> int (no tournament_completion_id param yet).

DI to reach tournament logic: inject TournamentService into CompletionsService via TYPE_CHECKING-guarded import to avoid circular import (CLAUDE.md). provide_tournament_service exists (tournament_service.py ~L629); Litestar resolves nested deps. Add the param to __init__ + provide_completions_service.

publish_message: api.tournament.* not in IGNORE_IDEMPOTENCY → idempotency_key required (f"tournament:submission:{user_id}:{tc_id}").
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Auto-detect + PB/non-PB branch in submit_completion (D1/D2/D7/D4 insert site)</name>
  <files>apps/api/services/completions_service.py</files>
  <read_first>
    - apps/api/services/completions_service.py: submit_completion full body (~L413-501), __init__ (~L86), provide_completions_service (~L922).
    - apps/api/repository/completions_repository.py: insert_completion (~L1505-1559) — confirm it does NOT catch CheckViolationError.
    - apps/api/services/tournament_service.py: __init__/DI (provide_tournament_service ~L629).
    - apps/api/repository/tournaments_repository.py: create_tournament_completion (~L885), cross_write_to_core (~L930), get_active_cycle_by_map_id (from 11-01), lookup_map_id if present (else add a map_id lookup).
  </read_first>
  <behavior>
    - Normal completion on a NON-tournament map: behavior unchanged — slower-than-pending still raises SlowerThanPendingError (HTTP 400); a slower-than-PB insert still hits the 0017 trigger and surfaces an error (no silent acceptance). (D-07 regression guard.)
    - Normal NEW-PB completion on the ACTIVE cycle's map: core row inserted (existing path) AND a tournaments.completions row inserted with map_id = cycle map AND core.completions.tournament_completion_id set to that tournament row — all in ONE transaction (Pitfall P6). No participation XP yet (granted on verify, D-06).
    - Slower-than-PB completion on the ACTIVE cycle's map: NO core row (0017 trigger rejects → caught as CheckViolationError); a tournaments.completions row IS inserted; the run is routed to tournament verification (OCR variant if no video, mod queue if video — Task wiring); the request succeeds ("recorded, pending verification"), NOT HTTP 400 (D-07).
    - Slower-than-PB on a NON-tournament map: still raises (CheckViolationError propagates / surfaces as today) — the relaxation must NOT leak to non-tournament maps.
  </behavior>
  <action>
    DI: add a TournamentService param to CompletionsService.__init__ (TYPE_CHECKING-guarded import; store as self._tournament_service) and add it to provide_completions_service (resolve via the existing provide_tournament_service). [If a circular import still appears, inject TournamentRepository + TournamentRewardService directly — A1 fallback — but prefer the service.]

    In submit_completion, resolve map_id for the submitted code once (add/extend a `lookup_map_id(code)` repo call or read it from the target_map CTE) BEFORE the insert branch, then `active_cycle = await tournament_repo.get_active_cycle_by_map_id(map_id, conn=conn)` (D-01). Restructure the insert as:

    - PENDING precheck (existing SlowerThanPendingError at ~L439-440): KEEP for non-tournament maps. For active-tournament maps, a slower-than-pending case must NOT 400 — instead fall through to the non-PB tournament path. Gate the SlowerThanPendingError raise on `active_cycle is None` (D-07: non-tournament maps unchanged; tournament maps routed to verification).
    - Try `completion_id = await insert_completion(...)` (PB path). On success with active_cycle: insert the tournament row via create_tournament_completion(cycle_id=active_cycle["id"], user_id, map_id, time, screenshot, video, conn=conn) and set core.completions.tournament_completion_id = tournament_row["id"] (extend insert_completion to accept optional tournament_completion_id, OR issue `UPDATE core.completions SET tournament_completion_id=$1 WHERE id=$2` in the same conn) — Pitfall P6, one transaction. (D-04a link.)
    - `except CheckViolationError` (import from asyncpg.exceptions — P7): if active_cycle is not None → non-PB path: completion_id=None, insert tournament row (create_tournament_completion), then mark the run for tournament verification (Task 2 handles OCR/mod dispatch). If active_cycle is None → re-raise (non-tournament map keeps existing failure behavior, D-07). Let UniqueViolationError/ForeignKeyViolationError (already translated by insert_completion) propagate untouched.

    Keep the existing post-insert flow (suspicious flags, OCR emit vs CompletionCreatedEvent publish) intact for the PB/core path. Do NOT grant participation XP here (D-06).
  </action>
  <verify>
    <automated>cd /Users/nebula/Documents/coding/parkour/genji/genjishimada && uv run --directory apps/api pytest tests/services/test_completions_service.py -v -p no:xdist -k "tournament or pb or slower or pending"</automated>
  </verify>
  <acceptance_criteria>
    - Unit tests prove: PB on active map → core row + tournament row + FK link in one txn; non-PB on active map → no core row + tournament row + no 400; non-tournament slower-than-pending → SlowerThanPendingError still raised; non-tournament slower-than-PB → still fails (CheckViolationError not swallowed).
    - CheckViolationError is imported from asyncpg.exceptions and the catch branches only on active_cycle.
    - DI wiring resolves (no circular import at app import time).
    - `just lint-api` passes.
  </acceptance_criteria>
  <done>submit_completion auto-detects active-cycle runs, branches PB vs non-PB correctly, preserves non-tournament behavior, and links core↔tournament for PB runs in one transaction.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PB verify side-effect + non-PB OCR variant + tournament OCR listener (D4a/D4b/D6)</name>
  <files>apps/api/services/completions_service.py, apps/api/events/completions.py</files>
  <read_first>
    - apps/api/services/completions_service.py: verify_completion (~L575-652), attempt_auto_verify_async (~L281-412) — copy OCR HTTP block verbatim.
    - apps/api/events/completions.py: handle_ocr_verification listener (~L17-40) — the exact @listener pattern.
    - From 11-01: TournamentOcrVerificationRequestedEvent (events/schemas.py).
    - From 11-02: tournament_service.verify_tournament_completion (terminal for OCR match); api.tournament.completion.created queue + TournamentCompletionCreatedEvent (mod review terminal).
    - apps/api/repository/tournaments_repository.py: get_active_cycle_by_map_id, set_tournament_verified, fetch_user_completion (read-time best, D3 reference).
  </read_first>
  <behavior>
    - PB verify (D4a): verifying a core completion that is linked (core.completions.tournament_completion_id set) and whose map is the active cycle → the linked tournament row becomes verified=TRUE AND participation XP is granted once — inside verify_completion, same transaction (the VerificationChangedEvent has no map_id, so cycle is resolved from the code here, P8). No second embed/event for the bot beyond the normal completion verification. Verifying twice grants XP once.
    - Non-PB OCR (D4b, no video): attempt_tournament_auto_verify_async runs the SAME /extract check; on a code/time/name match → calls tournament_service.verify_tournament_completion(tc_id) (sets verified + XP + publishes verification event); on mismatch → publishes TournamentCompletionCreatedEvent on api.tournament.completion.created (mod review). The terminal differs from the core OCR variant (P4) — no core row exists for non-PB.
    - Non-PB video: routed (from Task 1's non-PB branch) to publish TournamentCompletionCreatedEvent on api.tournament.completion.created for mod Accept/Reject (no auto-verify).
    - A single screenshot is never reviewed twice (PB runs use the core verdict; non-PB runs use exactly one tournament verification surface).
  </behavior>
  <action>
    In verify_completion, after update_verification and BEFORE/with the existing VerificationChangedEvent publish: using the `code` from fetch_completion_for_moderation, resolve map_id and `active_cycle = get_active_cycle_by_map_id(map_id, conn=conn)`; fetch the core completion's tournament_completion_id; if event.verified is True AND active_cycle is not None AND tournament_completion_id is not None → `set_tournament_verified(tournament_completion_id, True, conn=conn)` + `await reward_service.award_participation(active_cycle, user_id, conn)` (D-06; idempotent — never branch on already-granted, P5) + publish TournamentVerificationChangedEvent on api.tournament.verification.changed (idempotency_key f"tournament:verify:{tournament_completion_id}"); flush deferred XP post-commit. Do NOT consume VerificationChangedEvent on the bot to do this — it carries no map_id (P8). Keep the existing core VerificationChangedEvent publish unchanged (the bot's normal completion verification UI still fires).

    Add `async def attempt_tournament_auto_verify_async(self, tournament_completion_id, cycle_id, user_id, code, time, screenshot, users, notifications)` mirroring attempt_auto_verify_async: copy the OCR HTTP /extract block verbatim (hostname switch, POST, msgspec OcrResponse decode, 3-way match). ONLY the terminal differs: on match → `await self._tournament_service.verify_tournament_completion(tournament_completion_id)`; on mismatch → publish TournamentCompletionCreatedEvent(completion_id=tournament_completion_id... use the extended fields user_id/time/video=None/screenshot, cycle_id) on api.tournament.completion.created (idempotency_key f"tournament:submission:{user_id}:{tournament_completion_id}") for mod review.

    In Task 1's non-PB branch, dispatch: if the run has NO video → `request.app.emit("tournament.ocr.requested", TournamentOcrVerificationRequestedEvent(tournament_completion_id, cycle_id, user_id, code, time, screenshot), svc=self, users=..., notifications=...)`; if it HAS video → publish TournamentCompletionCreatedEvent on api.tournament.completion.created directly (mod review).

    In events/completions.py add `@listener("tournament.ocr.requested") async def handle_tournament_ocr_verification(event, svc, users, notifications)` mirroring handle_ocr_verification, calling `svc.attempt_tournament_auto_verify_async(...)`. Auto-discovered by events/__init__.py — no manual registration.
  </action>
  <verify>
    <automated>cd /Users/nebula/Documents/coding/parkour/genji/genjishimada && uv run --directory apps/api pytest tests/services/test_completions_service.py -v -p no:xdist -k "verify or ocr or tournament"