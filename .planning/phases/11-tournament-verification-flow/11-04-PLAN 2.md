---
phase: 11-tournament-verification-flow
plan: 04
type: execute
wave: 3
depends_on: ["11-01", "11-02"]
files_modified:
  - apps/bot/extensions/api_service.py
  - apps/bot/extensions/tournaments.py
  - apps/api/tests/bot/test_tournaments_handler.py
autonomous: true
requirements: [SUB-01]
must_haves:
  truths:
    - "D-04b: the bot consumes api.tournament.completion.created and posts an Accept/Reject view to the verification channel for non-PB video runs"
    - "D-04b: Accept calls verify_tournament_completion(tc_id), Reject calls reject_tournament_completion(tc_id) — the bot NEVER writes the DB, only calls the API"
    - "the tournament Accept/Reject buttons use distinct custom_ids (tournament:accept / tournament:reject) that do not collide with the completions buttons"
  artifacts:
    - path: "apps/bot/extensions/api_service.py"
      provides: "verify_tournament_completion + reject_tournament_completion bot client wrappers"
      contains: "def verify_tournament_completion"
    - path: "apps/bot/extensions/tournaments.py"
      provides: "api.tournament.completion.created consumer + Accept/Reject ui.View"
      contains: "api.tournament.completion.created"
  key_links:
    - from: "apps/bot/extensions/tournaments.py Accept button"
      to: "API PATCH /tournaments/completions/{id}/verify"
      via: "self.view.bot.api.verify_tournament_completion"
      pattern: "verify_tournament_completion"
    - from: "apps/bot/extensions/tournaments.py consumer"
      to: "verification channel"
      via: "@queue_consumer(api.tournament.completion.created) → send(view)"
      pattern: "tournament.completion.created"
---

<objective>
Build the Discord side of the non-PB tournament verification surface: a queue consumer that posts an Accept/Reject embed for video runs needing mod review, the two bot API client wrappers it calls, and handler tests. Mirrors the completions Accept/Reject pattern 1:1 with distinct custom_ids.

Purpose: Let mods verify slower-than-PB video tournament runs from Discord (bot calls the API; never writes the DB).
Output: Two api_service wrappers, one consumer + Accept/Reject view in TournamentHandler, bot tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-tournament-verification-flow/11-CONTEXT.md
@.planning/phases/11-tournament-verification-flow/11-RESEARCH.md
@.planning/phases/11-tournament-verification-flow/11-PATTERNS.md
@.planning/phases/11-tournament-verification-flow/11-01-SUMMARY.md
@.planning/phases/11-tournament-verification-flow/11-02-SUMMARY.md

<interfaces>
<!-- Templates from completions bot extension + 11-01/11-02 contracts. Re-grep line numbers at edit time. -->

From 11-01: SDK TournamentCompletionCreatedEvent(completion_id, cycle_id, user_id, time, video, screenshot) — extended/slim per 11-01 SUMMARY.
From 11-02: API endpoints PATCH /tournaments/completions/{id}/verify and /reject (scope tournaments:verify); queue api.tournament.completion.created.

Completions bot templates (apps/bot/extensions/completions.py — re-grep lines):
  - _process_create_submission_message (~L562-571): @queue_consumer("api.completion.submission", struct_type=CompletionCreatedEvent, idempotent=True) → fetch submission, build CompletionVerificationView, send to self.verification_channel, store verification_id.
  - CompletionVerificationView(ui.LayoutView) (~L299-355): ui.Container/ui.Section/ui.MediaGallery/ui.ActionRow.
  - CompletionsVerificationAcceptButton (~L103-153): ButtonStyle.green, custom_id="completions:accept"; callback defers, disables buttons, edits message, calls self.view.bot.api.verify_completion(...), poll_job_until_complete.
  - CompletionsVerificationRejectButton (~L155-187): red, custom_id="completions:reject"; opens RejectionReasonModal (~L84), then verify_completion(verified=False).

TournamentHandler (apps/bot/extensions/tournaments.py — exists from Phase 9):
  - TournamentHandler(BaseHandler) (~L68), PUBLIC bot.tournaments (RabbitHandler skips _-prefixed attrs).
  - _resolve_channels (~L73) caches channels — confirm/add a tournament verification channel ref (default: reuse the completions verification channel per CONTEXT discretion).
  - Existing consumers: @queue_consumer("api.tournament.cycle_started", struct_type=..., idempotent=True) (~L79), cycle_completed (~L111).

Bot client (apps/bot/extensions/api_service.py): verify_completion (~L1080) → PATCH /completions/{id}/verify; sync def returning self._request job-status shape; tournament wrappers from Phase 10 exist (~L1095+) as the style template.

Bot test (apps/api/tests/bot/test_tournaments_handler.py): local stub _queue_consumer(queue_name, *, struct_type, idempotent=False, **_) returns the raw handler (attaching _struct_type/_idempotent); _make_handler does object.__new__(TournamentHandler) + SimpleNamespace(api=mock_api) + fake channel. conftest.py puts apps/bot on sys.path, removes in finally (commit 07e8b0d).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Bot API client wrappers</name>
  <files>apps/bot/extensions/api_service.py</files>
  <read_first>
    - apps/bot/extensions/api_service.py: verify_completion (~L1080) and the Phase 10 tournament wrappers (~L1095+) for exact request/return style.
  </read_first>
  <action>
    Add `def verify_tournament_completion(self, tc_id: int)` → `self._request(... PATCH /tournaments/completions/{tc_id}/verify ...)` and `def reject_tournament_completion(self, tc_id: int)` → `PATCH /tournaments/completions/{tc_id}/reject`, mirroring verify_completion exactly (same auth header, same job-status return shape, same sync-def style as the other tournament wrappers). No DB access — these are HTTP calls (bot never writes DB).
  </action>
  <verify>
    <automated>cd /Users/nebula/Documents/coding/parkour/genji/genjishimada && grep -n "def verify_tournament_completion\|def reject_tournament_completion" apps/bot/extensions/api_service.py && uv run --directory apps/api pytest tests/bot/test_tournament_commands.py -p no:xdist -q || true