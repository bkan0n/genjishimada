---
phase: 11-tournament-verification-flow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - libs/sdk/src/genjishimada_sdk/tournaments.py
  - apps/api/events/schemas.py
  - infra/rabbitmq/definitions.json
  - apps/api/repository/tournaments_repository.py
  - apps/api/repository/completions_repository.py
  - apps/api/tests/services/test_tournament_verification.py
autonomous: true
requirements: [SUB-01]
user_setup: []

must_haves:
  truths:
    - "D-01: an active-cycle lookup keyed on map_id exists so a normal completion's map can be matched to a running tournament cycle without a separate submit step"
    - "D-04a: a repo method can flip a single tournaments.completions row to verified=TRUE, used by BOTH the PB side-effect and the non-PB verify endpoint"
    - "New tournament completion + verification RabbitMQ queues (with DLQs) exist so downstream API publishes and bot consumers have a transport"
    - "SDK carries the event structs the new non-PB verification surface serializes"
  artifacts:
    - path: "apps/api/repository/tournaments_repository.py"
      provides: "get_active_cycle_by_map_id, set_tournament_verified"
      contains: "async def get_active_cycle_by_map_id"
    - path: "libs/sdk/src/genjishimada_sdk/tournaments.py"
      provides: "TournamentVerificationChangedEvent + extended TournamentCompletionCreatedEvent"
      contains: "class TournamentVerificationChangedEvent"
    - path: "infra/rabbitmq/definitions.json"
      provides: "api.tournament.completion.created + api.tournament.verification.changed queues + DLQs"
      contains: "api.tournament.verification.changed"
    - path: "apps/api/tests/services/test_tournament_verification.py"
      provides: "Wave-0 failing test scaffold for SC-2/SC-3"
      contains: "def test_"
  key_links:
    - from: "apps/api/repository/tournaments_repository.py::get_active_cycle_by_map_id"
      to: "tournaments.cycles WHERE map_id=$1 AND status='active'"
      via: "asyncpg fetchrow with positional params"
      pattern: "get_active_cycle_by_map_id"
---

<objective>
Establish the contracts the rest of Phase 11 builds against: the two new tournament repo lookups (`get_active_cycle_by_map_id` for D-01 auto-detect, `set_tournament_verified` for D-04a/D-06 verify propagation), a map-id-by-code resolver the submit hook needs, the new SDK event structs, the new RabbitMQ queues + DLQs, and the Wave-0 failing test scaffold for SC-2/SC-3. Interface-first: nothing here changes behavior, so it lands before the hot-path edits in 11-02/11-03.

Purpose: Downstream plans consume these contracts directly without re-deriving them. PRESERVE `cross_write_to_core` (L930) and `create_tournament_completion` (L885) — reused by the PB path (D-05/P9).
Output: Repo methods, SDK structs, queue definitions, test scaffold.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-tournament-verification-flow/11-RESEARCH.md
@.planning/phases/11-tournament-verification-flow/11-PATTERNS.md

<interfaces>
<!-- Verified this session. Re-grep line numbers at edit time (P1: they drift). -->

tournaments_repository.py existing analogs:
- check_active_cycle_for_category (L257) — keys on category_id, returns int | None (cycle_id). Do NOT key auto-detect on this.
- create_tournament_completion (L885) — plain INSERT ... RETURNING * (no ON CONFLICT). PRESERVE.
- cross_write_to_core (L930) — PB-gated CTE. PRESERVE.
- fetch_user_completion (L1055) — best-per-user read (ORDER BY verified DESC, time ASC LIMIT 1).
- claim_xp_grant (L837) — ON CONFLICT (cycle_id,user_id,reason) DO NOTHING.

tournaments.completions schema (0020:86-99): id, cycle_id, user_id, map_id, time numeric(10,2), screenshot text NOT NULL, video text NULL, verified bool DEFAULT FALSE, completion bool DEFAULT FALSE, inserted_at timestamptz, UNIQUE(cycle_id,user_id,inserted_at). NO verified_by/verified_at columns. P2: UNIQUE is (cycle_id,user_id,inserted_at) NOT (cycle_id,user_id) — multiple rows per user allowed, best is read-time. Do NOT add an ON CONFLICT (cycle_id,user_id) upsert.

tournaments.cycles schema (0020:59-69): id, category_id, map_id, status('pending'|'active'|'finalizing'|'completed'), started_at, ended_at, created_at. idx_cycles_map_id exists (0020:72).

SDK existing (genjishimada_sdk/tournaments.py): __all__ sorted tuple L8; TournamentCompletionCreatedEvent (L441) carries only completion_id, cycle_id; TournamentCompletionResponse (L347), TournamentCompletionCreateRequest (L331) — KEEP.
SDK analog (genjishimada_sdk/completions.py): CompletionCreatedEvent (L325), VerificationChangedEvent (L335) — plain msgspec.Struct event templates.
events/schemas.py analog: OcrVerificationRequestedEvent.
RabbitMQ definitions.json: api.tournament.cycle_started + .dlq is the copy template. Tournament XP stays on api.xp.grant.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add tournament repo lookups (D-01 auto-detect, D-04a verify-flip, map-id resolver)</name>
  <files>apps/api/repository/tournaments_repository.py, apps/api/repository/completions_repository.py</files>
  <read_first>
    - apps/api/repository/tournaments_repository.py (check_active_cycle_for_category L257, create_tournament_completion L885, cross_write_to_core L930, fetch_user_completion L1055, BaseRepository._get_connection usage)
    - apps/api/repository/completions_repository.py (find an existing code→map_id resolver, e.g. the target_map CTE inside insert_completion ~L1512, or a lookup_map_id helper; reuse if present)
    - apps/api/migrations/0020_tournaments.sql (lines 59-99 — cycles + completions schema; confirm idx_cycles_map_id)
    - .planning/phases/11-tournament-verification-flow/11-PATTERNS.md (§tournaments_repository.py — exact query bodies)
  </read_first>
  <action>
    Add THREE lookups, mirroring the file's `_conn = self._get_connection(conn)` + positional-param (`$1`) convention; never f-string SQL.
    1. To TournamentRepository: `async def get_active_cycle_by_map_id(self, map_id: int, *, conn: Connection | None = None) -> dict | None` — `SELECT id, category_id, map_id, status FROM tournaments.cycles WHERE map_id = $1 AND status = 'active' LIMIT 1`; return `dict(row) if row else None` (D-01/D-02; backed by idx_cycles_map_id).
    2. To TournamentRepository: `async def set_tournament_verified(self, tournament_completion_id: int, verified: bool = True, *, conn: Connection | None = None) -> dict | None` — `UPDATE tournaments.completions SET verified = $2 WHERE id = $1 RETURNING id, cycle_id, user_id, time`; return `dict(row) if row else None` (D-04a/D-06; idempotent — setting TRUE twice is harmless, P5).
    3. Code→map_id resolver the submit hook needs: if a resolver already exists in completions_repository, do NOT duplicate — name it in SUMMARY. Only if none exists, add `async def lookup_map_id(self, code: str, *, conn: Connection | None = None) -> int | None` to CompletionsRepository (`SELECT id FROM core.maps WHERE code = $1`). 11-02 consumes it to feed get_active_cycle_by_map_id.
    Google docstrings (Args/Returns/Raises). Full type annotations.
  </action>
  <verify>
    <automated>cd apps/api && grep -c "async def get_active_cycle_by_map_id" repository/tournaments_repository.py && grep -c "async def set_tournament_verified" repository/tournaments_repository.py && grep -c "async def cross_write_to_core\|async def create_tournament_completion" repository/tournaments_repository.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "async def get_active_cycle_by_map_id" apps/api/repository/tournaments_repository.py` == 1.
    - `grep -c "async def set_tournament_verified" apps/api/repository/tournaments_repository.py` == 1.
    - No NEW f-string SQL added: `grep -nE "f[\"']SELECT|f[\"']UPDATE" apps/api/repository/tournaments_repository.py` shows no new occurrences on the added methods.
    - get_active_cycle_by_map_id filters `status = 'active'`; set_tournament_verified RETURNINGs id, cycle_id, user_id, time.
    - cross_write_to_core + create_tournament_completion UNCHANGED (grep count == 2).
    - A code→map_id resolver is named in the SUMMARY (reused or new).
  </acceptance_criteria>
  <done>Three lookups available; PB-path helpers preserved; lint clean.</done>
</task>

<task type="auto">
  <name>Task 2: Add SDK event structs + extend completion-created event; update __all__</name>
  <files>libs/sdk/src/genjishimada_sdk/tournaments.py</files>
  <read_first>
    - libs/sdk/src/genjishimada_sdk/tournaments.py (__all__ L8, TournamentCompletionCreatedEvent L441, the `# Event types (RabbitMQ)` section ~L399)
    - libs/sdk/src/genjishimada_sdk/completions.py (CompletionCreatedEvent L325, VerificationChangedEvent L335, __all__ L10 — struct + __all__ conventions to mirror)
    - .planning/phases/11-tournament-verification-flow/11-PATTERNS.md (§libs/sdk — RESEARCH recommends EXTEND for fewer round-trips)
  </read_first>
  <action>
    In `libs/sdk/src/genjishimada_sdk/tournaments.py`:
    1. EXTEND `TournamentCompletionCreatedEvent` (L441) with `user_id: int`, `time: float`, `video: str | None`, `screenshot: str` (keep `completion_id`, `cycle_id`). Rides `api.tournament.completion.created` (non-PB video path) so the bot embed needs no extra fetch.
    2. ADD `class TournamentVerificationChangedEvent(Struct)` with `tournament_completion_id: int`, `cycle_id: int`, `user_id: int`, `verified: bool`, `time: float`. No `verified_by` (table has no such column; 0023 migration out of scope). Rides `api.tournament.verification.changed`.
    3. Add `"TournamentVerificationChangedEvent"` to the sorted `__all__` tuple, preserving alphabetical order.
    Plain `msgspec.Struct` like the completions analogs. Do NOT touch TournamentCompletionCreateRequest / TournamentCompletionResponse (removal is 11-04).
    After editing the SDK (workspace package), run `just fix` so API+bot resolve the change (MEMORY: ModuleNotFoundError otherwise).
  </action>
  <verify>
    <automated>cd /Users/nebula/Documents/coding/parkour/genji/genjishimada && just fix >/dev/null 2>&1; uv run python -c "from genjishimada_sdk.tournaments import TournamentVerificationChangedEvent as V, TournamentCompletionCreatedEvent as C, __all__; assert 'TournamentVerificationChangedEvent' in __all__; V(tournament_completion_id=1,cycle_id=2,user_id=3,verified=True,time=4.5); C(completion_id=1,cycle_id=2,user_id=3,time=4.5,video=None,screenshot='x'); print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `TournamentVerificationChangedEvent` constructs with exactly `tournament_completion_id, cycle_id, user_id, verified, time`.
    - `TournamentCompletionCreatedEvent` constructs with `completion_id, cycle_id, user_id, time, video, screenshot`.
    - `"TournamentVerificationChangedEvent" in __all__` is True and __all__ remains sorted.
    - `TournamentCompletionCreateRequest` and `TournamentCompletionResponse` still importable (unchanged).
  </acceptance_criteria>
  <done>Both events importable from the SDK; just fix run; bot+api resolve the package.</done>
</task>

<task type="auto">
  <name>Task 3: Declare new tournament queues + DLQs; add OCR-variant event schema; scaffold failing verification tests</name>
  <files>infra/rabbitmq/definitions.json, apps/api/events/schemas.py, apps/api/tests/services/test_tournament_verification.py</files>
  <read_first>
    - infra/rabbitmq/definitions.json (api.tournament.cycle_started queue + its .dlq block — the exact copy template)
    - apps/api/events/schemas.py (OcrVerificationRequestedEvent — mirror for the tournament OCR-variant struct)
    - apps/api/tests/services/test_tournament_service.py (conventions: pytest-asyncio auto, X-PYTEST-ENABLED=1 publish-skip, fixtures)
    - apps/api/tests/integration/test_tournaments_integration.py (integration client + cycle/map fixtures)
    - .planning/phases/11-tournament-verification-flow/11-VALIDATION.md (SC-2/SC-3 validation rows the scaffold targets)
    - .planning/phases/11-tournament-verification-flow/11-PATTERNS.md (§infra/rabbitmq/definitions.json — DLQ pattern; §events; §tests)
  </read_first>
  <action>
    1. `infra/rabbitmq/definitions.json`: add TWO main queues + TWO DLQs, copying the `api.tournament.cycle_started` block verbatim (durable, auto_delete false, x-dead-letter-exchange "", x-queue-type classic) — names `api.tournament.completion.created` (+ `.dlq`) and `api.tournament.verification.changed` (+ `.dlq`). Each main queue's `x-dead-letter-routing-key` is its own `<name>.dlq`; each .dlq is a standalone classic queue (Phase 09-03 per-queue-DLQ isolation, commits 4c31fa4/33059e7). Do NOT add api.tournament.xp.grant.
    2. `apps/api/events/schemas.py`: add `TournamentOcrVerificationRequestedEvent` mirroring `OcrVerificationRequestedEvent` with `tournament_completion_id: int`, `cycle_id: int`, `user_id: int`, `code: str`, `time: float`, `screenshot: str`. This is the in-process schema 11-03 dispatches on `tournament.ocr.requested`.
    3. Create `apps/api/tests/services/test_tournament_verification.py` as a Wave-0 scaffold. Write tests with REAL assertions (not `pass`) for SC-3 (`test_pb_path_*`: PB run on cycle map verified once → BOTH core.completions.verified AND tournament row verified) and SC-2 (`test_non_pb_path_*`: slower-than-PB run on cycle map → no core row, tournament row created, verifiable via the new endpoint). Mark not-yet-implemented assertions `pytest.mark.xfail(reason="implemented in 11-02/11-03", strict=False)` so the suite is green NOW and flips to real coverage as the surface lands. Include a fixture docstring note: a pre-seeded faster core completion to trip the 0017 trigger, plus a tournament-completion factory keyed to a cycle map. Names must be `-k`-selectable per SC (`pb_path`, `non_pb_path`).
  </action>
  <verify>
    <automated>cd /Users/nebula/Documents/coding/parkour/genji/genjishimada && python -c "import json; d=json.load(open('infra/rabbitmq/definitions.json')); n={q['name'] for q in d['queues']}; assert {'api.tournament.completion.created','api.tournament.completion.created.dlq','api.tournament.verification.changed','api.tournament.verification.changed.dlq'} <= n; print('queues ok')" && uv run --directory apps/api pytest tests/services/test_tournament_verification.py -p no:xdist -q 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `definitions.json` parses as JSON and contains all four new queue names; each main queue has its own `<name>.dlq` routing key.
    - `TournamentOcrVerificationRequestedEvent` importable from `events.schemas` with `tournament_completion_id`.
    - `test_tournament_verification.py` collects and the suite exits green (xfail-marked stubs do not fail the run); names contain `pb_path` and `non_pb_path`.
    - No `is True/False` assertion against `check_active_cycle_for_category` (returns int|None — MEMORY).
  </acceptance_criteria>
  <done>Queues declared; OCR-variant schema added; SC-2/SC-3 scaffold collects green and is -k-selectable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| broker config → runtime | definitions.json declares queues loaded by RabbitMQ at boot; malformed JSON breaks broker load |
| SDK struct → wire | event structs are the serialization contract between API publisher and bot consumer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Denial of Service | new RabbitMQ consumers (no DLQ) | mitigate | each new queue gets its own `<name>.dlq` per Phase 09-03 isolated-sweep pattern (commit 33059e7) so a poison message cannot cascade |
| T-11-02 | Tampering | malformed definitions.json | mitigate | Task 3 verify parses the file as JSON and asserts all four queue names present before merge |
| T-11-03 | Tampering | SQL in new repo lookups | mitigate | asyncpg `$1`/`$2` positional params only; acceptance criterion greps for absence of new f-string SQL |
| T-11-SC | Tampering | npm/pip/cargo installs | mitigate | no package installs in this plan (RESEARCH: zero external packages added); no install task present |
</threat_model>

<verification>
- `python -c "import json; json.load(open('infra/rabbitmq/definitions.json'))"` succeeds and all four queues present.
- `uv run python -c "from genjishimada_sdk.tournaments import TournamentVerificationChangedEvent"` succeeds after `just fix`.
- `uv run --directory apps/api pytest tests/services/test_tournament_verification.py -p no:xdist -q` exits 0 (xfail stubs).
- `cd apps/api && uv run ruff check repository/tournaments_repository.py repository/completions_repository.py` clean.
</verification>

<success_criteria>
- get_active_cycle_by_map_id + set_tournament_verified exist; code→map_id resolver available; cross_write_to_core/create_tournament_completion preserved.
- SDK carries TournamentVerificationChangedEvent + extended TournamentCompletionCreatedEvent; __all__ updated; just fix run.
- Four new queues (2 main + 2 DLQ) declared; TournamentOcrVerificationRequestedEvent added.
- SC-2/SC-3 test scaffold collects green and is -k-selectable.
</success_criteria>

<output>
Create `.planning/phases/11-tournament-verification-flow/11-01-SUMMARY.md` when done. Record: which code→map_id resolver was reused/added, exact new SDK event field order, and the new queue names.
</output>
