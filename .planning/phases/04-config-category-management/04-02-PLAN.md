---
phase: 04-config-category-management
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - apps/api/routes/v3/tournaments.py
  - apps/api/tests/integration/test_tournaments_integration.py
autonomous: true
requirements: [CYCLE-02, CYCLE-03, CYCLE-08, ADM-01, ADM-02]
must_haves:
  truths:
    - "GET /api/v3/tournaments/config returns the config singleton with 200"
    - "PATCH /api/v3/tournaments/config updates provided fields and returns updated config"
    - "POST /api/v3/tournaments/categories creates a category and returns 201"
    - "GET /api/v3/tournaments/categories returns a list of all categories"
    - "GET /api/v3/tournaments/categories/{id} returns a single category or 404"
    - "PATCH /api/v3/tournaments/categories/{id} updates a category or returns 404/409"
    - "DELETE /api/v3/tournaments/categories/{id} deletes a category and returns 204"
    - "Category update/delete returns 409 when an active cycle exists"
    - "Duplicate category name returns 409"
    - "Non-admin requests are rejected by scope guard"
  artifacts:
    - path: "apps/api/routes/v3/tournaments.py"
      provides: "TournamentsController with 7 REST endpoints"
      contains: "class TournamentsController"
    - path: "apps/api/tests/integration/test_tournaments_integration.py"
      provides: "Integration tests for all tournament config and category endpoints"
      contains: "class TestGetConfig"
  key_links:
    - from: "apps/api/routes/v3/tournaments.py"
      to: "apps/api/services/tournament_service.py"
      via: "DI-injected tournament_service parameter"
      pattern: "tournament_service: TournamentService"
    - from: "apps/api/routes/v3/tournaments.py"
      to: "apps/api/routes/v3/__init__.py"
      via: "auto-discovery of Controller subclass"
      pattern: "class TournamentsController.*Controller"
---

<objective>
Create the TournamentsController with all 7 REST endpoints and integration tests validating the full HTTP stack.

Purpose: Expose tournament config and category management to admin clients via REST API, completing the Phase 4 goal. Integration tests verify the complete request -> controller -> service -> repository -> database flow.

Output: tournaments.py controller (new), test_tournaments_integration.py (new)
</objective>

<execution_context>
@/Users/nebula/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nebula/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-config-category-management/04-CONTEXT.md
@.planning/phases/04-config-category-management/04-RESEARCH.md
@.planning/phases/04-config-category-management/04-PATTERNS.md
@.planning/phases/04-config-category-management/04-01-SUMMARY.md

<interfaces>
<!-- Key types the executor needs from Plan 01 output and existing codebase -->

From apps/api/services/tournament_service.py (created in Plan 01):
- TournamentService(BaseService)
  - get_config() -> TournamentConfigResponse
  - update_config(data: TournamentConfigPatchRequest) -> TournamentConfigResponse
  - create_category(data: TournamentCategoryCreateRequest) -> TournamentCategoryResponse
  - list_categories() -> list[TournamentCategoryResponse]
  - get_category(category_id: int) -> TournamentCategoryResponse
  - update_category(category_id: int, data: TournamentCategoryPatchRequest) -> TournamentCategoryResponse
  - delete_category(category_id: int) -> None
- provide_tournament_service(state: State, tournament_repo: TournamentRepository) -> TournamentService

From apps/api/repository/tournaments_repository.py:
- provide_tournament_repository(state: State) -> TournamentRepository

From apps/api/services/exceptions/tournaments.py:
- CategoryNotFoundError(TournamentsError) — 404
- CategoryLockedError(TournamentsError) — 409
- CategoryNameExistsError(TournamentsError) — 409

From libs/sdk/src/genjishimada_sdk/tournaments.py:
- TournamentConfigResponse: blacklist_weeks (int), created_at (datetime), updated_at (datetime)
- TournamentConfigPatchRequest: blacklist_weeks (int | UnsetType)
- TournamentCategoryCreateRequest: name (str), difficulties (list[DifficultyTop]), cycle_frequency (CycleFrequency="weekly"), participation_xp (int=0), placement_xp (list[PlacementXpTier]=[]), streak_xp (list[StreakXpTier]=[]), champion_role_id (int|None=None)
- TournamentCategoryPatchRequest: all fields UnsetType defaults
- TournamentCategoryResponse: id, name, difficulties, cycle_frequency, participation_xp, placement_xp, streak_xp, champion_role_id, is_active, created_at, updated_at

From apps/api/utilities/errors.py:
- CustomHTTPException(status_code: int, detail: str)

From apps/api/routes/v3/store.py (reference pattern):
- Controller class with tags, path, dependencies dict
- Scope guard via opt={"required_scopes": {"scope:name"}}
- try/except domain exception -> CustomHTTPException mapping
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create TournamentsController with all 7 endpoints</name>
  <files>apps/api/routes/v3/tournaments.py</files>
  <read_first>
    - apps/api/routes/v3/store.py (complete reference: class structure, dependencies dict, scope guard, exception mapping, GET/PUT/POST patterns)
    - apps/api/routes/v3/maps.py (DELETE endpoint with 204 response pattern, PATCH endpoint, path parameter pattern)
    - apps/api/services/tournament_service.py (method signatures from Plan 01)
    - apps/api/services/exceptions/tournaments.py (exception classes to catch)
    - apps/api/utilities/errors.py (CustomHTTPException)
    - apps/api/routes/v3/__init__.py (auto-discovery mechanism — no registration needed)
    - .planning/phases/04-config-category-management/04-PATTERNS.md
  </read_first>
  <action>
Create `apps/api/routes/v3/tournaments.py` with `TournamentsController(Controller)`. Per D-02.

Module docstring: `"""Tournaments v3 controller."""`

Imports needed: `from __future__ import annotations`, `from typing import Annotated`, `litestar`, `Provide` from litestar.di, `Body` from litestar.params, `Parameter` from litestar.params, status codes (`HTTP_200_OK`, `HTTP_201_CREATED`, `HTTP_204_NO_CONTENT`, `HTTP_404_NOT_FOUND`, `HTTP_409_CONFLICT`), `Response` from litestar.response, SDK types (TournamentCategoryCreateRequest, TournamentCategoryPatchRequest, TournamentCategoryResponse, TournamentConfigPatchRequest, TournamentConfigResponse), `provide_tournament_repository` from repository.tournaments_repository, exception classes (CategoryLockedError, CategoryNameExistsError, CategoryNotFoundError) from services.exceptions.tournaments, `TournamentService` and `provide_tournament_service` from services.tournament_service, `CustomHTTPException` from utilities.errors.

Controller class attributes per D-02:
- `tags = ["Tournaments"]`
- `path = "/tournaments"`
- `dependencies = {"tournament_repo": Provide(provide_tournament_repository), "tournament_service": Provide(provide_tournament_service)}`

7 handler methods per D-03:

1. `get_config` — `@litestar.get(path="/config", summary="Get Tournament Config", description="Get global tournament configuration.", opt={"required_scopes": {"tournaments:read"}})`. Parameter: `tournament_service: TournamentService`. Returns `TournamentConfigResponse`. Delegates to `tournament_service.get_config()`. No exception handling needed (config always exists per D-09).

2. `update_config` — `@litestar.patch(path="/config", summary="Update Tournament Config", description="Update global tournament configuration fields.", status_code=HTTP_200_OK, opt={"required_scopes": {"tournaments:write"}})`. Parameters: `tournament_service: TournamentService`, `data: Annotated[TournamentConfigPatchRequest, Body(title="Config Update")]`. Returns `TournamentConfigResponse`. Delegates to `tournament_service.update_config(data)`.

3. `create_category` — `@litestar.post(path="/categories", summary="Create Tournament Category", description="Create a new tournament category with difficulty groupings.", status_code=HTTP_201_CREATED, opt={"required_scopes": {"tournaments:write"}})`. Parameters: `tournament_service: TournamentService`, `data: Annotated[TournamentCategoryCreateRequest, Body(title="Category")]`. Returns `TournamentCategoryResponse`. Wraps call in try/except catching `CategoryNameExistsError` -> `CustomHTTPException(status_code=HTTP_409_CONFLICT, detail=str(e))`.

4. `list_categories` — `@litestar.get(path="/categories", summary="List Tournament Categories", description="List all tournament categories.", opt={"required_scopes": {"tournaments:read"}})`. Parameter: `tournament_service: TournamentService`. Returns `list[TournamentCategoryResponse]`. Delegates to `tournament_service.list_categories()`.

5. `get_category` — `@litestar.get(path="/categories/{category_id:int}", summary="Get Tournament Category", description="Get a single tournament category by ID.", opt={"required_scopes": {"tournaments:read"}})`. Parameters: `tournament_service: TournamentService`, `category_id: Annotated[int, Parameter(description="Category ID")]`. Returns `TournamentCategoryResponse`. Wraps in try/except catching `CategoryNotFoundError` -> `CustomHTTPException(status_code=HTTP_404_NOT_FOUND, detail=str(e))`.

6. `update_category` — `@litestar.patch(path="/categories/{category_id:int}", summary="Update Tournament Category", description="Update a tournament category's fields.", status_code=HTTP_200_OK, opt={"required_scopes": {"tournaments:write"}})`. Parameters: `tournament_service: TournamentService`, `category_id: Annotated[int, Parameter(description="Category ID")]`, `data: Annotated[TournamentCategoryPatchRequest, Body(title="Category Update")]`. Returns `TournamentCategoryResponse`. Wraps in try/except catching `CategoryNotFoundError` -> 404, `CategoryLockedError` -> 409, `CategoryNameExistsError` -> 409. Per D-10.

7. `delete_category` — `@litestar.delete(path="/categories/{category_id:int}", summary="Delete Tournament Category", description="Delete a tournament category.", status_code=HTTP_204_NO_CONTENT, opt={"required_scopes": {"tournaments:write"}})`. Parameters: `tournament_service: TournamentService`, `category_id: Annotated[int, Parameter(description="Category ID")]`. Returns `Response[None]` with `Response(None, status_code=HTTP_204_NO_CONTENT)`. Wraps in try/except catching `CategoryNotFoundError` -> 404, `CategoryLockedError` -> 409.

All handler methods have Google-style docstrings with Args, Returns, and Raises sections following the store.py pattern. Always use `from e` when re-raising as CustomHTTPException.
  </action>
  <verify>
    <automated>cd /Users/nebula/Documents/coding/parkour/genji/genjishimada && uv run --directory apps/api ruff check apps/api/routes/v3/tournaments.py && uv run --directory apps/api ruff format --check apps/api/routes/v3/tournaments.py && uv run --directory apps/api basedpyright apps/api/routes/v3/tournaments.py</automated>
  </verify>
  <acceptance_criteria>
    - apps/api/routes/v3/tournaments.py exists
    - Contains `class TournamentsController(litestar.Controller):` (or `Controller` imported from litestar)
    - Controller has `tags = ["Tournaments"]`, `path = "/tournaments"`, `dependencies` dict with tournament_repo and tournament_service
    - GET /config endpoint uses `opt={"required_scopes": {"tournaments:read"}}`
    - PATCH /config endpoint uses `opt={"required_scopes": {"tournaments:write"}}`
    - POST /categories endpoint returns HTTP_201_CREATED
    - DELETE /categories/{category_id} returns HTTP_204_NO_CONTENT
    - CategoryNotFoundError caught and mapped to HTTP_404_NOT_FOUND
    - CategoryLockedError caught and mapped to HTTP_409_CONFLICT
    - CategoryNameExistsError caught and mapped to HTTP_409_CONFLICT
    - All exception re-raises use `from e`
    - ruff check passes with 0 errors
    - basedpyright passes with 0 errors
  </acceptance_criteria>
  <done>TournamentsController exists with all 7 endpoints, scope guards, and exception-to-HTTP mapping</done>
</task>

<task type="auto">
  <name>Task 2: Create integration tests for tournament config and category endpoints</name>
  <files>apps/api/tests/integration/test_tournaments_integration.py</files>
  <read_first>
    - apps/api/tests/integration/test_store_integration.py (reference for integration test patterns, fixture usage, assertion style)
    - apps/api/tests/integration/conftest.py (auth_headers, no_auth_headers, unauthenticated_client fixtures)
    - apps/api/tests/conftest.py (test_client fixture, asyncpg_pool, create_test_user, setup_test_db)
    - apps/api/routes/v3/tournaments.py (endpoint paths and expected responses from Task 1)
    - apps/api/migrations/0020_tournaments.sql (seed data — config row seeded by migration)
    - libs/sdk/src/genjishimada_sdk/tournaments.py (response field names for assertions)
  </read_first>
  <action>
Create `apps/api/tests/integration/test_tournaments_integration.py` with integration tests covering all 7 endpoints.

Module docstring: `"""Integration tests for Tournaments v3 controller.\n\nTests HTTP interface: request/response serialization,\nerror translation, and full stack flow through real database."""`

Use `pytestmark = [pytest.mark.integration, pytest.mark.domain_tournaments]`.

Test classes (each testing one endpoint):

**TestGetConfig** — `GET /api/v3/tournaments/config`
- `test_returns_config_singleton(self, test_client)` — Asserts 200, response JSON has `blacklist_weeks` (int), `created_at`, `updated_at`. Config exists because migration seeds it.

**TestUpdateConfig** — `PATCH /api/v3/tournaments/config`
- `test_update_blacklist_weeks(self, test_client)` — PATCH with `{"blacklist_weeks": 5}`, asserts 200, response `blacklist_weeks` == 5.
- `test_empty_patch_returns_unchanged(self, test_client)` — PATCH with `{}`, asserts 200, config still returned.

**TestCreateCategory** — `POST /api/v3/tournaments/categories`
- `test_create_minimal_category(self, test_client)` — POST with `{"name": "Test Category <uuid>", "difficulties": ["Easy"]}`, asserts 201, response has `id`, `name`, `difficulties`, `cycle_frequency` == "weekly" (default), `is_active` == True (default).
- `test_create_full_category(self, test_client)` — POST with all fields including `placement_xp: [{"place": 1, "xp": 100}]`, `streak_xp: [{"threshold": 3, "xp": 50}]`, `champion_role_id: 123456`, `participation_xp: 25`, `cycle_frequency: "biweekly"`. Asserts 201 and all fields present.
- `test_duplicate_name_returns_409(self, test_client)` — Create category, then POST same name again. Asserts 409.

**TestListCategories** — `GET /api/v3/tournaments/categories`
- `test_list_returns_array(self, test_client)` — Asserts 200, response is a list.
- `test_list_includes_created_category(self, test_client)` — Create a category with unique name, then list. Assert the created name appears in the list.

**TestGetCategory** — `GET /api/v3/tournaments/categories/{id}`
- `test_get_existing_category(self, test_client)` — Create a category, then GET by its id. Asserts 200, response `id` matches.
- `test_get_nonexistent_returns_404(self, test_client)` — GET with id=999999. Asserts 404.

**TestUpdateCategory** — `PATCH /api/v3/tournaments/categories/{id}`
- `test_update_name(self, test_client)` — Create a category, PATCH with `{"name": "Updated <uuid>"}`. Asserts 200, response name matches.
- `test_update_nonexistent_returns_404(self, test_client)` — PATCH id=999999. Asserts 404.
- `test_update_duplicate_name_returns_409(self, test_client)` — Create two categories, try to rename second to first's name. Asserts 409.

**TestDeleteCategory** — `DELETE /api/v3/tournaments/categories/{id}`
- `test_delete_existing(self, test_client)` — Create a category, DELETE by id. Asserts 204. GET the same id returns 404.
- `test_delete_nonexistent_returns_404(self, test_client)` — DELETE id=999999. Asserts 404.

**TestCategoryLockedGuard** — Tests for CYCLE-08
- `test_update_locked_during_active_cycle(self, test_client, asyncpg_pool)` — Create a category via API (POST), then insert an active cycle row directly into `tournaments.cycles` via `asyncpg_pool` (INSERT INTO tournaments.cycles (category_id, map_id, status) VALUES ($1, $2, 'active') — use a real map_id from `create_test_map` fixture). Then PATCH the category. Asserts 409.
- `test_delete_locked_during_active_cycle(self, test_client, asyncpg_pool)` — Same setup as above but DELETE. Asserts 409.

**TestUnauthenticated** — Scope guard rejection
- `test_config_rejected_without_auth(self, unauthenticated_client)` — GET /api/v3/tournaments/config without API key. Asserts 401.

Use uuid4().hex[:8] in category names to avoid collisions in parallel test execution. Use the `test_client` fixture (which includes auth headers) for authenticated requests and `unauthenticated_client` for unauthenticated requests.

For the active cycle tests, the `asyncpg_pool` fixture and `create_test_map` fixture provide direct DB access and map creation. Insert the cycle row with a map_id from create_test_map.
  </action>
  <verify>
    <automated>cd /Users/nebula/Documents/coding/parkour/genji/genjishimada && uv run --directory apps/api pytest tests/integration/test_tournaments_integration.py -v -p no:xdist --timeout=60</automated>
  </verify>
  <acceptance_criteria>
    - apps/api/tests/integration/test_tournaments_integration.py exists
    - Contains pytestmark with pytest.mark.integration and pytest.mark.domain_tournaments
    - TestGetConfig tests config singleton retrieval
    - TestUpdateConfig tests PATCH with blacklist_weeks
    - TestCreateCategory tests minimal creation (201), full creation, and duplicate name (409)
    - TestListCategories tests list returns array with created categories
    - TestGetCategory tests existing (200) and nonexistent (404)
    - TestUpdateCategory tests update (200), nonexistent (404), duplicate name (409)
    - TestDeleteCategory tests delete (204) and nonexistent (404)
    - TestCategoryLockedGuard tests update (409) and delete (409) during active cycle
    - TestUnauthenticated tests scope guard rejection (401)
    - All tests pass with 0 failures
  </acceptance_criteria>
  <done>Integration test suite passes, covering all 7 endpoints, error cases, active cycle guard, and auth rejection</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP client -> TournamentsController | Untrusted input: request body, path params |
| Controller -> Service | Trusted boundary after scope guard validation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05 | Elevation of Privilege | TournamentsController | mitigate | All GET endpoints require `tournaments:read` scope, all mutations require `tournaments:write` scope via opt dict. Superusers bypass by design. |
| T-04-06 | Tampering | TournamentsController PATCH handlers | mitigate | Input validated by Litestar/msgspec auto-deserialization against SDK Struct types; invalid fields rejected before reaching service |
| T-04-07 | Spoofing | TournamentsController | mitigate | X-API-KEY validated by CustomAuthenticationMiddleware before controller is invoked |
| T-04-08 | Denial of Service | DELETE /categories | mitigate | Active cycle guard prevents deletion of categories with running cycles |
</threat_model>

<verification>
Run integration tests:
```bash
cd /Users/nebula/Documents/coding/parkour/genji/genjishimada && uv run --directory apps/api pytest tests/integration/test_tournaments_integration.py -v -p no:xdist
```

Run full lint on new files:
```bash
uv run --directory apps/api ruff check apps/api/routes/v3/tournaments.py && uv run --directory apps/api basedpyright apps/api/routes/v3/tournaments.py
```

Run full test suite to check no regressions:
```bash
just test-api
```
</verification>

<success_criteria>
- TournamentsController auto-discovered by Litestar with all 7 endpoints operational
- All endpoint paths match D-03: GET/PATCH /tournaments/config, POST/GET/GET/PATCH/DELETE /tournaments/categories
- Scope guards enforce tournaments:read and tournaments:write per D-01
- Exception-to-HTTP mapping matches D-10: 404 for not found, 409 for locked/duplicate
- Integration tests pass covering happy paths, error cases, and auth rejection
- Full test suite passes with no regressions
</success_criteria>

<output>
Create `.planning/phases/04-config-category-management/04-02-SUMMARY.md` when done
</output>
