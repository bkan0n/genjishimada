---
phase: 12.1-verification-aware-tournament-results
plan: 02
subsystem: api
tags: [msgspec, sdk, tournaments, rabbitmq, events, backward-compat]

# Dependency graph
requires:
  - phase: 12-overhaul-of-tournaments
    provides: "TournamentRolloverEvent (edition_rollover combined event), TournamentCycleCompletedEvent element type, EditionStatus Literal"
  - phase: 12.1-01
    provides: "Migration 0025 awaiting_results edition status + tri-state completions"
provides:
  - "TournamentEditionResultsEvent struct (edition_id, results) on api.tournament.results — the deferred results-only wire contract (D-09)"
  - "TournamentRolloverEvent.results_pending: bool = False additive flag (old-payload-convertible, Pitfall 2)"
  - "EditionStatus Literal extended with awaiting_results"
affects: [12.1-04, 12.1-05]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Additive msgspec field with a default value preserves backward compatibility for in-flight jsonb outbox payloads"
    - "New *Event struct mirrors the existing TournamentRolloverEvent docstring + element-type conventions"

key-files:
  created:
    - apps/api/tests/services/test_tournament_sdk_events.py
  modified:
    - libs/sdk/src/genjishimada_sdk/tournaments.py

key-decisions:
  - "results_pending defaults to False so old-shape edition_rollover payloads (no results_pending key) still msgspec.convert — hard backward-compat constraint (Pitfall 2)"
  - "TournamentEditionResultsEvent carries edition_id + results only (no results_pending) — it is the drained-results path, mirroring TournamentRolloverEvent's results shape"
  - "Combined no-pending case stays on TournamentRolloverEvent; the new event is used ONLY for the deferred drain-later path (Pattern 6)"

patterns-established:
  - "Interface-first SDK plan: define wire contracts before downstream poller/bot implement against them"
  - "Additive-with-default field for outbox payload forward/backward compatibility"

requirements-completed: [D-09]

# Metrics
duration: 3min
completed: 2026-06-01
---

# Phase 12.1 Plan 02: SDK results-only event + results_pending flag + EditionStatus extension Summary

**Adds `TournamentEditionResultsEvent` (api.tournament.results), an additive `results_pending: bool = False` flag on `TournamentRolloverEvent` that keeps old-shape outbox payloads convertible, and extends the `EditionStatus` Literal with `awaiting_results` — the wire contracts Plans 04/05 build against.**

## Performance

- **Duration:** 3 min
- **Started:** 2026-06-01T21:35:27Z
- **Completed:** 2026-06-01T21:38:24Z
- **Tasks:** 1 (TDD: RED + GREEN)
- **Files modified:** 2 (1 created, 1 modified)

## Accomplishments
- New `TournamentEditionResultsEvent` struct (`edition_id`, `results: list[TournamentCycleCompletedEvent]`) on `api.tournament.results`, exported in `__all__` (D-09).
- `TournamentRolloverEvent` gained `results_pending: bool = False` as its last field — an OLD-shape payload (no `results_pending` key) still `msgspec.convert`s cleanly (Pitfall 2 backward-compat constraint for in-flight outbox rows at deploy).
- `EditionStatus` Literal extended to `Literal["active", "awaiting_results", "completed"]`.
- 6 SDK wire-contract tests (round-trip + old-payload compat + `__all__` export + Literal membership) GREEN; `just lint-sdk` clean.

## Task Commits

TDD gate sequence (RED → GREEN; no REFACTOR needed — implementation is minimal and already convention-clean):

1. **RED: failing SDK wire-contract tests** - `ce2afc3` (test)
2. **GREEN: SDK struct + field + Literal** - `bca32ed` (feat)

**Plan metadata:** _(final docs commit below)_

## Files Created/Modified
- `apps/api/tests/services/test_tournament_sdk_events.py` (created) - 6 msgspec round-trip + old-payload-compat + `__all__`/Literal assertions for the new event, the `results_pending` flag, and `awaiting_results`.
- `libs/sdk/src/genjishimada_sdk/tournaments.py` (modified) - extended `EditionStatus`; added `results_pending: bool = False` to `TournamentRolloverEvent` (+ docstring); added `TournamentEditionResultsEvent`; added it to `__all__` alphabetically.

## Decisions Made
- `results_pending` is the LAST field with a `False` default (mandatory for old-payload `msgspec.convert` per Pitfall 2 / Runtime State Inventory).
- The new event mirrors `TournamentRolloverEvent`'s `results` shape and docstring style; it deliberately has NO `results_pending` field (it is the drained-results path itself).
- Ran `just fix` after the SDK edit so the editable workspace package exposes the new struct to the API test import (MEMORY: SDK import failures need reinstall).

## Deviations from Plan
None - plan executed exactly as written.

## Issues Encountered
- The RED test file was already present on disk (untracked) from a prior session but the SDK changes were not yet made. Confirmed RED first (`ImportError: cannot import name 'TournamentEditionResultsEvent'`), committed it as the RED gate, then implemented GREEN. No redo of completed work.

## TDD Gate Compliance
- RED gate commit present: `ce2afc3` (`test(12.1-02): ...`) — verified failing on import before implementation.
- GREEN gate commit present: `bca32ed` (`feat(12.1-02): ...`) after RED — all 6 tests pass.
- REFACTOR gate intentionally skipped (minimal additive change, no cleanup warranted).

## Full-Suite Gate (wave-merge)
`uv run --directory apps/api pytest -n 4 --no-testmon` → **15 failed / 1795 passed / 2 skipped / 2 xfailed**. All 15 failures are deferred-by-design and NOT regressions from this plan (which touches only the SDK file + a new passing test):
- **11 owned by 12.1-03**: `set_tournament_verified` still does `SET verified = $2` against the now-generated `verified` column (verify/reject/leaderboard/cross-write/participation) — `test_tournament_rewards.py`, `test_tournaments_integration.py` (verify/leaderboard), `test_tournaments_repository.py` (cross-write/leaderboard/fetch-completion).
- **3 owned by 12.1-04**: `test_edition_transitions.py` asserts the OLD cron-finalizes-to-completed behavior; the timing-only cron (12.1-01) correctly stops at `awaiting_results`.
- **1 pre-existing flake**: `test_filter_by_single_category` (documented `-n 4` cross-worker contamination; passes in isolation).

This matches the 15-failure baseline STATE.md recorded after 12.1-01 exactly — no new regressions.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Wire contracts are locked: Plan 04 (poller) can emit `TournamentEditionResultsEvent` on `api.tournament.results` (idempotency `tournament:results:{edition_id}`) and set `results_pending=True` on the start-only `TournamentRolloverEvent`; Plan 05 (bot) can register `_on_edition_results` against the new struct.
- No blockers introduced.

## Self-Check: PASSED
- FOUND: libs/sdk/src/genjishimada_sdk/tournaments.py (modified)
- FOUND: apps/api/tests/services/test_tournament_sdk_events.py
- FOUND commit: ce2afc3 (RED)
- FOUND commit: bca32ed (GREEN)

---
*Phase: 12.1-verification-aware-tournament-results*
*Completed: 2026-06-01*
