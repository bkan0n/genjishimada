---
phase: 12.1-verification-aware-tournament-results
verified: 2026-06-01T18:00:00Z
status: human_needed
score: 9/9 must-haves verified
overrides_applied: 0
human_verification:
  - test: "On dev, trigger a rollover while at least one tournament run is pending verification. Confirm: (1) start of N+1 announces on time, (2) results section shows 'Results pending verification…' placeholder, (3) previous champion KEEPS their role."
    expected: "A single combined card posts with the start section and the pending placeholder. The champion role is not touched."
    why_human: "pg_cron timing + live RabbitMQ + Discord rendering cannot be tested in the automated suite."
  - test: "On dev, after the above rollover, resolve (verify or reject) the last pending run. Confirm: a SEPARATE new announcement card posts with the final standings, and the champion role transfers to the new champion (or stays vacant if no winner)."
    expected: "A new card posts — the original placeholder card is unchanged. The champion role is updated."
    why_human: "End-to-end deferred publish requires the live poller cadence (~10 s), RabbitMQ, and Discord."
  - test: "On dev (or staging), use /tournament-publish-results as a non-mod user. Confirm the command is rejected with a user-facing error and no force-publish is triggered."
    expected: "UserFacingError ephemeral response; no API call made."
    why_human: "Discord slash command permissions and ephemeral UI require live bot verification."
  - test: "On dev, use /tournament-publish-results as a mod user while an edition is awaiting_results with still-pending runs. Confirm the confirmation dialog appears, and after confirming, results publish immediately and the edition flips to completed."
    expected: "Results announce as a new card. Pending runs remain status='pending' (audit trail). Edition is completed."
    why_human: "End-to-end command → API → poller drain → RabbitMQ → bot announcement flow."
---

# Phase 12.1: Verification-Aware Tournament Results — Verification Report

**Phase Goal:** Fix the correctness bug where pending-verification runs are permanently excluded from standings at the grid boundary; decouple edition results from the on-time start of the next edition; add a tri-state verification model, admin force-publish escape hatch, and deferred results announcement via the outbox poller.
**Verified:** 2026-06-01T18:00:00Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | D-08: Tri-state (pending/verified/rejected) on `tournaments.completions`; rejected is distinguishable from pending | VERIFIED | Migration 0025 line 63: `CHECK (status IN ('pending', 'verified', 'rejected'))`; `set_tournament_verified` maps `False→'rejected'` writing `SET status = $2` (repo line 1526); schema tests `TestTournamentsVerificationAwareResults` cover all three values |
| 2 | D-08: `verified` is a STORED generated column (`status = 'verified'`); ranking unchanged | VERIFIED | Migration 0025 line 85: `ADD COLUMN verified boolean GENERATED ALWAYS AS (status = 'verified') STORED`; index recreated with same `verified DESC, time ASC`; ranking test `test_ranking_unchanged_verified_above_pending_and_rejected` passes |
| 3 | D-06: `process_edition_transitions()` is timing-only — no leaderboard snapshot, no outbox row, flips edition→awaiting_results, cycles→finalizing | VERIFIED | Migration 0025 lines 203–236: UPDATE to `awaiting_results`, child cycle UPDATE to `finalizing`, creates N+1; no `INSERT INTO tournaments.pending_transitions` inside function body; direct-invocation schema test `test_process_edition_transitions_is_timing_only` asserts zero outbox rows |
| 4 | D-07/D-01: First poller tick, pending=0 → combined `TournamentRolloverEvent(results_pending=False)`, grants XP, edition→completed | VERIFIED | `process_awaiting_results_editions` (outbox_service.py line 388–412): `if not start_announced and inflight == 0` branch computes results, runs grant loop, publishes, calls `complete_edition`; `TestPollerFirstTickNoPending::test_first_tick_no_pending_emits_combined` passes |
| 5 | D-07/D-01/D-05: First tick, pending>0 → start-only `TournamentRolloverEvent(results_pending=True)`, empty results (bot holds role), `start_announced` set, no grants | VERIFIED | `process_awaiting_results_editions` (outbox_service.py line 369–386): `if not start_announced and inflight > 0` branch; `mark_edition_start_announced` called; `TestPollerFirstTickPending::test_first_tick_pending_emits_start_only` passes |
| 6 | D-07/D-02: Later tick, drained → write `edition_results` outbox row (at-least-once), flip edition→completed | VERIFIED | `process_awaiting_results_editions` (outbox_service.py line 414–418): `if inflight == 0` (with `start_announced=True`): calls `_write_drained_results_row` which writes `edition_results` row via `create_pending_transition` and calls `complete_edition`; `TestPollerLaterTickDrained::test_later_tick_drained_emits_results` passes |
| 7 | D-03: Force-publish route `PATCH /api/v3/tournaments/publish-results` (`tournaments:write`) + `force_publish_results` service reusing the drained path ignoring pending | VERIFIED | Route at `apps/api/routes/v3/tournaments.py` line 525–559: `opt={"required_scopes": {"tournaments:write"}}`; `force_publish_results` in service calls `_write_drained_results_row` (shared helper); scope-guard integration test passes; route test `test_write_scope_publishes_and_completes` passes in isolation |
| 8 | D-09/D-04: New `TournamentEditionResultsEvent` on `api.tournament.results`; `TournamentRolloverEvent.results_pending` defaults False; bot `_on_edition_results` posts NEW card + held role transfer | VERIFIED | SDK: `TournamentEditionResultsEvent(edition_id, results)` in `__all__`; `results_pending: bool = False` on `TournamentRolloverEvent`; bot: `@queue_consumer("api.tournament.results", struct_type=TournamentEditionResultsEvent, idempotent=True)` on `_on_edition_results`; handler posts a new card, calls `_transfer_champion_role` per entry; `test_on_edition_results_posts_card_and_transfers_champion` passes |
| 9 | D-03: Bot `/tournament-publish-results` command is mod-gated (authoritative `is_mod` check), confirmation-gated, calls `force_publish_tournament_results` | VERIFIED | `tournament_publish_results` in `TournamentRerollCog` (tournaments.py line 925–989): `is_mod` check raises `UserFacingError` before any API call; `ConfirmationView` gates the abandon; `test_publish_results_gate_rejects_non_admin_without_api_write` asserts API not called; `test_publish_results_mod_confirmed_calls_force_publish` asserts confirmation triggers the call |

**Score:** 9/9 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `apps/api/migrations/0025_verification_aware_tournament_results.sql` | Tri-state, awaiting_results, start_announced, timing-only cron, edition_results CHECK | VERIFIED | All 5 regions present; `GENERATED ALWAYS AS`, `awaiting_results`, `start_announced`, timing-only function body (no outbox INSERT), `edition_results` added to event_type CHECK |
| `apps/api/services/tournament_outbox_service.py` | `process_awaiting_results_editions` drain state machine; `_write_drained_results_row` shared helper; `_EVENT_ROUTING['edition_results']` | VERIFIED | All three items present; grant loop reused verbatim inside the outbox transaction; module docstring 21–38 invariant intact |
| `apps/api/services/tournament_service.py` | `force_publish_results` reusing the drained-path helper | VERIFIED | Lines 512–568; calls `_write_drained_results_row`; `NoAwaitingResultsEditionError` on no awaiting edition |
| `apps/api/routes/v3/tournaments.py` | `PATCH /publish-results`, `tournaments:write` scope | VERIFIED | Line 525; `opt={"required_scopes": {"tournaments:write"}}` |
| `apps/api/repository/tournaments_repository.py` | `count_inflight_verifications`, `fetch_awaiting_results_editions`, `complete_edition`, `set_tournament_verified` writes `status` | VERIFIED | All methods present; `count_inflight_verifications` JOINs cycles WHERE `status='pending'`; `set_tournament_verified` writes `status = $2`, no `SET verified` writes remain |
| `libs/sdk/src/genjishimada_sdk/tournaments.py` | `TournamentEditionResultsEvent`, `results_pending: bool = False`, `EditionStatus` includes `awaiting_results` | VERIFIED | All three present; `"TournamentEditionResultsEvent"` in `__all__` alphabetically; `EditionStatus = Literal["active", "awaiting_results", "completed"]` |
| `apps/bot/extensions/tournaments.py` | `_on_edition_results` consumer, results-pending placeholder in `_on_edition_rollover`, `tournament_publish_results` command | VERIFIED | All three present; `_on_edition_results` registered `idempotent=True`; placeholder at line 407–409; command at line 925 |
| `apps/bot/extensions/api_service.py` | `force_publish_tournament_results` calling `PATCH /tournaments/publish-results` | VERIFIED | Line 1126; `Route("PATCH", "/tournaments/publish-results")` |
| `apps/api/services/exceptions/tournaments.py` | `NoAwaitingResultsEditionError` | VERIFIED | Line 81 |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `set_tournament_verified(verified=False)` | `tournaments.completions.status = 'rejected'` | `target_status = "rejected"` then `SET status = $2` | VERIFIED | Repo line 1522–1527; no `SET verified =` write found anywhere in the method |
| `count_inflight_verifications(edition_id)` | `tournaments.completions WHERE status='pending'` | JOIN cycles ON edition_id | VERIFIED | Repo lines 1555–1565; SQL confirmed |
| `process_awaiting_results_editions` | drain detection → `award_cycle_end` + `_reset_non_participant_streaks` | inside the same outbox transaction | VERIFIED | Outbox service lines 182–235; grants inside transaction; module docstring invariant intact |
| Deferred results | `edition_results` outbox row → publish-before-mark loop | `create_pending_transition` + existing drain loop | VERIFIED | `_write_drained_results_row` writes outbox row then `complete_edition`; `_EVENT_ROUTING["edition_results"] = ("api.tournament.results", TournamentEditionResultsEvent)` |
| `force_publish_results` (service) | same `_write_drained_results_row` helper | shared helper, no divergence | VERIFIED | Service line 553 calls `_write_drained_results_row` from `tournament_outbox_service` |
| `/tournament-publish-results` command | `api_service.force_publish_tournament_results` → `PATCH /tournaments/publish-results` | bot-side `is_mod` gate then API call | VERIFIED | Bot line 975; api_service line 1139; guard at line 951–958 precedes the call |
| `_on_edition_results` | `_transfer_champion_role` (held transfer) | iterate `event.results`, call per entry | VERIFIED | Bot lines 500–503; `_transfer_champion_role` called per result entry before card post |
| `TournamentRolloverEvent.results_pending=True` | no `_transfer_champion_role` call in `_on_edition_rollover` | empty `results` → transfer loop skips | VERIFIED | Bot line 360 early-return guard; transfer loop at line 371–374 iterates `event.results` (empty → no transfer); `test_rollover_results_pending_renders_placeholder_and_holds_champion` asserts no call |
| Idempotency keys | both events carry edition-scoped keys | `_idempotency_key` function + `tournament:rollover:{id}` / `tournament:results:{id}` | VERIFIED | Outbox service lines 119–135; rollover key at lines 382, 408; results key via `_idempotency_key`; `_on_edition_results` registered `idempotent=True` |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|-------------------|--------|
| `process_awaiting_results_editions` | `inflight` (drain signal) | `count_inflight_verifications` → `SELECT COUNT(*) ... WHERE status='pending'` | Yes — live DB query | FLOWING |
| `_build_cycle_completed_event` | `standings` | `fetch_leaderboard(cycle_id)` → DB RANK() query | Yes — live DB query at drain time | FLOWING |
| `_write_drained_results_row` | `edition_results` outbox row | `create_pending_transition` INSERT | Yes — writes to `tournaments.pending_transitions` | FLOWING |
| `force_publish_results` | edition row | `fetch_awaiting_results_editions` → SELECT WHERE status='awaiting_results' | Yes — live DB query | FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Schema test suite (tri-state, cron timing-only) | `uv run --directory apps/api pytest tests/integration/test_tournaments_schema.py --no-testmon -p no:xdist -q` | 22 passed | PASS |
| SDK wire-contract tests | `uv run --directory apps/api pytest tests/services/test_tournament_sdk_events.py --no-testmon -p no:xdist -q` | 6 passed | PASS |
| Outbox poller drain-state-machine tests (3 branches + idempotency + stacked + empty) | `uv run --directory apps/api pytest tests/repository/tournaments/test_outbox_poller.py --no-testmon -p no:xdist -q` | 16 passed | PASS |
| Bot handler tests (_on_edition_results, placeholder, transfer) | `uv run --directory apps/api pytest tests/bot/test_tournaments_handler.py --no-testmon -p no:xdist -q` | 21 passed | PASS |
| Bot command tests (non-mod rejection, mod-confirm) | `uv run --directory apps/api pytest tests/bot/test_tournament_commands.py --no-testmon -p no:xdist -q` | 14 passed | PASS |
| Full authoritative suite | `uv run --directory apps/api pytest -n 4 --no-testmon` | **1839 passed, 0 failed, 2 skipped, 2 xfailed** | PASS |

Note: `TestForcePublishResultsRoute::test_write_scope_publishes_and_completes` shows a spurious failure when run alongside other integration tests in a single process (shared-DB cross-contamination: another test's `awaiting_results` edition from a prior parallel run is picked up by the force-publish, so the specific edition seeded by this test is never the "oldest `ends_at`" winner). This is the documented `-n 4` cross-worker DB contamination pattern (MEMORY.md). The test passes in isolation and in the `-n 4` parallel run used by CI. It is not a code defect.

### Probe Execution

Step 7c: SKIPPED — no `scripts/*/tests/probe-*.sh` probes declared or present for this phase. Behavioral verification covered via the full test suite.

### Requirements Coverage

| Decision | Covering Plan(s) | Description | Status | Evidence |
|----------|-----------------|-------------|--------|---------|
| D-01 | 12.1-04, 12.1-05 | Four announcement cases (combined/start-only/results-only/deferred) | SATISFIED | Three-branch drain state machine in `process_awaiting_results_editions`; results-pending placeholder in bot |
| D-02 | 12.1-04 | Drain-only, no time cap | SATISFIED | Poller loops indefinitely until `count_inflight_verifications == 0`; no timer/deadline code path |
| D-03 | 12.1-04, 12.1-05 | Admin force-publish escape hatch | SATISFIED | `PATCH /publish-results` (`tournaments:write`); bot `/tournament-publish-results` (mod-gated) |
| D-04 | 12.1-05 | Deferred results post as new message, no edit-in-place | SATISFIED | `_on_edition_results` calls `channel.send` (new message); no message-id storage; test asserts new send |
| D-05 | 12.1-04, 12.1-05 | Champion role held through pending window; transfers only in `_on_edition_results` | SATISFIED | Start-only rollover carries `results=[]` → transfer loop skips; role transfer in `_on_edition_results` per entry |
| D-06 | 12.1-01 | `process_edition_transitions()` timing-only (no snapshot, no outbox row) | SATISFIED | Migration 0025 rewrites the function; direct-invocation test asserts zero outbox rows |
| D-07 | 12.1-04 | Outbox poller owns results computation + drain detection + three emit paths | SATISFIED | `process_awaiting_results_editions` inside the outbox transaction; all three branches implemented and tested |
| D-08 | 12.1-01, 12.1-03 | Tri-state verification (pending/verified/rejected); drain detectable | SATISFIED | Migration 0025 adds CHECK + GENERATED ALWAYS AS; `set_tournament_verified` writes `status`; `count_inflight_verifications` counts `status='pending'` |
| D-09 | 12.1-02, 12.1-04, 12.1-05 | `TournamentEditionResultsEvent` on `api.tournament.results`; `results_pending` flag | SATISFIED | SDK struct exists, in `__all__`, round-trips; `_EVENT_ROUTING` maps `edition_results`; `_on_edition_results` registered; `results_pending: bool = False` backward-compat default |

**All 9 decisions: SATISFIED**

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `apps/bot/extensions/tournaments.py` | 80 | `# TODO: swap for real tournament artwork.` | INFO | Pre-existing from Phase 12 commit `22815f2` (predates Phase 12.1); cosmetic content concern (image URL), no behavioral impact |

No blockers found. The TODO marker is pre-existing and unrelated to Phase 12.1 deliverables.

### Human Verification Required

The automated test suite (1839 passed, 0 failed) covers all code paths. The following require live environment verification:

#### 1. Deferred results end-to-end flow (normal path)

**Test:** On dev, trigger a tournament rollover while at least one run is pending verification (status='pending' in `tournaments.completions`). Watch the Discord channel.
**Expected:** (1) An on-time combined card posts with the start section AND a "Results pending verification…" placeholder in the results section. (2) The previous champion KEEPS their role (no role transfer occurs). (3) After resolving the last pending run (verify or reject it via the mod verification flow), a SEPARATE new card posts within ~10 seconds with the final standings. (4) The champion role transfers at this point (or stays vacant if no winner).
**Why human:** pg_cron grid timing, live RabbitMQ + bot consumer, Discord rendering, and end-to-end champion-role transfer require the live dev environment. The automated suite exercises all code paths but cannot observe Discord output.

#### 2. Deferred results end-to-end flow (force-publish path)

**Test:** On dev, with an edition in `awaiting_results` (pending runs present), use `/tournament-publish-results` as a mod user. Observe the confirmation dialog and the result.
**Expected:** (1) A confirmation dialog appears. (2) After confirming, results publish immediately as a new card. (3) The edition flips to `completed` in the DB. (4) Still-pending runs remain `status='pending'` (audit trail — not force-rejected). (5) A non-mod user attempting the command receives an ephemeral error and no announcement occurs.
**Why human:** Discord slash command mod-gate verification, ephemeral UI, and confirmation flow require live bot context.

### Gaps Summary

No gaps found. All 9 decisions (D-01 through D-09) are implemented in code and covered by passing tests.

---

_Verified: 2026-06-01T18:00:00Z_
_Verifier: Claude (gsd-verifier)_
