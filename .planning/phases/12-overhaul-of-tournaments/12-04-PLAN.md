---
phase: 12-overhaul-of-tournaments
plan: 04
type: execute
wave: 4
depends_on: ["12-03"]
files_modified:
  - apps/api/routes/v3/tournaments.py
  - apps/api/tests/integration/test_tournaments_integration.py
  - apps/api/tests/integration/test_config_tournament.py  # NEW — API route/scope integration tests for the global config endpoints
autonomous: true
requirements: []
must_haves:
  truths:
    - "D-03: pause and debug-cycle-length are mutated via config-level routes (not /categories/{id}/...)"
    - "D-02: cadence is mutated via the config PATCH route"
    - "D-07: anchor (weekday/time/tz) is mutated via the config PATCH route"
    - "D-05: edition timing is readable via a GET /editions/active (or edition timing surfaced on the active cycle response)"
    - "D-08: the edition read surfaces the stored ends_at (frontend no longer derives it; closes frontend-spec §8)"
    - "Access control: every config mutation route enforces opt={'required_scopes': {'tournaments:write'}}; edition reads enforce tournaments:read"
  artifacts:
    - path: "apps/api/routes/v3/tournaments.py"
      provides: "config-level pause/debug/cadence/anchor PATCH handlers (scope-guarded), GET /editions/active read; per-cycle endpoints kept cycle-scoped"
      contains: "editions/active"
    - path: "apps/api/tests/integration/test_config_tournament.py"
      provides: "NEW — API route/scope integration tests: tournaments:write guard rejects unscoped callers on every config mutation; production debug-guard; edition read"
      contains: "required_scopes"
  key_links:
    - from: "config PATCH / pause / debug routes"
      to: "tournament_service global setters"
      via: "thin handler → service, scope-guarded tournaments:write"
      pattern: "required_scopes.*tournaments:write"
    - from: "GET /editions/active"
      to: "tournament_service.fetch_active_edition"
      via: "TournamentEditionResponse"
      pattern: "editions/active"
---

<objective>
Move tournament configuration mutation (pause, debug-cycle-length, cadence, anchor) from per-category routes to global config-level routes, and add an edition read surface (`GET /editions/active`) exposing the stored shared timing — while keeping all per-cycle endpoints (`/cycles`, `/cycles/{id}/leaderboard`, `/cycles/{id}/submit`) cycle-scoped (Open Question 1 / A5 resolved: keep cycle-scoped, add edition awareness). Every mutation route keeps its scope guard.

Purpose: Expose the global config and edition timing to the frontend/admin dashboard with correct access control, closing frontend-spec §8 (stored `ends_at`).

Output: Updated `routes/v3/tournaments.py` with config-level mutation handlers + edition read, plus a NEW `tests/integration/test_config_tournament.py` (API route/scope integration tests) and extended `tests/integration/test_tournaments_integration.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-overhaul-of-tournaments/12-CONTEXT.md
@.planning/phases/12-overhaul-of-tournaments/12-RESEARCH.md
@.planning/phases/12-overhaul-of-tournaments/12-PATTERNS.md
@.planning/phases/12-overhaul-of-tournaments/12-03-SUMMARY.md
@docs/specs/tournament-frontend-spec.md

<interfaces>
<!-- From Plan 03 (service) + Plan 02 (SDK) + existing routes. -->

From apps/api/services/tournament_service.py (Plan 03):
- bootstrap_edition, set_transitions_paused (global), set_debug_cycle_length (global, production-guarded), set_cadence/set_anchor, fetch_active_edition → TournamentEditionResponse.

From libs/sdk/src/genjishimada_sdk/tournaments.py (Plan 02):
- TournamentEditionResponse, config-level pause/debug/cadence/anchor request structs, TournamentConfigResponse.

From apps/api/routes/v3/tournaments.py (existing analogs):
- bootstrap_cycle handler (~464-510), set_transitions_paused handler (~511-547), set_debug_cycle_length handler (~549-590), config PATCH (~91-112).
- Scope pattern: `opt={"required_scopes": {"tournaments:write"}}` (~96), `tournaments:read` (~75, ~150).
- /cycles/{id}/leaderboard (~671-676) — keep cycle-scoped.
- Litestar Controller conventions: class-level tags/path/dependencies; service call in try/except → HTTPException/CustomHTTPException.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Routes — config-level pause/debug/cadence/anchor + GET /editions/active</name>
  <read_first>
    - apps/api/routes/v3/tournaments.py (bootstrap_cycle ~464-510; set_transitions_paused ~511-547; set_debug_cycle_length ~549-590; config PATCH ~91-112; scope opt usages ~75,96,150; /cycles/{id}/leaderboard ~671-676)
    - apps/api/services/exceptions/tournaments.py (DebugRouteDisabledError + *NotFoundError → HTTP mapping)
    - apps/api/tests/integration/test_tournaments_integration.py (EXISTING route/scope integration test analog — copy its client setup, X-API-KEY scope fixtures, and 401/403-vs-200 assertion style; this is the pattern to mirror for the NEW config test)
    - apps/api/tests/bot/test_config_tournament.py (NOTE: this is a BOT TOML config test — channel/role IDs — NOT a route/scope test; do NOT mirror it. The NEW API test file lives at tests/integration/test_config_tournament.py and tests route scope enforcement)
    - .planning/phases/12-overhaul-of-tournaments/12-PATTERNS.md (the route § — move pause/debug to config-level; scope-guard pattern; keep per-cycle endpoints cycle-scoped)
    - docs/specs/tournament-frontend-spec.md (§3 cycle shapes, §6 polling, §8 missing ends_at — the gap this closes)
  </read_first>
  <action>
    In `apps/api/routes/v3/tournaments.py`:
    Replace the per-category pause/debug handlers with config-level handlers: a global pause PATCH and a global debug-cycle-length PATCH that call the Plan-03 global service setters and return the config-level response. Add cadence + anchor mutation to the config PATCH handler (or dedicated PATCH routes) calling set_cadence/set_anchor. ALL mutations keep `opt={"required_scopes": {"tournaments:write"}}`; the debug route additionally relies on the service-side production guard (maps DebugRouteDisabledError → an appropriate HTTP error). Validate anchor_tz / cadence inputs via the msgspec request struct typing; let the service tz-validation raise → convert to HTTPException.
    Add `GET /editions/active` returning `TournamentEditionResponse` (id/started_at/ends_at/status) with `opt={"required_scopes": {"tournaments:read"}}` — this surfaces the stored shared timing so the frontend stops deriving ends_at (D-05/D-08, closes frontend-spec §8). Add `edition_id` (and optionally edition started_at/ends_at) to the cycle response if the existing cycle read does not already carry it.
    KEEP `/cycles`, `/cycles/{id}/leaderboard`, `/cycles/{id}/submit`, `/categories/{id}/next-cycle` cycle-scoped (submit/leaderboard/rewards stay per cycle — A5). Rename the bootstrap route target to bootstrap_edition. Keep all Controller conventions (tags/path/dependencies, try/except → HTTPException/CustomHTTPException).
    Create NEW `apps/api/tests/integration/test_config_tournament.py` — API route/scope integration tests for the global config endpoints (this file does NOT exist yet). Mirror the client/fixture setup from the existing `tests/integration/test_tournaments_integration.py`. Assert: (a) every config mutation route (pause/debug/cadence/anchor) rejects an unauthenticated or wrong-scope caller with 401/403 and accepts a `tournaments:write` caller with 200; (b) the debug-cycle-length route is rejected in a production-simulated environment (DebugRouteDisabledError → HTTP error); (c) `GET /editions/active` requires `tournaments:read` and returns the stored started_at + ends_at. Extend `tests/integration/test_tournaments_integration.py` if the edition_id-on-cycle response addition needs coverage there.
    Flag the frontend-spec update (stored ends_at on the edition read) in the plan SUMMARY for /gsd-transition.
  </action>
  <verify>
    <automated>uv run --directory apps/api pytest tests/integration/test_tournaments_integration.py tests/integration/test_config_tournament.py --no-testmon -p no:xdist</automated>
  </verify>
  <acceptance_criteria>
    - NEW file `apps/api/tests/integration/test_config_tournament.py` exists and is collected by the verify command (the `<automated>` path resolves and pytest discovers tests in it).
    - `routes/v3/tournaments.py` contains an `editions/active` GET route returning TournamentEditionResponse.
    - Source assertion: each config-mutation handler (pause/debug/cadence/anchor) declares `opt={"required_scopes": {"tournaments:write"}}`; the edition read declares `tournaments:read`.
    - Behavior: an unauthenticated / wrong-scope request to a config mutation route is rejected (401/403); a tournaments:write request succeeds.
    - Behavior: GET /editions/active returns the stored started_at + ends_at of the active edition.
    - Per-cycle endpoints remain present and cycle-scoped (no removal).
    - Integration tests pass.
  </acceptance_criteria>
  <done>Config mutations (pause/debug/cadence/anchor) are global and scope-guarded; the active-edition read exposes stored timing; per-cycle endpoints unchanged; the NEW route/scope integration test file exists and passes alongside the extended tournaments integration test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → API config routes | untrusted admin input mutating global cadence/anchor/pause/debug |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-09 | Elevation of Privilege | global cadence/anchor/pause/debug mutation routes | mitigate | Every mutation route declares `opt={"required_scopes": {"tournaments:write"}}`; superuser bypass is the existing model; NEW integration test asserts unauthenticated/wrong-scope → 401/403 |
| T-12-07 | Elevation of Privilege | debug-cycle-length route in production | mitigate | tournaments:write scope + service-side APP_ENVIRONMENT == 'production' reject (Plan 03) → HTTP error |
| T-12-10 | Input Validation | anchor_tz / cadence body | mitigate | msgspec struct typing on the request; service validates anchor_tz against pg_timezone_names; cadence constrained to weekly/biweekly |
| T-12-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages (RESEARCH Package Legitimacy Audit: not applicable) |
</threat_model>

<verification>
- Targeted: `uv run --directory apps/api pytest tests/integration/test_tournaments_integration.py tests/integration/test_config_tournament.py --no-testmon -p no:xdist`
- Source gate: `grep -n "required_scopes" apps/api/routes/v3/tournaments.py` shows write-scope on every config mutation handler
- Lint: `just lint-api`
- Wave merge: `uv run --directory apps/api pytest -n 4 --no-testmon`
</verification>

<success_criteria>
- Pause/debug/cadence/anchor are global config-level routes, all tournaments:write guarded (D-02/D-03/D-07).
- GET /editions/active exposes stored shared timing (D-05/D-08).
- Per-cycle endpoints retained cycle-scoped.
- NEW tests/integration/test_config_tournament.py exists, is collected, and asserts scope enforcement + production debug-guard.
- Integration tests green; `just lint-api` clean; frontend-spec ends_at change flagged.
</success_criteria>

<output>
Create `.planning/phases/12-overhaul-of-tournaments/12-04-SUMMARY.md` when done.
</output>
