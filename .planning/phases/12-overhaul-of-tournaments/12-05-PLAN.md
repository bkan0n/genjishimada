---
phase: 12-overhaul-of-tournaments
plan: 05
type: execute
wave: 4
depends_on: ["12-03"]
files_modified:
  - apps/bot/extensions/tournaments.py
  - apps/api/tests/bot/test_tournaments_handler.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "D-09: a single bot handler _on_edition_rollover on api.tournament.rollover replaces the _on_cycle_started + _on_cycle_completed pair"
    - "D-10: the rollover card renders conditional sections — a results section iff event.results, a starting section iff event.started"
    - "D-10: the three cases render correctly — normal (results+started), into-hiatus (results-only), out-of-hiatus (started-only)"
    - "Champion-role transfer runs first and only when there are results, reusing the existing strip-all-then-grant logic"
    - "Security: winners are mentioned by numeric <@id> only with AllowedMentions(users=allow-list, everyone=False, roles=False); ping text lives inside a ui.TextDisplay (CV2 LayoutView has no content=)"
  artifacts:
    - path: "apps/bot/extensions/tournaments.py"
      provides: "_on_edition_rollover combined-rollover consumer with conditional CV2 sections; reused _transfer_champion_role"
      contains: "api.tournament.rollover"
  key_links:
    - from: "_on_edition_rollover"
      to: "_transfer_champion_role"
      via: "iterate event.results, transfer champion per category before sending the card"
      pattern: "_transfer_champion_role"
    - from: "_on_edition_rollover"
      to: "TournamentRolloverEvent"
      via: "@queue_consumer(api.tournament.rollover, struct_type=TournamentRolloverEvent, idempotent=True)"
      pattern: "TournamentRolloverEvent"
---

<objective>
Collapse the bot's `_on_cycle_started` + `_on_cycle_completed` consumer pair into a single `_on_edition_rollover` handler on `api.tournament.rollover` that renders ONE CV2 card with conditional results/starting sections (D-09/D-10), reusing the existing champion-role transfer and AllowedMentions safety verbatim.

Purpose: Deliver the user-visible outcome — one combined announcement per rollover, with the correct three-case rendering (normal / into-hiatus / out-of-hiatus) — completing the event path from DB → outbox → bot.

Output: Updated `apps/bot/extensions/tournaments.py` (one consumer replacing two) + extended handler tests covering the three conditional render cases.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-overhaul-of-tournaments/12-CONTEXT.md
@.planning/phases/12-overhaul-of-tournaments/12-RESEARCH.md
@.planning/phases/12-overhaul-of-tournaments/12-PATTERNS.md
@.planning/phases/12-overhaul-of-tournaments/12-03-SUMMARY.md

<interfaces>
<!-- From Plan 02 (SDK event) + existing bot handlers. -->

From libs/sdk/src/genjishimada_sdk/tournaments.py (Plan 02):
- TournamentRolloverEvent(edition_id: int, results: list[TournamentCycleCompletedEvent], started: list[TournamentCycleStartedEvent])
- TournamentCycleCompletedEvent (cycle_id, category_id, standings, winner_user_id), TournamentCycleStartedEvent (per-category start element).

From apps/bot/extensions/tournaments.py (existing analogs):
- _on_cycle_started (~298-337), _on_cycle_completed (~339-410) — the pair to fuse.
- _transfer_champion_role (~475-547) — strips ALL holders first then grants the winner (self-healing, A6); REUSE verbatim.
- CV2 build: ui.Container + per-entry ui.Separator() + ui.TextDisplay(section) (~375-400); winners ping inside ui.TextDisplay (CV2 LayoutView has no content= — MEMORY.md).
- AllowedMentions(users=[Object(id=w)...], everyone=False, roles=False) (~405-408) — numeric-id-only mentions.
- @queue_consumer decorator usage; self.bot.api.get_tournament_category / get_map for on-receipt lookups (bot never reads Postgres).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bot — single _on_edition_rollover handler with conditional sections</name>
  <read_first>
    - apps/bot/extensions/tournaments.py (_on_cycle_started ~298-337; _on_cycle_completed ~339-410; _transfer_champion_role ~475-547; CV2 card build ~375-408; @queue_consumer usage)
    - apps/bot/extensions/_queue_registry.py (queue_consumer decorator signature)
    - apps/api/tests/bot/test_tournaments_handler.py (existing handler test conventions — mock api, assert card sections + AllowedMentions)
    - .planning/phases/12-overhaul-of-tournaments/12-PATTERNS.md (the bot § — fuse the pair; conditional sections; PRESERVE champion-transfer order + AllowedMentions + ui.TextDisplay ping)
    - .planning/phases/12-overhaul-of-tournaments/12-RESEARCH.md (§"Code Examples": Bot handler conditional rendering; Security Domain mention-injection rule)
  </read_first>
  <action>
    In `apps/bot/extensions/tournaments.py`:
    Remove the two `@queue_consumer` handlers `_on_cycle_started` and `_on_cycle_completed`. Add ONE `@queue_consumer("api.tournament.rollover", struct_type=TournamentRolloverEvent, idempotent=True)` handler `_on_edition_rollover(self, event, message)`.
    Ordering (preserve Pitfall 5 ordering — roles before send): FIRST, iff `event.results` is non-empty, iterate the per-category results, fetch each category via `self.bot.api.get_tournament_category(entry.category_id)`, and call `_transfer_champion_role(entry, category)` (REUSE verbatim — it strips all holders then grants the winner, self-healing the cutover, A6).
    THEN build ONE CV2 card: append a results `ui.Container` section iff `event.results` (per-category standings/winner), then append a "new cycle / starting" section iff `event.started` (per-category new maps), separated by `ui.Separator()`; send once. If both lists are empty, do nothing (defensive).
    PRESERVE the load-bearing safety rules verbatim: winners mentioned by numeric `<@id>` ONLY; `AllowedMentions(users=[...], everyone=False, roles=False)`; the winners ping must live INSIDE a `ui.TextDisplay` in the LayoutView (CV2 send accepts no content= — MEMORY.md). Fetch all missing map/category data via the API on receipt (bot never reads Postgres).
    Remove now-dead references to TournamentCyclesStarted/CompletedEvent imports in the bot module.
  </action>
  <verify>
    <automated>uv run --directory apps/api pytest tests/bot/test_tournaments_handler.py -k "rollover" --no-testmon -p no:xdist</automated>
  </verify>
  <acceptance_criteria>
    - `apps/bot/extensions/tournaments.py` contains `_on_edition_rollover` decorated with `@queue_consumer("api.tournament.rollover", struct_type=TournamentRolloverEvent, idempotent=True)`; `_on_cycle_started`/`_on_cycle_completed` removed.
    - Behavior (tests, three cases): normal event (results+started) → card has both sections + champion transfer called; into-hiatus (results-only) → results section only, transfer called, no starting section; out-of-hiatus (started-only) → starting section only, no transfer call.
    - Source/behavior: winners pinged via numeric <@id> with AllowedMentions(everyone=False, roles=False); ping text inside a ui.TextDisplay.
    - Champion transfer runs before the card is sent and only when results are present.
    - Handler test passes.
  </acceptance_criteria>
  <done>One combined-rollover consumer renders conditional results/starting sections across the three cases, reuses champion transfer + AllowedMentions safety, and the started/completed pair is gone; tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RabbitMQ event → bot card | event-carried winner names could inject mentions |
| bot → Discord roles | champion-role mutation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-11 | Spoofing/Tampering | mention-injection in the rollover card | mitigate | Winners mentioned by numeric `<@id>` ONLY; AllowedMentions(users=allow-list, everyone=False, roles=False); never mention from free-text names (preserve tournaments.py:405-408) |
| T-12-12 | Tampering | duplicate rollover delivery double-posting / double-role-grant | mitigate | @queue_consumer(idempotent=True) claims the message; champion transfer strips-all-then-grants (idempotent end state); single idempotency key upstream (Plan 03) |
| T-12-13 | Tampering | bot writing to Postgres | mitigate | Handler reads missing data only via self.bot.api.* and performs Discord-only actions (role transfer, card post) — architecturally forbidden to write DB (CLAUDE.md) |
| T-12-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages (RESEARCH Package Legitimacy Audit: not applicable) |
</threat_model>

<verification>
- Targeted: `uv run --directory apps/api pytest tests/bot/test_tournaments_handler.py -k "rollover" --no-testmon -p no:xdist`
- Source gate: `grep -n "api.tournament.rollover\|AllowedMentions" apps/bot/extensions/tournaments.py`
- Lint: `just lint-bot`
- Wave merge: `uv run --directory apps/api pytest -n 4 --no-testmon`
</verification>

<success_criteria>
- One _on_edition_rollover consumer replaces the started/completed pair (D-09).
- Conditional sections render the three cases correctly (D-10).
- Champion transfer + AllowedMentions + ui.TextDisplay ping safety preserved.
- Handler tests green; `just lint-bot` clean.
</success_criteria>

<output>
Create `.planning/phases/12-overhaul-of-tournaments/12-05-SUMMARY.md` when done.
</output>
