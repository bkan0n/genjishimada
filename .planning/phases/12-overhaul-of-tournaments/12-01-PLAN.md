---
phase: 12-overhaul-of-tournaments
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/migrations/0024_tournament_editions_overhaul.sql
  - apps/api/tests/repository/tournaments/conftest.py
  - apps/api/tests/repository/tournaments/test_grid_boundary.py
  - apps/api/tests/repository/tournaments/test_edition_transitions.py
  - apps/api/tests/integration/test_tournaments_schema.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "D-01: all categories transition together on one shared grid — one rollover creates exactly one edition with one child cycle per active category"
    - "D-02: a single global cadence ('weekly'|'biweekly') on tournaments.config replaces per-category tournaments.categories.cycle_frequency"
    - "D-03: global transitions_paused + debug_cycle_seconds live on tournaments.config; per-category columns from migration 0023 are dropped"
    - "D-05: tournaments.editions is the top-level timing entity; each tournaments.cycles row links to it via edition_id"
    - "D-06: an edition rolls over on a configured anchor_weekday + anchor_time slot"
    - "D-07: anchor (weekday/time/tz) is stored on tournaments.config and changes take effect next edition"
    - "D-08: the transition function NEVER writes now() into edition started_at/ends_at; the next edition's started_at = prev.ends_at exactly and ends_at = started_at + period"
    - "D-13a: bootstrap/resume snaps the first/next edition to the next grid boundary via next_grid_boundary()"
    - "D-13: fresh-restart wipes in-flight cycles and tournament completions"
    - "D-14: TRUNCATE removes all tournaments.cycles, tournaments.completions, tournaments.editions rows"
    - "D-15: core.completions.tournament_completion_id is NULLed on cross-written rows while core.completions PB rows are preserved (no cascade delete)"
  artifacts:
    - path: "apps/api/migrations/0024_tournament_editions_overhaul.sql"
      provides: "edition table + child FK, global config columns, drop per-category columns, next_grid_boundary(), process_edition_transitions() rewrite, fresh-restart wipe, idempotent pg_cron re-registration"
      contains: "CREATE TABLE IF NOT EXISTS tournaments.editions"
    - path: "apps/api/tests/repository/tournaments/test_grid_boundary.py"
      provides: "next_grid_boundary() correctness incl. DST crossing"
    - path: "apps/api/tests/repository/tournaments/test_edition_transitions.py"
      provides: "drift-immunity, single-edition, hiatus behavior of process_edition_transitions()"
  key_links:
    - from: "process_edition_transitions()"
      to: "tournaments.editions.started_at/ends_at"
      via: "next.started_at = old.ends_at (no now())"
      pattern: "started_at.*ends_at"
    - from: "fresh-restart wipe"
      to: "core.completions"
      via: "UPDATE SET tournament_completion_id = NULL before TRUNCATE (no cascade)"
      pattern: "tournament_completion_id = NULL"
---

<objective>
Build the database foundation for the single-edition tournament model: a new `tournaments.editions` parent entity holding the shared grid-anchored timing, global cadence/anchor/pause/debug config on `tournaments.config`, a grid-boundary helper, the rewritten status-only transition function (the drift fix), the fresh-restart wipe, and idempotent pg_cron re-registration — plus the Wave 0 test scaffolds that prove drift-immunity, grid-boundary correctness, and the safe wipe.

Purpose: Eliminate the cross-category drift bug (re-stamped `started_at = now()`) at its root by making the DB record exact grid timestamps, and establish the schema every downstream layer (SDK, repo, service, bot) reads from.

Output: `apps/api/migrations/0024_tournament_editions_overhaul.sql` + three Wave 0 test files (new/extended) that pass against the pytest-databases test DB.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-overhaul-of-tournaments/12-CONTEXT.md
@.planning/phases/12-overhaul-of-tournaments/12-RESEARCH.md
@.planning/phases/12-overhaul-of-tournaments/12-PATTERNS.md
@.planning/phases/12-overhaul-of-tournaments/12-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 0: Wave 0 test scaffolds — grid boundary + edition transition fixtures</name>
  <read_first>
    - apps/api/tests/repository/tournaments/conftest.py (existing fixtures + DB setup)
    - apps/api/tests/repository/tournaments/test_cycle_transitions.py (the model: invokes the PL/pgSQL fn directly, asserts on resulting rows)
    - apps/api/tests/repository/tournaments/test_lifecycle_control.py (pause/debug behavior assertions)
    - apps/api/migrations/0021_tournament_cycle_transitions.sql (current fn + cron guard on pg_extension — test DBs have no pg_cron)
    - .planning/phases/12-overhaul-of-tournaments/12-RESEARCH.md (§"Wave 0 Gaps", §"Phase Requirements → Test Map")
  </read_first>
  <action>
    Create `tests/repository/tournaments/test_grid_boundary.py` and `tests/repository/tournaments/test_edition_transitions.py` as RED scaffolds (they import nothing that does not exist yet but assert the behaviors below; they go GREEN once Task 1's migration is applied to the test DB by the existing schema-load fixture).
    Add two shared fixtures to `tests/repository/tournaments/conftest.py`: an `advance_past_ends_at` helper that sets an edition's `ends_at` to a past instant, and a `simulate_late_cron` helper that invokes `SELECT tournaments.process_edition_transitions()` after an arbitrary simulated delay — both prove D-08 without real time passing.
    test_grid_boundary.py: call `SELECT tournaments.next_grid_boundary($1,$2,$3,$4,$5)` and assert it returns the correct next weekday@time-of-day in the given tz, including a date that crosses a DST transition (e.g. America/Los_Angeles spring-forward) — assert the wall-clock slot is preserved (D-06/D-07).
    test_edition_transitions.py: (a) `drift` — two consecutive rollovers under a simulated-late cron tick land on exact grid instants, asserting `next.started_at == prev.ends_at` and no now() leakage (D-08); (b) `single_edition` — one rollover creates ONE editions row and one child cycle per active category, all child cycles sharing the edition timing (D-01/D-05); (c) `hiatus` — with config.transitions_paused = TRUE, crossing the boundary completes the current edition AND creates NO next edition AND writes a results-only edition_rollover outbox row (D-12).
    Mark the assertion bodies so they fail loudly until the migration exists (no skips).
  </action>
  <verify>
    <automated>uv run --directory apps/api pytest tests/repository/tournaments/test_grid_boundary.py tests/repository/tournaments/test_edition_transitions.py --no-testmon -p no:xdist</automated>
  </verify>
  <acceptance_criteria>
    - Both new test files exist and import the conftest fixtures `advance_past_ends_at` and `simulate_late_cron`.
    - Before Task 1: the test run fails (RED) because `tournaments.editions` / `next_grid_boundary` / `process_edition_transitions` do not exist (NOT skipped).
    - Tests reference test cases named `drift`, `single_edition`, `hiatus` (the `-k` selectors in 12-RESEARCH §Test Map resolve).
    - No watch-mode flags; targeted run completes < 30s.
  </acceptance_criteria>
  <done>Three Wave 0 scaffolds (2 test files + conftest fixtures) committed and failing for the right reason (missing schema, not import errors).</done>
</task>

<task type="auto">
  <name>Task 1: Migration 0024 — edition schema, global config, grid fn, transition rewrite, wipe, cron</name>
  <read_first>
    - apps/api/migrations/0020_tournaments.sql (config singleton CHECK(id=1) line 16; tournaments.categories cycle_frequency ~36-37; tournaments.cycles status enum + columns ~60-66; pending_transitions outbox cycle_id NOT NULL + event_type CHECK ~142-143; core.completions FK ON DELETE SET NULL ~170-172; streaks.last_cycle_id ON DELETE SET NULL ~124; xp_grants cycle_id ON DELETE CASCADE)
    - apps/api/migrations/0021_tournament_cycle_transitions.sql (select_eligible_map ~33-89; advisory lock ~113; cron unschedule/schedule guarded on pg_extension ~274-291)
    - apps/api/migrations/0023_tournament_cycle_lifecycle_control.sql (per-category transitions_paused/debug_cycle_seconds cols ~26-31; transition fn body; the now() writes at lines 152 and 183 — the drift bug to delete; tier-then-time RANK() snapshot CTE ~88-122; outbox INSERTs ~131-141, 159-203)
    - apps/api/migrations/0013_coin_store.sql (store.config stored next_rotation_at ~18-32; cron guard ~280-300 — singleton + stored-timestamp precedent)
    - .planning/phases/12-overhaul-of-tournaments/12-RESEARCH.md (§"Code Examples": Edition table, Global config columns, Fresh-restart wipe; §"Architecture Patterns" Pattern 1 next_grid_boundary body lines 200-227, Pattern 2 status-only flip, Pattern 3 outbox — note the payload list key is `started`)
    - .planning/phases/12-overhaul-of-tournaments/12-PATTERNS.md (the migration §, six sub-parts with verbatim-copy anchors)
  </read_first>
  <action>
    Create `apps/api/migrations/0024_tournament_editions_overhaul.sql` as ONE migration (Discretion resolved per RESEARCH Open Question 2 — single file, atomic wipe+schema+function+cron). Wrap DDL/DML in `BEGIN;`/`COMMIT;` like 0023; keep the `CREATE EXTENSION`/`cron.schedule` block as an UNWRAPPED `DO $body$ ... $body$` guarded on `pg_extension` like 0021 so test DBs no-op.
    (1) Create table `tournaments.editions` (id GENERATED ALWAYS AS IDENTITY PK; started_at timestamptz NOT NULL; ends_at timestamptz NOT NULL; status text NOT NULL DEFAULT 'active' CHECK status IN ('active','completed'); created_at timestamptz DEFAULT now()). Indexes on status and ends_at. Add `tournaments.cycles.edition_id int REFERENCES tournaments.editions(id) ON DELETE CASCADE` + index.
    (2) ALTER `tournaments.config` ADD COLUMN IF NOT EXISTS: `cadence text NOT NULL DEFAULT 'weekly' CHECK (cadence IN ('weekly','biweekly'))` (D-02), `anchor_weekday int NOT NULL DEFAULT 1 CHECK (BETWEEN 0 AND 6)` (PostgreSQL EXTRACT(DOW) convention 0=Sun..6=Sat, A8 — document in COMMENT), `anchor_time time NOT NULL DEFAULT '00:00'`, `anchor_tz text NOT NULL DEFAULT 'UTC'` (D-07), `transitions_paused boolean NOT NULL DEFAULT FALSE` (D-03/D-12), `debug_cycle_seconds int CHECK (NULL OR > 0)` (D-03). Add a CHECK or service-side validation that anchor_tz is a known tz (RESEARCH Security V5) — use `CHECK (anchor_tz IN (SELECT name FROM pg_timezone_names))` is not allowed in a CHECK (subquery); instead leave the tz validation to the service layer (note this in a column COMMENT) and rely on the default 'UTC'.
    (3) Migrate per-category levers to global then DROP them: `UPDATE tournaments.config SET transitions_paused = (SELECT bool_or(transitions_paused) FROM tournaments.categories) WHERE id = 1;` then `ALTER TABLE tournaments.categories DROP COLUMN IF EXISTS transitions_paused, DROP COLUMN IF EXISTS debug_cycle_seconds, DROP COLUMN IF EXISTS cycle_frequency` (D-02/D-03).
    (4) Outbox schema for the combined event (RESEARCH Pattern 3, A3 resolved → option a): `ALTER TABLE tournaments.pending_transitions ADD COLUMN IF NOT EXISTS edition_id int REFERENCES tournaments.editions(id) ON DELETE CASCADE`, make `cycle_id` nullable (`ALTER COLUMN cycle_id DROP NOT NULL`), and extend the `event_type` CHECK to include `'edition_rollover'`.
    (5) Create `tournaments.next_grid_boundary(p_from timestamptz, p_weekday int, p_tod time, p_tz text, p_period interval) RETURNS timestamptz LANGUAGE plpgsql` — body verbatim from RESEARCH Pattern 1 lines 200-227 (AT TIME ZONE local-wall-clock composition for DST correctness; step one p_period if the candidate is already past). Add `COMMENT ON FUNCTION` per the 0021 convention.
    (6) CREATE OR REPLACE FUNCTION `tournaments.process_edition_transitions()` (keep the cron job NAME `tournament-cycle-transitions`, A7). Copy the EXACT 0023 skeleton: `pg_try_advisory_xact_lock(2025070100)` (REUSE — must not collide with store lock 1234567890), one transaction, the tier-then-time RANK() snapshot CTE per child cycle copied VERBATIM from 0023:88-122. Replace the per-category loop with a single-edition detection `WHERE status='active' AND now() >= ends_at`. DELETE the now() writes (0023:152,183): the next edition's `started_at = old.ends_at`, `ends_at = old.ends_at + v_period` where v_period = CASE debug_cycle_seconds NOT NULL → make_interval(secs=>...) WHEN cadence='biweekly' → make_interval(weeks=>2) ELSE make_interval(weeks=>1). On pause (config.transitions_paused TRUE at boundary): finalize current edition (status→completed) and DO NOT create the next edition (D-12 hiatus). Pre-roll next maps via `select_eligible_map(category_id)` per active category (reused as-is). Write ONE `pending_transitions` row with `event_type='edition_rollover'`, `edition_id = <finalized edition id>`, `cycle_id = NULL`, payload = jsonb with EXACTLY these three keys: `{results:[...per finalized child cycle...], started:[...per new child cycle...], edition_id}`. The list key for the newly-started cycles is `started` (NOT `next`), matching the SDK `TournamentRolloverEvent` field name byte-identically (RESEARCH Pattern 3 lines 280-296 + 12-02-PLAN Task 1 lines 100/102). All three jsonb keys (`results`, `started`, `edition_id`) MUST be byte-identical to Plan 02's struct fields — a key mismatch makes `msgspec.convert(row["payload"], TournamentRolloverEvent)` (12-03-PLAN Task 2) raise ValidationError, leaving the outbox row unpublished so the rollover announcement never fires (Pitfall 5).
    (7) Fresh-restart wipe (D-13/14/15) inside the BEGIN/COMMIT: `UPDATE core.completions SET tournament_completion_id = NULL WHERE tournament_completion_id IS NOT NULL;` THEN `TRUNCATE tournaments.completions, tournaments.cycles, tournaments.editions RESTART IDENTITY CASCADE;` THEN `DELETE FROM tournaments.pending_transitions;`. NEVER add CASCADE to the core FK; NEVER delete core.completions rows.
    (8) Idempotent pg_cron re-registration: copy 0021:274-291 VERBATIM inside the unwrapped `DO $body$` guard, pointing the schedule at `SELECT tournaments.process_edition_transitions()`.
    Do NOT bootstrap the first edition inside the migration — bootstrap is a service-driven action (Plan 03) so the snap-to-boundary uses the configured anchor at runtime (D-13a).
  </action>
  <verify>
    <automated>uv run --directory apps/api pytest tests/repository/tournaments/test_grid_boundary.py tests/repository/tournaments/test_edition_transitions.py tests/integration/test_tournaments_schema.py -k "drift or single_edition or hiatus or overhaul or preserve_pbs or grid" --no-testmon -p no:xdist</automated>
  </verify>
  <acceptance_criteria>
    - `apps/api/migrations/0024_tournament_editions_overhaul.sql` contains `CREATE TABLE IF NOT EXISTS tournaments.editions` and `CREATE OR REPLACE FUNCTION tournaments.next_grid_boundary` and `CREATE OR REPLACE FUNCTION tournaments.process_edition_transitions`.
    - Source assertion: `grep -v '^--' apps/api/migrations/0024_tournament_editions_overhaul.sql | grep -c "started_at = now()"` returns 0 (the drift bug is absent from edition timestamp writes).
    - Source assertion: the outbox payload uses the key `started` (not `next`) — `grep -v '^--' apps/api/migrations/0024_tournament_editions_overhaul.sql | grep -c "'started'"` returns >= 1, and `grep -v '^--' apps/api/migrations/0024_tournament_editions_overhaul.sql | grep -c "'next'"` returns 0 (byte-identical to TournamentRolloverEvent fields `results`/`started`/`edition_id`).
    - Source assertion: the file contains `UPDATE core.completions SET tournament_completion_id = NULL` appearing before `TRUNCATE tournaments.completions`.
    - Migration applies clean against the pytest-databases test DB (test_tournaments_schema overhaul case passes).
    - Behavior: drift test green (next.started_at == prev.ends_at under late cron); single_edition green; hiatus green (results-only, no next edition).
    - Behavior: preserve_pbs green — core.completions row count for cross-written PBs unchanged, their tournament_completion_id is NULL.
    - The cron block is guarded on `pg_extension` (no-ops on the test DB).
  </acceptance_criteria>
  <done>Migration 0024 applies cleanly; Wave 0 transition/grid/wipe tests are GREEN; no now() write to edition timestamps; outbox payload key is `started` not `next`; core PB rows preserved with NULLed FK.</done>
</task>

<task type="auto">
  <name>Task 2: Extend schema integration test — wipe/FK-null/PB-preservation</name>
  <read_first>
    - apps/api/tests/integration/test_tournaments_schema.py (existing schema assertions to extend in place)
    - apps/api/migrations/0024_tournament_editions_overhaul.sql (just created — the wipe + edition table + config columns)
    - .planning/phases/12-overhaul-of-tournaments/12-RESEARCH.md (§"Phase Requirements → Test Map" D-13/14/15 + D-15 FK-safety rows, §"Common Pitfalls" Pitfall 4)
  </read_first>
  <action>
    Extend `tests/integration/test_tournaments_schema.py` with `overhaul` and `preserve_pbs` cases: seed `tournaments.completions` rows cross-written into `core.completions` (with `tournament_completion_id` set), seed `tournaments.cycles`/`editions`, then run the wipe portion of migration 0024 (or assert post-migration state if the fixture applies all migrations); assert (a) all `tournaments.cycles`/`completions`/`editions` rows gone, (b) `core.completions` row count for the cross-written PBs UNCHANGED, (c) their `tournament_completion_id` is NULL, (d) the new `tournaments.config` columns exist with documented defaults, (e) `tournaments.categories` no longer has `cycle_frequency`/`transitions_paused`/`debug_cycle_seconds` columns.
  </action>
  <verify>
    <automated>uv run --directory apps/api pytest tests/integration/test_tournaments_schema.py -k "overhaul or preserve_pbs" --no-testmon -p no:xdist</automated>
  </verify>
  <acceptance_criteria>
    - Behavior: post-wipe core.completions count unchanged for cross-written PBs; tournament_completion_id NULL (D-15).
    - Behavior: tournaments.categories.cycle_frequency / transitions_paused / debug_cycle_seconds columns absent (D-02/D-03).
    - Behavior: tournaments.config has cadence/anchor_weekday/anchor_time/anchor_tz/transitions_paused/debug_cycle_seconds.
    - Test run exits 0.
  </acceptance_criteria>
  <done>Schema integration test proves the fresh-restart wipe preserves PBs, drops per-category columns, and adds global config columns.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pg_cron → DB transition fn | scheduled job mutates edition/cycle state; concurrent ticks possible |
| migration DML → core.completions | wipe must not destroy non-tournament PB rows |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Tampering | fresh-restart wipe (D-13/14/15) | mitigate | Explicit `UPDATE core.completions SET tournament_completion_id = NULL` BEFORE `TRUNCATE tournaments.*`; core FK stays `ON DELETE SET NULL`, never CASCADE; integration test asserts core row count preserved + FK NULLed (Task 2) |
| T-12-02 | Denial of Service | process_edition_transitions() | mitigate | `pg_try_advisory_xact_lock(2025070100)` serializes concurrent cron ticks (auto-released on COMMIT); RETURN on lock-miss (no double rollover) |
| T-12-03 | Tampering | edition timestamp writes (drift bug) | mitigate | Function never writes now() into started_at/ends_at; next.started_at = prev.ends_at; grep gate asserts 0 occurrences; drift test proves late-cron immunity |
| T-12-04 | Denial of Service | invalid anchor_tz crashing the cron fn | mitigate | tz validated at the service write layer (Plan 03); default 'UTC'; bad value can only be set by a `tournaments:write` admin, never anonymously |
| T-12-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages this phase (RESEARCH Package Legitimacy Audit: not applicable); no install tasks, no [ASSUMED]/[SUS] packages |
</threat_model>

<verification>
- Targeted: `uv run --directory apps/api pytest tests/repository/tournaments/test_grid_boundary.py tests/repository/tournaments/test_edition_transitions.py tests/integration/test_tournaments_schema.py --no-testmon -p no:xdist`
- Source gate: `grep -v '^--' apps/api/migrations/0024_tournament_editions_overhaul.sql | grep -c "started_at = now()"` == 0
- Source gate: `grep -v '^--' apps/api/migrations/0024_tournament_editions_overhaul.sql | grep -c "'next'"` == 0 (payload uses `started`, not `next`)
- Wave merge: `uv run --directory apps/api pytest -n 4 --no-testmon` (TRUE full suite — testmon hides failures, MEMORY.md)
</verification>

<success_criteria>
- Migration 0024 applies cleanly to the test DB and is idempotent (IF NOT EXISTS / IF EXISTS guards).
- Edition entity owns shared grid timing; cycles link via edition_id (D-01/D-05).
- Global cadence/anchor/pause/debug on config; per-category columns dropped (D-02/D-03/D-06/D-07).
- No now() in edition timestamp writes; drift test green (D-08); bootstrap snap helper exists (D-13a, consumed in Plan 03).
- Outbox payload keys are byte-identical to TournamentRolloverEvent (`results`, `started`, `edition_id`) — no `next` key.
- Fresh-restart wipe preserves core PBs while NULLing the FK (D-13/D-14/D-15).
- Cron re-registration guarded on pg_extension; advisory lock id 2025070100.
</success_criteria>

<output>
Create `.planning/phases/12-overhaul-of-tournaments/12-01-SUMMARY.md` when done.
</output>
