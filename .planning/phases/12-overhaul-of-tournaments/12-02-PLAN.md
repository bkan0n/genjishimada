---
phase: 12-overhaul-of-tournaments
plan: 02
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - libs/sdk/src/genjishimada_sdk/tournaments.py
  - libs/sdk/src/genjishimada_sdk/__init__.py
  - apps/api/repository/tournaments_repository.py
  - apps/api/tests/repository/tournaments/test_tournaments_repository.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "D-05: a TournamentEditionResponse struct carries the edition's id/started_at/ends_at/status (shared timing moves up off individual cycles)"
    - "D-08: edition ends_at is a stored field on the edition response (closes frontend-spec §8; timing no longer derived)"
    - "D-09: a single TournamentRolloverEvent struct collapses the started/completed event pair"
    - "D-10: TournamentRolloverEvent has conditional sections — results: list (empty on out-of-hiatus) + started: list (empty on into-hiatus)"
    - "D-11: TournamentRolloverEvent carries edition_id (the idempotency key source)"
    - "D-02: a global config response/request surfaces cadence ('weekly'|'biweekly')"
    - "D-03: a global config surface carries transitions_paused + debug_cycle_seconds (no per-category id framing)"
    - "D-07: a global config surface carries anchor_weekday/anchor_time/anchor_tz"
    - "D-08: edition CRUD repo methods bind grid timestamps as parameters — never now()"
  artifacts:
    - path: "libs/sdk/src/genjishimada_sdk/tournaments.py"
      provides: "TournamentRolloverEvent, TournamentEditionResponse, global config structs; retained per-cycle TournamentCycleStartedEvent/CompletedEvent"
      contains: "class TournamentRolloverEvent"
    - path: "apps/api/repository/tournaments_repository.py"
      provides: "edition CRUD (create with grid-timestamp params, fetch active edition), global config setters built on the allow-list SET builder, create_pending_transition with edition_id + nullable cycle_id"
      contains: "tournaments.editions"
  key_links:
    - from: "TournamentRolloverEvent"
      to: "TournamentCycleCompletedEvent / TournamentCycleStartedEvent"
      via: "results/started list element types"
      pattern: "TournamentRolloverEvent"
    - from: "edition create repo method"
      to: "tournaments.editions"
      via: "INSERT ... started_at, ends_at bound as $n (no now())"
      pattern: "INSERT INTO tournaments.editions"
---

<objective>
Mirror the new database model into the SDK and the repository layer: add the combined `TournamentRolloverEvent` (collapsing the started/completed pair into conditional results/started sections), the `TournamentEditionResponse` edition struct, and reshape the lifecycle structs to config-level (global) semantics; then add edition CRUD and global config setters to the repository — binding grid timestamps as parameters, never `now()`.

Purpose: Establish the type contracts (interface-first) that the service, outbox, route, and bot layers all build against, and the raw-SQL data access for editions + global config.

Output: Updated `tournaments.py` SDK module (+ `__init__.py` exports), updated `tournaments_repository.py`, and extended repository tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-overhaul-of-tournaments/12-CONTEXT.md
@.planning/phases/12-overhaul-of-tournaments/12-RESEARCH.md
@.planning/phases/12-overhaul-of-tournaments/12-PATTERNS.md
@.planning/phases/12-overhaul-of-tournaments/12-01-SUMMARY.md

<interfaces>
<!-- Contracts the executor needs — extracted from the codebase + RESEARCH. Use directly, no exploration. -->

From libs/sdk/src/genjishimada_sdk/tournaments.py (existing, to mirror/reshape):
- TournamentCycleStartedEvent (~431-451) — per-category start element; RETAIN as a payload element.
- TournamentCycleCompletedEvent (~453-466) — per-category results element (cycle_id, category_id, standings, winner_user_id); RETAIN as a payload element.
- TournamentCyclesStartedEvent / TournamentCyclesCompletedEvent (~469-496) — batch events; SUPERSEDED by TournamentRolloverEvent (do not delete the per-category element structs above).
- TournamentCycleResponse (~221-240) — mirror its shape for TournamentEditionResponse.
- TournamentCategoryLifecycleResponse (~177-191), TournamentPauseRequest (~194-202), TournamentDebugCycleLengthRequest (~205-213) — reshape to config-level (drop per-category id framing).
- UNSET/UnsetType usage for PATCH fields (~170-174).

New struct target shape (RESEARCH Code Examples):
- class TournamentRolloverEvent(Struct): edition_id: int; results: list[TournamentCycleCompletedEvent]; started: list[TournamentCycleStartedEvent]
- class TournamentEditionResponse(Struct): id: int; started_at: dt.datetime; ends_at: dt.datetime; status: EditionStatus (Literal['active','completed']); created_at: dt.datetime

From apps/api/repository/tournaments_repository.py (existing analogs):
- create_active_cycle (~443-478) — copy shape for edition create; the `now()` at line 469 is the bug — bind timestamps as params instead.
- fetch_config / update_config (~39-79) — allow-list SET builder (field names from a fixed dict, values as $n positional params; SQL-injection-safe).
- set_category_paused (~312-341), set_category_debug_cycle_seconds (~343-372) — replace with config-level global setters.
- create_pending_transition (~1340-1377) — add edition_id param + allow nullable cycle_id.
- All methods: `conn: Connection | None = None` keyword-only; `self._get_connection(conn)`; catch ForeignKeyViolationError/CheckConstraintViolationError with extract_constraint_name(e).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SDK — TournamentRolloverEvent, edition struct, global config structs</name>
  <read_first>
    - libs/sdk/src/genjishimada_sdk/tournaments.py (lines 170-240 lifecycle/response structs; 431-496 event structs; CycleStatus Literal definition)
    - libs/sdk/src/genjishimada_sdk/__init__.py (export list — add the new module symbols)
    - .planning/phases/12-overhaul-of-tournaments/12-RESEARCH.md (§"Code Examples": TournamentRolloverEvent + TournamentEditionResponse; §"State of the Art" — RETAIN per-category element structs)
    - .planning/phases/12-overhaul-of-tournaments/12-PATTERNS.md (the SDK § — collapse pair, reshape lifecycle structs to config-level)
  </read_first>
  <action>
    In `libs/sdk/src/genjishimada_sdk/tournaments.py`:
    Add `EditionStatus` Literal['active','completed'] mirroring the existing CycleStatus convention.
    Add `class TournamentEditionResponse(Struct)` with fields `id: int`, `started_at: dt.datetime`, `ends_at: dt.datetime`, `status: EditionStatus`, `created_at: dt.datetime` (D-05/D-08 — ends_at stored, closes frontend-spec §8).
    Add `class TournamentRolloverEvent(Struct)` with fields `edition_id: int`, `results: list[TournamentCycleCompletedEvent]`, `started: list[TournamentCycleStartedEvent]` (D-09/D-10/D-11). RETAIN TournamentCycleStartedEvent and TournamentCycleCompletedEvent unchanged (they are the per-category payload elements). The batch structs TournamentCyclesStartedEvent/TournamentCyclesCompletedEvent are superseded — mark with a deprecation docstring note but leave them importable to avoid breaking unrelated imports until Plan 03/05 remove references.
    Reshape the lifecycle structs to config-level (drop per-category `id` framing): add a global `TournamentConfigResponse` (or extend the existing config response struct) carrying `cadence`, `anchor_weekday`, `anchor_time`, `anchor_tz`, `transitions_paused`, `debug_cycle_seconds`, `blacklist_weeks` (D-02/D-03/D-07); add a `TournamentPauseRequest`-style PATCH request scoped to config (no category id) using `UNSET`/`UnsetType` for optional fields; add a config-scoped debug-cycle-length request. Keep `*Request`/`*Response`/`*Event` suffixes.
    Payload field names of TournamentRolloverEvent MUST match the migration 0024 jsonb_build_object keys (`edition_id`, `results`, `started`) byte-identically (Pitfall 5).
    Update `libs/sdk/src/genjishimada_sdk/__init__.py` to export the new symbols. After editing, run `just fix` so the workspace SDK reinstalls (MEMORY.md — stale imports otherwise).
  </action>
  <verify>
    <automated>just fix && uv run --directory apps/api python -c "from genjishimada_sdk.tournaments import TournamentRolloverEvent, TournamentEditionResponse; import msgspec; print(msgspec.convert({'edition_id':1,'results':[],'started':[]}, TournamentRolloverEvent))"</automated>
  </verify>
  <acceptance_criteria>
    - `class TournamentRolloverEvent` and `class TournamentEditionResponse` present in libs/sdk/src/genjishimada_sdk/tournaments.py and exported from __init__.
    - Behavior: `msgspec.convert({'edition_id':1,'results':[],'started':[]}, TournamentRolloverEvent)` succeeds (proves conditional-empty sections + key names).
    - TournamentCycleStartedEvent / TournamentCycleCompletedEvent still importable (retained as elements).
    - Global config response carries cadence/anchor_weekday/anchor_time/anchor_tz/transitions_paused/debug_cycle_seconds without per-category id.
    - `just fix` completes (no ModuleNotFoundError: genjishimada_sdk).
  </acceptance_criteria>
  <done>SDK exports the combined event + edition + config-level structs; msgspec round-trips an empty-sections rollover payload; per-category element structs retained.</done>
</task>

<task type="auto">
  <name>Task 2: Repository — edition CRUD + global config setters + outbox edition_id</name>
  <read_first>
    - apps/api/repository/tournaments_repository.py (create_active_cycle ~443-478 incl. the now() bug at 469; fetch_config/update_config ~39-79 allow-list SET builder; set_category_paused ~312-341; set_category_debug_cycle_seconds ~343-372; create_pending_transition ~1340-1377; fetch_unpublished_transitions ~1379-1408; mark_transition_published ~1410-1434; extract_constraint_name usage ~436-441)
    - apps/api/repository/exceptions.py (ForeignKeyViolationError, CheckConstraintViolationError signatures)
    - apps/api/migrations/0024_tournament_editions_overhaul.sql (edition table columns, config columns, pending_transitions new edition_id + nullable cycle_id)
    - .planning/phases/12-overhaul-of-tournaments/12-PATTERNS.md (the repository § — copy verbatim, swap SQL; the now() anti-pattern at line 469)
  </read_first>
  <action>
    In `apps/api/repository/tournaments_repository.py`:
    Add `create_edition(self, started_at, ends_at, status='active', *, conn=None)` — copy the `create_active_cycle` shape but `INSERT INTO tournaments.editions (started_at, ends_at, status) VALUES ($1, $2, $3) RETURNING *`; started_at/ends_at are bound parameters (grid values computed by the caller / DB fn) — NEVER `now()` (the bug at line 469 must NOT be copied here).
    Add `create_cycle_for_edition(self, edition_id, category_id, map_id, *, conn=None)` (or extend create_active_cycle to accept edition_id) — child cycle INSERT with `edition_id` set, `started_at` inherited from the edition (caller passes it) — no now().
    Add `fetch_active_edition(self, *, conn=None)` — `SELECT * FROM tournaments.editions WHERE status='active' ORDER BY started_at DESC LIMIT 1` (returns Record or None).
    Replace `set_category_paused`/`set_category_debug_cycle_seconds` with config-level setters `set_transitions_paused(self, paused, *, conn=None)` and `set_debug_cycle_seconds(self, seconds, *, conn=None)` built on the existing `update_config` allow-list SET builder (field names from a fixed dict, values via $n — never interpolate values; RESEARCH Security V12). Add `set_cadence` / `set_anchor` config setters reusing the same builder.
    Extend `create_pending_transition` to accept `edition_id` and allow `cycle_id=None` (Pattern 3 / A3) — bind both as params.
    `fetch_config` (SELECT *) already returns new columns automatically — no change. `fetch_leaderboard` unchanged (rewards stay per cycle_id, Pattern 4).
    Every new method: `conn: Connection | None = None` keyword-only, `self._get_connection(conn)`, catch ForeignKeyViolationError/CheckConstraintViolationError with extract_constraint_name(e) and re-raise repo exceptions.
  </action>
  <verify>
    <automated>uv run --directory apps/api pytest tests/repository/tournaments/test_tournaments_repository.py --no-testmon -p no:xdist</automated>
  </verify>
  <acceptance_criteria>
    - `tournaments_repository.py` contains `INSERT INTO tournaments.editions` and a `fetch_active_edition` method.
    - Source assertion: the new edition-create method binds started_at/ends_at as `$1`/`$2` params (grep shows no `now()` inside create_edition / create_cycle_for_edition).
    - Global setters `set_transitions_paused`, `set_debug_cycle_seconds` use the allow-list SET builder (no value interpolation).
    - `create_pending_transition` accepts edition_id and nullable cycle_id.
    - Repository test file passes (extend it with: create_edition round-trip, fetch_active_edition, global config setters, create_pending_transition with edition_id + null cycle_id).
  </acceptance_criteria>
  <done>Repository provides edition CRUD with parameter-bound grid timestamps, global config setters via the safe SET builder, and outbox writes carrying edition_id; tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| service → repo SET builders | config field updates must not allow SQL injection |
| jsonb payload ↔ SDK struct | key drift surfaces as a loud ValidationError |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-05 | Tampering | global config SET builders (set_transitions_paused/debug/cadence/anchor) | mitigate | Field names from a fixed allow-list dict; values bound as $n positional params (asyncpg) — never string-interpolated (reuse existing update_config pattern) |
| T-12-06 | Tampering | TournamentRolloverEvent ↔ migration payload key drift | mitigate | Struct field names byte-identical to jsonb_build_object keys; msgspec.convert test in Task 1 proves round-trip; a mismatch raises ValidationError and leaves the outbox row unpublished (fail-closed) |
| T-12-03 | Tampering | edition timestamp writes (drift bug) | mitigate | Edition-create repo methods bind started_at/ends_at as params; grep gate asserts no now() in those methods |
| T-12-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages (RESEARCH Package Legitimacy Audit: not applicable); `just fix` only reinstalls the workspace-local SDK |
</threat_model>

<verification>
- Targeted: `uv run --directory apps/api pytest tests/repository/tournaments/test_tournaments_repository.py --no-testmon -p no:xdist`
- SDK round-trip: `uv run --directory apps/api python -c "from genjishimada_sdk.tournaments import TournamentRolloverEvent; import msgspec; msgspec.convert({'edition_id':1,'results':[],'started':[]}, TournamentRolloverEvent)"`
- Lint: `just lint-sdk && just lint-api` (source is not lint-exempt)
- Wave merge: `uv run --directory apps/api pytest -n 4 --no-testmon`
</verification>

<success_criteria>
- TournamentRolloverEvent + TournamentEditionResponse + global config structs exported (D-05/D-08/D-09/D-10/D-11/D-02/D-03/D-07).
- Per-category element event structs retained for reuse inside the combined event.
- Repository edition CRUD binds grid timestamps as parameters (no now()); global config setters use the injection-safe allow-list builder; outbox supports edition_id + nullable cycle_id.
- Repository + SDK tests green; `just lint-sdk`/`just lint-api` clean.
</success_criteria>

<output>
Create `.planning/phases/12-overhaul-of-tournaments/12-02-SUMMARY.md` when done.
</output>
