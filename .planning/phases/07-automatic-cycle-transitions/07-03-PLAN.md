---
phase: 07-automatic-cycle-transitions
plan: 03
type: execute
wave: 3
depends_on: ["07-01", "07-02"]
files_modified:
  - apps/api/tests/repository/tournaments/test_cycle_transitions.py
  - apps/api/tests/repository/tournaments/test_select_eligible_map.py
  - apps/api/tests/repository/tournaments/test_outbox_poller.py
autonomous: true
requirements: [CYCLE-01]
must_haves:
  truths:
    - "A test invokes SELECT tournaments.process_cycle_transitions() directly (no cron) and asserts the full state machine"
    - "Tests prove only due cycles transition (weekly=7d, biweekly=14d boundary math)"
    - "A test proves concurrent invocations no-op via the advisory lock"
    - "A test proves the cycle_completed payload standings match fetch_leaderboard ranking and deserialize into TournamentCycleCompletedEvent"
    - "A test proves submit_completion raises CycleNotActiveError once status is finalizing (regression guard)"
    - "Tests prove the poller publishes + marks rows, SKIP LOCKED prevents double-publish, and a failed publish leaves the row unpublished"
    - "A parity test proves select_eligible_map respects difficulties + blacklist window + LRU fallback like fetch_eligible_maps"
    - "D-06: test_parity_with_python_eligible asserts the SQL select_eligible_map helper enforces the same eligibility set as the Phase 5 fetch_eligible_maps path (SQL/Python parity is the flagged risk this test closes)"
    - "D-07: test_missing_pending_cycle_edge and test_select_returns_null_when_no_maps prove the missing-pending / no-eligible-map edges complete the run via RAISE NOTICE skip rather than aborting"
  artifacts:
    - path: "apps/api/tests/repository/tournaments/test_cycle_transitions.py"
      provides: "transition state-machine, due-detection, advisory-lock, placement-snapshot, submission-rejection tests"
      contains: "process_cycle_transitions"
    - path: "apps/api/tests/repository/tournaments/test_select_eligible_map.py"
      provides: "SQL helper vs Python selection parity tests"
      contains: "select_eligible_map"
    - path: "apps/api/tests/repository/tournaments/test_outbox_poller.py"
      provides: "poller publish/mark, SKIP LOCKED, at-least-once tests"
      contains: "publish_pending_transitions"
  key_links:
    - from: "test_cycle_transitions.py"
      to: "tournaments.process_cycle_transitions"
      via: "SELECT invocation via asyncpg connection"
      pattern: "SELECT tournaments.process_cycle_transitions"
    - from: "test_outbox_poller.py"
      to: "publish_pending_transitions"
      via: "X-PYTEST-ENABLED=1 skip publish, assert published flag"
      pattern: "X-PYTEST-ENABLED"
---

<objective>
Prove the Phase 7 critical behaviors with integration tests that invoke the SQL transition function directly (pg_cron is absent in the test DB) and exercise the outbox poller with the test-mode publish skip.

Purpose: Closes all Wave 0 test gaps from 07-VALIDATION.md and proves CYCLE-01 success criteria 1, 2, 3, 4, 5. These are the assertions that make the migration (07-01) and the bridge (07-02) verifiable.
Output: three new test modules under the existing tournaments test tree (inheriting its fixtures).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-automatic-cycle-transitions/07-VALIDATION.md
@.planning/phases/07-automatic-cycle-transitions/07-RESEARCH.md
@.planning/phases/07-automatic-cycle-transitions/07-PATTERNS.md
@.planning/phases/07-automatic-cycle-transitions/07-01-SUMMARY.md
@.planning/phases/07-automatic-cycle-transitions/07-02-SUMMARY.md

<interfaces>
<!-- Reusable fixtures (apps/api/tests/repository/tournaments/conftest.py) — place new files under this tree to inherit them -->
create_test_category(**overrides) -> int   # defaults: difficulties=["Medium"], cycle_frequency="weekly", placement_xp/streak_xp "[]"
create_test_cycle(category_id, map_id, **overrides) -> int   # overrides: status (default "pending"), started_at, ended_at
create_test_tournament_completion(cycle_id, user_id, map_id, **overrides) -> int   # overrides: time, verified, completion
asyncpg_pool: asyncpg.Pool   # session pool with all migrations applied (0021 included)

<!-- Direct cron-function invocation (test DB lacks pg_cron) — analog test_store_integration.py §905-946 -->
# async with asyncpg_pool.acquire() as conn: await conn.execute("SELECT tournaments.process_cycle_transitions()")

<!-- Poller entry point (07-02) -->
# from services.tournament_outbox_service import publish_pending_transitions
# publish_message skips real publish when headers["X-PYTEST-ENABLED"] == "1"

<!-- Extended structs (07-01): TournamentCycleStartedEvent, TournamentCycleCompletedEvent -->
<!-- submit_completion raises CycleNotActiveError when cycle status != 'active' (tournament_service.py §498-499) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Transition state-machine, due-detection, advisory-lock, placement-snapshot, submission-rejection tests</name>
  <files>apps/api/tests/repository/tournaments/test_cycle_transitions.py</files>
  <read_first>
    - apps/api/tests/repository/tournaments/conftest.py (create_test_category / create_test_cycle / create_test_tournament_completion factory fixtures + asyncpg_pool)
    - apps/api/tests/integration/test_store_integration.py (§905-946 — direct SELECT fn() cron-function test pattern)
    - apps/api/services/tournament_service.py (§498-499 submit_completion CycleNotActiveError guard)
    - apps/api/repository/tournaments_repository.py (§889-911 fetch_leaderboard ranking the snapshot must match)
    - .planning/phases/07-automatic-cycle-transitions/07-VALIDATION.md (§41-49 critical behaviors table)
    - .planning/phases/07-automatic-cycle-transitions/07-RESEARCH.md (§597-603 critical behaviors; §518-524 test invocation)
  </read_first>
  <action>
    Create `test_cycle_transitions.py` (test files are exempt from lint/type rules per CLAUDE.md; no annotations needed). Write these tests, all invoking the function directly via `await conn.execute("SELECT tournaments.process_cycle_transitions()")`:

    - `test_detects_due_cycle`: create a weekly active cycle with `started_at` 8 days in the past and a second weekly active cycle started 1 day ago; run the function; assert ONLY the overdue cycle moved off `active` (now `completed`) and the recent one stays `active`. Add a biweekly case: a cycle started 8 days ago (within 14d) stays active; started 15 days ago transitions.
    - `test_transition_state_machine`: due active cycle with a `pending` cycle for the same category; run; assert the original is `completed` with `ended_at` set, AND the previously-`pending` cycle is now `active` with a fresh `started_at`, AND a NEW `pending` cycle exists for the category (pre-roll, D-06). Assert exactly two `pending_transitions` rows were written for the completed cycle's category (`cycle_completed` + `cycle_started`).
    - `test_completed_payload_standings`: seed 3 tournament completions for the due cycle with mixed verified/time so ranking is deterministic; run; fetch the `cycle_completed` row payload; assert the `standings` array ranks fully-verified above partial and fastest-first within tier (matches the `RANK() OVER (ORDER BY verified DESC, time ASC)` semantics), and assert `msgspec.convert(payload, TournamentCycleCompletedEvent)` succeeds (round-trip, Pitfall 5). Also assert the `cycle_started` payload `msgspec.convert(payload, TournamentCycleStartedEvent)` succeeds and carries `map_code`/`map_name`/`started_at`/`ends_at`.
    - `test_advisory_lock_concurrency`: open two separate connections; in connection A `BEGIN` and `SELECT pg_advisory_xact_lock(2025070100)` (hold the lock), then call `SELECT tournaments.process_cycle_transitions()` on connection B — assert it no-ops (the due cycle is untouched because B's `pg_try_advisory_xact_lock` fails). Commit/rollback A; run B again and assert the transition now completes. (Adjust to the exact lock constant chosen in 07-01 if different from 2025070100 — read 07-01-SUMMARY.)
    - `test_submission_rejected_during_finalizing`: regression guard — set a cycle to `finalizing`, call the submission path (or the service `submit_completion`), assert `CycleNotActiveError` is raised (confirms success criterion 2 needs no new code).
    - `test_missing_pending_cycle_edge`: due active cycle with NO pending cycle for the category; run; assert the function does not error and (per D-07) either inline-selects an active cycle or leaves the category without an active cycle while the run still completes other categories — assert no exception and the due cycle is `completed`.

    Use `dt.datetime.now(dt.UTC) - dt.timedelta(days=...)` for past `started_at` values via `create_test_cycle(..., status="active", started_at=<past>)`. Decode jsonb payloads (already dicts via codec) directly; import the structs from `genjishimada_sdk.tournaments`.
  </action>
  <verify>
    <automated>uv run --directory apps/api pytest tests/repository/tournaments/test_cycle_transitions.py -p no:xdist -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run --directory apps/api pytest tests/repository/tournaments/test_cycle_transitions.py -p no:xdist` exits 0
    - File contains `SELECT tournaments.process_cycle_transitions` and `msgspec.convert`
    - File contains `pg_advisory_xact_lock` (concurrency test) and `CycleNotActiveError` (rejection regression)
    - At least the six named tests are present and pass
  </acceptance_criteria>
  <done>The transition state machine, due-detection, advisory-lock no-op, placement snapshot round-trip, submission rejection, and missing-pending edge are all proven.</done>
</task>

<task type="auto">
  <name>Task 2: select_eligible_map SQL/Python parity tests</name>
  <files>apps/api/tests/repository/tournaments/test_select_eligible_map.py</files>
  <read_first>
    - apps/api/tests/repository/tournaments/conftest.py (fixtures + asyncpg_pool)
    - apps/api/repository/tournaments_repository.py (§548-571 fetch_eligible_maps; §593-604 fetch_least_recently_used_map — the Python the SQL helper must match)
    - .planning/phases/07-automatic-cycle-transitions/07-RESEARCH.md (§322-342 parity risk + recommendation; §595 parity test row)
    - .planning/phases/07-automatic-cycle-transitions/07-PATTERNS.md (§65-75 eligible-query core to mirror)
  </read_first>
  <action>
    Create `test_select_eligible_map.py`. Tests invoke the SQL helper via `await conn.fetchval("SELECT tournaments.select_eligible_map($1)", category_id)`:

    - `test_select_respects_difficulties`: create a category with a specific difficulty grouping and maps inside/outside that grouping (including the `-`/`+` suffix normalization case, e.g. `Hard +` should normalize to `Hard`); assert the helper only ever returns map ids whose base difficulty is in the category's `difficulties`.
    - `test_select_excludes_blacklist_window`: create a map used in a recent cycle (within the configured blacklist weeks) and an eligible one outside the window; assert the in-window map is never returned.
    - `test_select_excludes_pending`: create a map already attached to a `pending` cycle; assert it is excluded.
    - `test_select_lru_fallback`: arrange so the eligible pool is empty (all candidate maps within the window) and assert the helper returns the least-recently-used map (lowest/NULL `started_at`) rather than NULL.
    - `test_select_returns_null_when_no_maps`: category whose difficulties match zero maps; assert the helper returns NULL (drives D-07 skip-pre-roll behavior).
    - `test_parity_with_python_eligible`: for a fixed seed of maps/cycles, call `TournamentRepository.fetch_eligible_maps(...)` and assert the SQL helper's returned id is within that eligible set (eligibility-set parity, since both use `ORDER BY random()` the exact pick differs but membership must match).
  </action>
  <verify>
    <automated>uv run --directory apps/api pytest tests/repository/tournaments/test_select_eligible_map.py -p no:xdist -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run --directory apps/api pytest tests/repository/tournaments/test_select_eligible_map.py -p no:xdist` exits 0
    - File contains `select_eligible_map` and `fetch_eligible_maps`
    - Blacklist-window, difficulty, pending-exclusion, LRU-fallback, NULL, and parity tests present and passing
  </acceptance_criteria>
  <done>The SQL selection helper is proven to enforce the same eligibility rules as the Phase 5 Python path, including LRU fallback and NULL exhaustion.</done>
</task>

<task type="auto">
  <name>Task 3: Outbox poller publish/mark, SKIP LOCKED, at-least-once tests</name>
  <files>apps/api/tests/repository/tournaments/test_outbox_poller.py</files>
  <read_first>
    - apps/api/tests/repository/tournaments/conftest.py (fixtures + asyncpg_pool)
    - apps/api/services/tournament_outbox_service.py (publish_pending_transitions, _build_event — from 07-02)
    - apps/api/services/base.py (§80-82 X-PYTEST-ENABLED publish skip; §56-76 idempotency requirement)
    - apps/api/repository/tournaments_repository.py (§947-1006 create_pending_transition + hardened fetch_unpublished_transitions)
    - .planning/phases/07-automatic-cycle-transitions/07-RESEARCH.md (§403-407 Pitfall 2; §296 publish-then-mark; §592-594 poller test rows)
    - .planning/phases/07-automatic-cycle-transitions/07-PATTERNS.md (§213-224 poller test analog — X-PYTEST-ENABLED skip)
  </read_first>
  <action>
    Create `test_outbox_poller.py`. Seed `tournaments.pending_transitions` rows directly (via `create_pending_transition` or raw INSERT with a valid `cycle_id` from a fixture cycle) with valid `cycle_started`/`cycle_completed` payloads matching the extended struct fields. Build the app `State` (or a minimal object exposing `db_pool` = `asyncpg_pool`) the poller needs; ensure publishing is skipped by arranging the `Headers` used inside `publish_pending_transitions` to carry `X-PYTEST-ENABLED=1` (if the poller hardcodes empty headers for production, add a test seam: either parametrize the headers or set the test-mode via the documented mechanism the 07-02 service exposes — read 07-02-SUMMARY for how the service decides test mode, and assert against that). Tests:

    - `test_poller_publishes_and_marks`: seed 2 unpublished rows; run `publish_pending_transitions(state)`; assert both rows now have `published = TRUE` and that `msgspec.convert` succeeded for each (no ValidationError) — i.e. the loop completed.
    - `test_skip_locked_no_double_publish`: open connection A, `BEGIN`, `SELECT ... FROM tournaments.pending_transitions WHERE published=FALSE FOR UPDATE SKIP LOCKED` (lock the rows); while A holds them, run `publish_pending_transitions` on the pool — assert it processes ZERO rows (they are skip-locked) and they remain `published=FALSE`. Roll back A; run again and assert they publish.
    - `test_publish_failure_leaves_unpublished`: force the publish step to raise (e.g. seed a payload that fails `msgspec.convert`, such as a `cycle_completed` payload missing the required `standings` key); run the poller and assert it raises/propagates per-row failure AND the offending row remains `published=FALSE` (publish-then-mark / at-least-once — the mark never ran). Confirm no row was marked published in that batch.
    - `test_invalid_event_type_rejected` (optional but cheap): seed a payload whose `event_type` is unknown to `_build_event`; assert the poller does not mark it published.

    Test files are lint-exempt; no annotations required.
  </action>
  <verify>
    <automated>uv run --directory apps/api pytest tests/repository/tournaments/test_outbox_poller.py -p no:xdist -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run --directory apps/api pytest tests/repository/tournaments/test_outbox_poller.py -p no:xdist` exits 0
    - File contains `publish_pending_transitions`, `X-PYTEST-ENABLED`, and `FOR UPDATE SKIP LOCKED`
    - publish/mark, skip-locked-no-double-publish, and publish-failure-leaves-unpublished tests present and passing
  </acceptance_criteria>
  <done>The outbox poller is proven to publish-and-mark, to skip rows locked by another transaction, and to leave rows unpublished when publish fails.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → process_cycle_transitions() | Tests invoke the elevated DB function directly (pg_cron absent) — they assert the safety properties (atomicity, lock) hold |
| seeded payload → poller msgspec.convert | Tests inject a malformed payload to prove validation rejects it before publish |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-09 | Integrity | advisory-lock double-transition (the safety property under test) | mitigate | `test_advisory_lock_concurrency` asserts a held lock makes the function no-op (no double champion / double payload) |
| T-07-10 | Repudiation/Integrity | double-publish under concurrent pollers | mitigate | `test_skip_locked_no_double_publish` asserts SKIP LOCKED prevents a second poller from re-publishing locked rows |
| T-07-11 | Validation | poisoned payload shipped as an event | mitigate | `test_publish_failure_leaves_unpublished` asserts a malformed payload fails `msgspec.convert` and is NOT marked published (stays for inspection) |
| T-07-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this phase (zero external packages per RESEARCH audit); no checkpoint needed |
</threat_model>

<verification>
- Each new test module exits 0 under `-p no:xdist`.
- After this wave, run `just test-api` (full suite, 8 workers) — must be green before `/gsd:verify-work`.
- Pre-existing unrelated failures (e.g. `test_difficulty_exact_filter`, the `TestCheckActiveCycleForCategory` deferred bug) are not introduced or worsened by this phase.
- Manual-only (per 07-VALIDATION): pg_cron actually firing on schedule and lifespan poller clean shutdown are verified on the dev VPS, not in CI.
</verification>

<success_criteria>
- All three new test modules pass under `-p no:xdist`.
- `just test-api` is green.
- Every CYCLE-01 critical behavior from 07-VALIDATION.md has an automated assertion.
</success_criteria>

<output>
Create `.planning/phases/07-automatic-cycle-transitions/07-03-SUMMARY.md` when done
</output>
</content>
