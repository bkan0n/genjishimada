---
context: phase
phase: 08-rewards-engine
task: 0
total_tasks: 0
status: discuss_complete_plan_not_started
last_updated: 2026-05-30
---

# Continue Here — Phase 8: Rewards Engine

**Stage:** discuss-phase ✅ complete · plan-phase ⏳ not yet run

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Tool-result batching/latency | This session, Read/Bash results returned empty in-turn and flushed in large duplicated batches many turns later; a stuck Read offset (defaulting to 1102) also recurred. Burned context re-dumping the 34k-token plan-phase workflow. | advisory | Resume in a **fresh session**. Prefer `--chunked` planning and small, targeted reads/greps over full-file reads. |
| Trusting DISCUSSION-LOG over CONTEXT.md | The DISCUSSION-LOG says streak thresholds live in a `tournaments.xp_config` table — that was the pre-verification guess and is **wrong**. | advisory | Treat `08-CONTEXT.md` as authoritative; config lives in `tournaments.categories.placement_xp`/`streak_xp` jsonb. |

<current_state>
Phase 8 discuss-phase finished in `--auto` mode. `08-CONTEXT.md` and `08-DISCUSSION-LOG.md`
are written; STATE.md session recorded; chain flag was set. Auto-advance into plan-phase was
started but deliberately halted (tool latency exhausted context before the planner spawned).
No plan-phase work done — nothing half-written. CONTEXT.md is ready for researcher/planner.
</current_state>

<completed_work>
- `08-CONTEXT.md` — authoritative decisions doc, all 6 gray areas auto-decided + corrected
  against verified source.
- `08-DISCUSSION-LOG.md` — audit trail (its "xp_config" line is superseded by CONTEXT.md).
- STATE.md session recorded. Doc commits are no-ops (`commit_docs: false`, `.planning/` gitignored).
</completed_work>

<remaining_work>
- Run plan-phase for Phase 8 (research → plan → verify). Not started.
</remaining_work>

<decisions_made>
- Participation XP (RWD-01): granted in `TournamentService` submit path on first cycle completion; once-per-cycle via DB-state guard.
- Placement XP (RWD-02): computed in Python at cycle finalization from the Phase-7 `cycle_completed` outbox payload → `categories.placement_xp` tiers.
- Streaks (RWD-04): updated at cycle-end; ≥1 category submission increments, miss resets to 0; stored in `tournaments.streaks`.
- Streak bonus (RWD-05): thresholds from `categories.streak_xp`; granted same finalization step.
- Double-grant safety: `api.xp.grant` is non-idempotent → DB-recorded award state + deterministic `tournament:{reason}:{cycle_id}:{user_id}` keys.
- Payload: reuse existing `api.xp.grant` + `BaseService.publish_message()`.
</decisions_made>

<blockers>
- None blocking. Open research questions captured in `08-CONTEXT.md` (top one: does the bot
  XP consumer on `api.xp.grant` accept `TournamentXpGrantEvent`, or must rewards emit the
  generic `xp.XpGrantEvent`?).
</blockers>

## Required Reading (in order)
1. `.planning/phases/08-rewards-engine/08-CONTEXT.md` — authoritative locked decisions + open questions.
2. `apps/api/migrations/0020_tournaments.sql` / `0021_tournament_cycle_transitions.sql` — schema + Phase-7 outbox.
3. `apps/api/services/lootbox_service.py:377-412` — reference: build XpGrantEvent + publish to `api.xp.grant`.
4. `libs/sdk/src/genjishimada_sdk/tournaments.py` (reward structs) + `.../xp.py` (generic XpGrant).

## Infrastructure State
- No running services started this session. No background processes. Nothing to tear down.

<context>
discuss-phase produced clean, source-verified context. The only reason plan-phase didn't run
is the environment's tool-result latency, not any planning blocker. A fresh session should
sail straight into planning.
</context>

<next_action>
Start a fresh session, then run: `/gsd:plan-phase 8 --auto` (or `--auto --chunked` given the
latency seen this session). CONTEXT.md is ready — no re-discussion needed.
</next_action>
