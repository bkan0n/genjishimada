---
phase: 05-map-selection-blacklist
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - apps/api/services/tournament_service.py
  - apps/api/routes/v3/tournaments.py
autonomous: true
requirements: [CYCLE-04, CYCLE-05, CYCLE-06, CYCLE-07]

must_haves:
  truths:
    - "select_map creates a pending cycle with a randomly selected eligible map"
    - "select_map returns 409 if a pending cycle already exists for the category"
    - "select_map falls back to LRU when eligible pool is exhausted and raises NoEligibleMapsError when LRU also returns None"
    - "get_next_cycle returns pending cycle details with map info"
    - "reroll_map deletes existing pending cycle and creates new one with a different map"
    - "choose_map validates map exists, matches category difficulties, and creates/replaces pending cycle"
    - "All mutation endpoints require tournaments:write scope, preview requires tournaments:read"
  artifacts:
    - path: "apps/api/services/tournament_service.py"
      provides: "select_map, get_next_cycle, reroll_map, choose_map methods"
      contains: "select_map"
    - path: "apps/api/routes/v3/tournaments.py"
      provides: "4 new endpoint handlers for map selection"
      contains: "select-map"
  key_links:
    - from: "apps/api/routes/v3/tournaments.py"
      to: "apps/api/services/tournament_service.py"
      via: "DI injection of TournamentService"
      pattern: "tournament_service\\.select_map"
    - from: "apps/api/services/tournament_service.py"
      to: "apps/api/repository/tournaments_repository.py"
      via: "self._tournament_repo method calls"
      pattern: "self\\._tournament_repo\\.fetch_pending_cycle"
---

<objective>
Implement service business logic and controller endpoints for map selection, preview, reroll, and explicit map choice.

Purpose: Wire the repository and SDK types from Plan 01 into the service layer (business logic with transaction safety) and controller layer (HTTP endpoints with exception-to-status-code mapping), completing the full request path for all four CYCLE requirements.
Output: 4 new service methods on TournamentService, 4 new endpoint handlers on TournamentsController.
</objective>

<execution_context>
@/Users/nebula/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nebula/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-map-selection-blacklist/05-CONTEXT.md
@.planning/phases/05-map-selection-blacklist/05-RESEARCH.md
@.planning/phases/05-map-selection-blacklist/05-PATTERNS.md
@.planning/phases/05-map-selection-blacklist/05-01-SUMMARY.md

<interfaces>
<!-- Executor MUST read Plan 01 SUMMARY for actual artifacts created -->

From libs/sdk/src/genjishimada_sdk/tournaments.py (created in Plan 01):
- TournamentNextCycleResponse(Struct): id, category_id, map_id, map_code, map_name, map_difficulty, status, created_at
- TournamentChooseMapRequest(Struct): map_code (str)
- TournamentCycleResponse(Struct): id, category_id, map_id, status, started_at, ended_at, created_at (existing)

From apps/api/services/exceptions/tournaments.py (created in Plan 01):
- NoEligibleMapsError(TournamentsError): __init__(category_id: int)
- PendingCycleAlreadyExistsError(TournamentsError): __init__(category_id: int)
- PendingCycleNotFoundError(TournamentsError): __init__(category_id: int)
- CategoryNotFoundError(TournamentsError): __init__(category_id: int) (existing)
- MapNotEligibleError(TournamentsError): __init__(map_id: int, reason: str = "") (existing)

From apps/api/repository/tournaments_repository.py (created/modified in Plan 01):
- fetch_pending_cycle(category_id, *, conn) -> dict | None (with map JOIN)
- delete_cycle(cycle_id, *, conn) -> bool
- fetch_map_by_code(map_code, *, conn) -> dict | None
- fetch_eligible_maps(difficulties, blacklist_weeks, *, exclude_map_ids, conn) -> list[dict]
- fetch_least_recently_used_map(difficulties, *, conn) -> dict | None (existing)
- create_cycle(category_id, map_id, *, conn) -> dict (existing)
- fetch_config(*, conn) -> dict (existing)
- fetch_category(category_id, *, conn) -> dict | None (existing)

From apps/api/services/tournament_service.py (existing):
- TournamentService(BaseService) with self._tournament_repo, self._pool
- update_category pattern: async with self._pool.acquire() as conn: ... conn=conn, # type: ignore[arg-type]
- get_category pattern: fetch -> raise if None -> msgspec.convert

From apps/api/routes/v3/tournaments.py (existing):
- TournamentsController at path="/tournaments"
- dependencies dict already wired with tournament_repo and tournament_service
- get_category handler: try/except CategoryNotFoundError -> CustomHTTPException(404)
- create_category handler: try/except -> CustomHTTPException(409)
- HTTP status imports: HTTP_200_OK, HTTP_201_CREATED, HTTP_204_NO_CONTENT, HTTP_404_NOT_FOUND, HTTP_409_CONFLICT
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 4 service methods to TournamentService</name>
  <files>
    apps/api/services/tournament_service.py
  </files>
  <read_first>
    apps/api/services/tournament_service.py,
    apps/api/repository/tournaments_repository.py,
    apps/api/services/exceptions/tournaments.py,
    libs/sdk/src/genjishimada_sdk/tournaments.py
  </read_first>
  <action>
    Add `from logging import getLogger` import and `log = getLogger(__name__)` at module level (after existing imports, before the class definition).

    Add the following SDK imports to the existing import block: `TournamentNextCycleResponse`, `TournamentChooseMapRequest`.

    Add the following exception imports to the existing import block: `NoEligibleMapsError`, `PendingCycleAlreadyExistsError`, `PendingCycleNotFoundError`, `MapNotEligibleError`.

    Add 4 new methods to `TournamentService` after the existing `delete_category` method:

    **1. `async def select_map(self, category_id: int) -> TournamentNextCycleResponse`** (per D-03, D-10, D-11)
    - Acquires connection with `async with self._pool.acquire() as conn:`
    - Inside transaction block, ALL repo calls use `conn=conn,  # type: ignore[arg-type]`
    - Step 1: `fetch_pending_cycle(category_id)` -- if not None, raise `PendingCycleAlreadyExistsError(category_id)`
    - Step 2: `fetch_config()` to get `blacklist_weeks`
    - Step 3: `fetch_category(category_id)` -- if None, raise `CategoryNotFoundError(category_id)`
    - Step 4: `fetch_eligible_maps(category["difficulties"], config["blacklist_weeks"])` (per D-03)
    - Step 5: If eligible non-empty, `selected = eligible[0]`. If empty, `log.warning("[!] Eligible map pool exhausted for category %s, using LRU fallback", category_id)` then call `fetch_least_recently_used_map(category["difficulties"])`. If LRU also None, raise `NoEligibleMapsError(category_id)` (per D-08)
    - Step 6: `create_cycle(category_id, selected["id"])` to create pending cycle
    - Step 7: `fetch_pending_cycle(category_id)` to get the full record with map JOIN
    - Return `msgspec.convert(result, TournamentNextCycleResponse)`
    - Google-style docstring with Args, Returns, Raises sections

    **2. `async def get_next_cycle(self, category_id: int) -> TournamentNextCycleResponse`** (per D-10)
    - Follow `get_category` pattern: fetch pending cycle, raise if None, convert
    - First check category exists: `fetch_category(category_id)` -- if None, raise `CategoryNotFoundError(category_id)` (pitfall 3: distinguish category-not-found from no-pending-cycle)
    - Then `fetch_pending_cycle(category_id)` -- if None, raise `PendingCycleNotFoundError(category_id)`
    - Return `msgspec.convert(row, TournamentNextCycleResponse)`

    **3. `async def reroll_map(self, category_id: int) -> TournamentNextCycleResponse`** (per D-07, D-10)
    - Acquires connection with `async with self._pool.acquire() as conn:`
    - ALL repo calls use `conn=conn,  # type: ignore[arg-type]`
    - Step 1: `fetch_category(category_id)` -- if None, raise `CategoryNotFoundError(category_id)`
    - Step 2: `fetch_pending_cycle(category_id)` -- if None, raise `PendingCycleNotFoundError(category_id)`
    - Step 3: Store `old_map_id = existing["map_id"]` and `old_cycle_id = existing["id"]`
    - Step 4: `delete_cycle(old_cycle_id)` to remove old pending cycle
    - Step 5: `fetch_config()` to get blacklist_weeks
    - Step 6: `fetch_eligible_maps(category["difficulties"], config["blacklist_weeks"], exclude_map_ids=[old_map_id])` (per D-07: exclude the just-deleted map)
    - Step 7: If eligible non-empty, take first. If empty, LRU fallback (same as select_map). If LRU None, raise `NoEligibleMapsError(category_id)`
    - Step 8: `create_cycle(category_id, selected["id"])` then `fetch_pending_cycle(category_id)`
    - Return `msgspec.convert(result, TournamentNextCycleResponse)`

    **4. `async def choose_map(self, category_id: int, data: TournamentChooseMapRequest) -> TournamentNextCycleResponse`** (per D-10)
    - Acquires connection with `async with self._pool.acquire() as conn:`
    - ALL repo calls use `conn=conn,  # type: ignore[arg-type]`
    - Step 1: `fetch_category(category_id)` -- if None, raise `CategoryNotFoundError(category_id)`
    - Step 2: `fetch_map_by_code(data.map_code)` -- if None, raise `MapNotEligibleError(0, reason=f"Map with code '{data.map_code}' not found.")`
    - Step 3: Validate map difficulty matches category: `regexp_replace` the map["difficulty"] to strip trailing `+`/`-` modifiers (use Python: `re.sub(r'\s*[-+]\s*$', '', map_row["difficulty"])`) and check it's in `category["difficulties"]`. If not, raise `MapNotEligibleError(map_row["id"], reason=f"Map difficulty '{map_row['difficulty']}' does not match category difficulties.")`
    - Step 4: Check for existing pending cycle: `fetch_pending_cycle(category_id)`. If exists, `delete_cycle(existing["id"])` to replace it
    - Step 5: `create_cycle(category_id, map_row["id"])` then `fetch_pending_cycle(category_id)`
    - Return `msgspec.convert(result, TournamentNextCycleResponse)`
    - Add `import re` at top of file for the difficulty strip

    Pitfall 4 enforcement: verify every single `self._tournament_repo.*` call inside an `async with self._pool.acquire() as conn:` block passes `conn=conn,  # type: ignore[arg-type]`.
  </action>
  <verify>
    <automated>cd /Users/nebula/Documents/coding/parkour/genji/genjishimada && uv run --directory apps/api ruff check services/tournament_service.py</automated>
  </verify>
  <acceptance_criteria>
    - `log = getLogger(__name__)` exists at module level
    - `select_map` method acquires connection, checks no pending exists, fetches config/category, selects map with fallback, creates pending cycle, returns TournamentNextCycleResponse
    - `get_next_cycle` method checks category exists (raises CategoryNotFoundError), checks pending exists (raises PendingCycleNotFoundError), returns TournamentNextCycleResponse
    - `reroll_map` method acquires connection, validates category/pending exist, deletes old, selects new excluding old_map_id, returns TournamentNextCycleResponse
    - `choose_map` method acquires connection, validates category, validates map exists and difficulty matches, replaces any existing pending, returns TournamentNextCycleResponse
    - Every repo call inside `async with self._pool.acquire() as conn:` block has `conn=conn,  # type: ignore[arg-type]`
    - Log warning uses `%s` formatting: `log.warning("[!] Eligible map pool exhausted for category %s, using LRU fallback", category_id)`
    - ruff check passes
  </acceptance_criteria>
  <done>TournamentService has select_map, get_next_cycle, reroll_map, choose_map methods with transaction safety and LRU fallback. ruff passes.</done>
</task>

<task type="auto">
  <name>Task 2: Add 4 controller endpoints to TournamentsController</name>
  <files>
    apps/api/routes/v3/tournaments.py
  </files>
  <read_first>
    apps/api/routes/v3/tournaments.py,
    apps/api/services/tournament_service.py
  </read_first>
  <action>
    Add new imports to `routes/v3/tournaments.py`:
    - SDK: `TournamentNextCycleResponse`, `TournamentChooseMapRequest` from `genjishimada_sdk.tournaments`
    - Exceptions: `NoEligibleMapsError`, `PendingCycleAlreadyExistsError`, `PendingCycleNotFoundError` from `services.exceptions.tournaments`
    - Status code: `HTTP_422_UNPROCESSABLE_ENTITY` from `litestar.status_codes`

    Add 4 new endpoint handlers to `TournamentsController` after the existing `delete_category` method:

    **1. `get_next_cycle`** (per D-05, D-06)
    - `@litestar.get(path="/categories/{category_id:int}/next-cycle", summary="Preview Next Cycle", description="Preview the pending next cycle for a category.", opt={"required_scopes": {"tournaments:read"}})`
    - Parameters: `tournament_service: TournamentService`, `category_id: Annotated[int, Parameter(description="Category ID")]`
    - Return type: `TournamentNextCycleResponse`
    - try/except: `CategoryNotFoundError` -> 404, `PendingCycleNotFoundError` -> 404

    **2. `select_map`** (per D-05, D-06)
    - `@litestar.post(path="/categories/{category_id:int}/select-map", summary="Select Map", description="Trigger random map selection for next cycle.", status_code=HTTP_201_CREATED, opt={"required_scopes": {"tournaments:write"}})`
    - Parameters: `tournament_service: TournamentService`, `category_id: Annotated[int, Parameter(description="Category ID")]`
    - Return type: `TournamentNextCycleResponse`
    - try/except: `CategoryNotFoundError` -> 404, `PendingCycleAlreadyExistsError` -> 409, `NoEligibleMapsError` -> 422

    **3. `reroll_map`** (per D-05, D-06)
    - `@litestar.post(path="/categories/{category_id:int}/reroll", summary="Reroll Map", description="Delete current pending cycle and select a new map.", status_code=HTTP_201_CREATED, opt={"required_scopes": {"tournaments:write"}})`
    - Parameters: `tournament_service: TournamentService`, `category_id: Annotated[int, Parameter(description="Category ID")]`
    - Return type: `TournamentNextCycleResponse`
    - try/except: `CategoryNotFoundError` -> 404, `PendingCycleNotFoundError` -> 404, `NoEligibleMapsError` -> 422

    **4. `choose_map`** (per D-05, D-06)
    - `@litestar.patch(path="/categories/{category_id:int}/next-cycle", summary="Choose Map", description="Explicitly set the map for next cycle.", status_code=HTTP_200_OK, opt={"required_scopes": {"tournaments:write"}})`
    - Parameters: `tournament_service: TournamentService`, `category_id: Annotated[int, Parameter(description="Category ID")]`, `data: Annotated[TournamentChooseMapRequest, Body(title="Choose Map")]`
    - Return type: `TournamentNextCycleResponse`
    - try/except: `CategoryNotFoundError` -> 404, `MapNotEligibleError` -> 422

    Add `MapNotEligibleError` to the exceptions import block if not already imported.

    All handlers follow the existing controller pattern: Google-style docstring with Args/Returns/Raises, `CustomHTTPException` wrapping with `from e`.
  </action>
  <verify>
    <automated>cd /Users/nebula/Documents/coding/parkour/genji/genjishimada && uv run --directory apps/api ruff check routes/v3/tournaments.py</automated>
  </verify>
  <acceptance_criteria>
    - GET `/categories/{category_id:int}/next-cycle` endpoint exists with `tournaments:read` scope
    - POST `/categories/{category_id:int}/select-map` endpoint exists with `tournaments:write` scope and `HTTP_201_CREATED`
    - POST `/categories/{category_id:int}/reroll` endpoint exists with `tournaments:write` scope and `HTTP_201_CREATED`
    - PATCH `/categories/{category_id:int}/next-cycle` endpoint exists with `tournaments:write` scope and `HTTP_200_OK`
    - `NoEligibleMapsError` maps to `HTTP_422_UNPROCESSABLE_ENTITY` (per D-09)
    - `PendingCycleAlreadyExistsError` maps to `HTTP_409_CONFLICT`
    - `PendingCycleNotFoundError` maps to `HTTP_404_NOT_FOUND`
    - `CategoryNotFoundError` maps to `HTTP_404_NOT_FOUND`
    - `MapNotEligibleError` maps to `HTTP_422_UNPROCESSABLE_ENTITY`
    - All handlers use `from e` exception chaining
    - ruff check passes
  </acceptance_criteria>
  <done>TournamentsController has 4 new endpoints for map selection preview, select, reroll, and choose. All scope guards, exception mappings, and return types correct. ruff passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP client -> TournamentsController | Untrusted input: category_id path param, map_code in request body |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | Elevation of Privilege | All mutation endpoints | mitigate | Scope guard requires `tournaments:write` on POST/PATCH endpoints (per D-06) |
| T-05-04 | Elevation of Privilege | Preview endpoint | mitigate | Scope guard requires `tournaments:read` on GET endpoint (per D-06) |
| T-05-05 | Tampering | TOCTOU on select_map | mitigate | Single-connection transaction boundary: check pending + create on same conn (per D-11) |
| T-05-06 | Tampering | map_code in choose_map | mitigate | Validated via parameterized SQL query in fetch_map_by_code ($1), difficulty checked against category |
| T-05-07 | Spoofing | category_id path param | mitigate | Integer type enforced by Litestar path parser `{category_id:int}`, validated against DB |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
- `uv run --directory apps/api ruff check services/tournament_service.py routes/v3/tournaments.py` passes
- `just lint-api` passes (full API lint)
- API starts without import errors: `cd apps/api && uv run litestar routes` lists the 4 new tournament endpoints
</verification>

<success_criteria>
- 4 new service methods with transaction safety and correct exception handling
- 4 new controller endpoints with proper scope guards and HTTP status codes
- All mutation endpoints behind tournaments:write, preview behind tournaments:read
- select_map and reroll_map have LRU fallback with warning log
- choose_map validates map existence and difficulty match
- ruff check passes for both files
</success_criteria>

<output>
Create `.planning/phases/05-map-selection-blacklist/05-02-SUMMARY.md` when done
</output>
