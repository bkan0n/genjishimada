---
phase: 05-map-selection-blacklist
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - libs/sdk/src/genjishimada_sdk/tournaments.py
  - apps/api/services/exceptions/tournaments.py
  - apps/api/services/exceptions/__init__.py
  - apps/api/repository/tournaments_repository.py
autonomous: true
requirements: [CYCLE-04, CYCLE-05, CYCLE-06, CYCLE-07]

must_haves:
  truths:
    - "SDK defines TournamentNextCycleResponse with map detail fields for admin preview"
    - "SDK defines TournamentChooseMapRequest with map_code field"
    - "NoEligibleMapsError, PendingCycleAlreadyExistsError, and PendingCycleNotFoundError exceptions exist"
    - "Repository can fetch a pending cycle with map details via JOIN"
    - "Repository can delete a cycle record by ID"
    - "Repository can fetch a map by workshop code from core.maps"
    - "Eligible maps query excludes maps in pending cycles"
    - "Eligible maps query supports exclude_map_ids parameter for reroll"
  artifacts:
    - path: "libs/sdk/src/genjishimada_sdk/tournaments.py"
      provides: "TournamentNextCycleResponse, TournamentChooseMapRequest structs"
      contains: "TournamentNextCycleResponse"
    - path: "apps/api/services/exceptions/tournaments.py"
      provides: "NoEligibleMapsError, PendingCycleAlreadyExistsError, PendingCycleNotFoundError"
      contains: "NoEligibleMapsError"
    - path: "apps/api/repository/tournaments_repository.py"
      provides: "fetch_pending_cycle, delete_cycle, fetch_map_by_code methods"
      contains: "fetch_pending_cycle"
  key_links:
    - from: "apps/api/services/exceptions/__init__.py"
      to: "apps/api/services/exceptions/tournaments.py"
      via: "barrel import"
      pattern: "NoEligibleMapsError"
---

<objective>
Add SDK structs, domain exceptions, and repository methods needed for map selection.

Purpose: Establish the foundational types, error classes, and data access methods that the service and controller layers (Plan 02) will consume. This separates contract definition from business logic.
Output: Extended SDK module, exception hierarchy, and repository with 3 new methods + 1 modified query.
</objective>

<execution_context>
@/Users/nebula/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nebula/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-map-selection-blacklist/05-CONTEXT.md
@.planning/phases/05-map-selection-blacklist/05-RESEARCH.md
@.planning/phases/05-map-selection-blacklist/05-PATTERNS.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From libs/sdk/src/genjishimada_sdk/tournaments.py:
- CycleStatus = Literal["pending", "active", "finalizing", "completed"]
- TournamentCycleResponse(Struct): id, category_id, map_id, status, started_at, ended_at, created_at
- __all__ tuple at lines 8-28 must be updated with new type names

From apps/api/services/exceptions/tournaments.py:
- TournamentsError(DomainError) is the base class
- CategoryNotFoundError pattern: __init__(self, category_id: int) -> super().__init__(msg, category_id=category_id)
- MapNotEligibleError pattern: __init__(self, map_id: int, reason: str = "")

From apps/api/repository/tournaments_repository.py:
- fetch_active_cycle(category_id, *, conn) -> dict | None -- pattern for fetch_pending_cycle
- delete_category(category_id, *, conn) -> bool -- pattern for delete_cycle
- fetch_category(category_id, *, conn) -> dict | None -- pattern for fetch_map_by_code
- fetch_eligible_maps(difficulties, blacklist_weeks, *, conn) -> list[dict] -- needs modification

From apps/api/services/exceptions/__init__.py:
- Tournament exceptions imported at lines 83-96 with aliased barrel pattern
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add SDK structs and domain exceptions</name>
  <files>
    libs/sdk/src/genjishimada_sdk/tournaments.py,
    apps/api/services/exceptions/tournaments.py,
    apps/api/services/exceptions/__init__.py
  </files>
  <read_first>
    libs/sdk/src/genjishimada_sdk/tournaments.py,
    apps/api/services/exceptions/tournaments.py,
    apps/api/services/exceptions/__init__.py
  </read_first>
  <action>
    **SDK structs (per D-05, D-07 discretion):**

    Add `TournamentNextCycleResponse(Struct)` after the existing `TournamentCycleResponse` class in the Cycle types section. Fields: `id: int`, `category_id: int`, `map_id: int`, `map_code: str`, `map_name: str`, `map_difficulty: str`, `status: CycleStatus`, `created_at: dt.datetime`. Google-style docstring with `Attributes:` section. This is a dedicated preview struct that includes joined map detail fields.

    Add `TournamentChooseMapRequest(Struct)` in the Cycle types section (after `TournamentNextCycleResponse`). Single field: `map_code: str`. Docstring: "Request payload for explicitly choosing a map for next cycle."

    Update `__all__` tuple to include `"TournamentChooseMapRequest"` and `"TournamentNextCycleResponse"` in alphabetical position.

    **Domain exceptions (per D-08, D-09):**

    Add three new exception classes to `services/exceptions/tournaments.py`:

    1. `NoEligibleMapsError(TournamentsError)` -- `__init__(self, category_id: int)`, message: "No eligible maps found. Consider reducing blacklist_weeks or adding more maps matching the category's difficulties.", context: `category_id=category_id`.

    2. `PendingCycleAlreadyExistsError(TournamentsError)` -- `__init__(self, category_id: int)`, message: "A pending cycle already exists for this category. Use reroll to replace it.", context: `category_id=category_id`.

    3. `PendingCycleNotFoundError(TournamentsError)` -- `__init__(self, category_id: int)`, message: "No pending cycle exists for this category.", context: `category_id=category_id`.

    **Barrel exports:**

    In `services/exceptions/__init__.py`, add `NoEligibleMapsError`, `PendingCycleAlreadyExistsError`, `PendingCycleNotFoundError` to the `from .tournaments import (...)` block (lines 83-96) and to the `__all__` list.
  </action>
  <verify>
    <automated>cd /Users/nebula/Documents/coding/parkour/genji/genjishimada && uv run --directory libs/sdk ruff check src/genjishimada_sdk/tournaments.py && uv run --directory apps/api ruff check services/exceptions/tournaments.py services/exceptions/__init__.py</automated>
  </verify>
  <acceptance_criteria>
    - `TournamentNextCycleResponse` class exists in SDK with exactly 8 fields: id, category_id, map_id, map_code, map_name, map_difficulty, status, created_at
    - `TournamentChooseMapRequest` class exists in SDK with exactly 1 field: map_code (str)
    - Both new types appear in `__all__` tuple
    - `NoEligibleMapsError.__init__` accepts `category_id: int` parameter
    - `PendingCycleAlreadyExistsError.__init__` accepts `category_id: int` parameter
    - `PendingCycleNotFoundError.__init__` accepts `category_id: int` parameter
    - All three new exceptions inherit from `TournamentsError`
    - All three appear in `services/exceptions/__init__.py` imports and `__all__`
    - ruff check passes for all modified files
  </acceptance_criteria>
  <done>SDK has TournamentNextCycleResponse and TournamentChooseMapRequest structs. Three new domain exceptions exist and are barrel-exported. ruff check passes.</done>
</task>

<task type="auto">
  <name>Task 2: Add repository methods and modify fetch_eligible_maps</name>
  <files>
    apps/api/repository/tournaments_repository.py
  </files>
  <read_first>
    apps/api/repository/tournaments_repository.py,
    apps/api/migrations/0020_tournaments.sql
  </read_first>
  <action>
    Add three new methods to `TournamentRepository` and modify one existing method. Place new methods in the Map Selection section (after `fetch_least_recently_used_map`, before the Streaks section).

    **1. `fetch_pending_cycle(self, category_id: int, *, conn: Connection | None = None) -> dict | None`**
    Follow the `fetch_active_cycle` pattern but filter on `status = 'pending'` and JOIN to `core.maps` for map details. SELECT columns: `cy.id`, `cy.category_id`, `cy.map_id`, `cy.status`, `cy.started_at`, `cy.ended_at`, `cy.created_at`, `m.code AS map_code`, `m.map_name`, `m.difficulty AS map_difficulty`. Use `LIMIT 1` per D-02 (at most one pending per category). Return `dict(row) if row else None`.

    **2. `delete_cycle(self, cycle_id: int, *, conn: Connection | None = None) -> bool`**
    Follow the `delete_category` pattern exactly. SQL: `DELETE FROM tournaments.cycles WHERE id = $1 RETURNING id`. Return `result is not None`.

    **3. `fetch_map_by_code(self, map_code: str, *, conn: Connection | None = None) -> dict | None`**
    Follow the `fetch_category` pattern. Query `core.maps` by code: `SELECT id, code, map_name, difficulty FROM core.maps WHERE code = $1`. Return `dict(row) if row else None`.

    **4. Modify `fetch_eligible_maps` (existing method):**
    - Add parameter `exclude_map_ids: list[int] | None = None` as a keyword-only parameter after `blacklist_weeks`.
    - Add `AND cy.status != 'pending'` condition to the NOT IN subquery (pitfall 1 fix: exclude maps in pending cycles from blacklist window check -- actually, add a separate `AND m.id NOT IN (SELECT map_id FROM tournaments.cycles WHERE status = 'pending')` clause, because pending cycles have NULL started_at and won't be caught by the date filter).
    - When `exclude_map_ids` is not None and non-empty, add `AND m.id != ALL($3::int[])` clause to the WHERE and pass `exclude_map_ids` as positional parameter `$3`. Shift the parameter numbering accordingly: difficulties = $1, blacklist_weeks = $2, exclude_map_ids = $3 (when present). When `exclude_map_ids` is None, keep the query with only $1 and $2.
    - Build the query conditionally: define the base query string, then conditionally append the `AND m.id != ALL($3::int[])` clause and adjust fetch call arguments.

    All methods must have Google-style docstrings with Args/Returns sections.
  </action>
  <verify>
    <automated>cd /Users/nebula/Documents/coding/parkour/genji/genjishimada && uv run --directory apps/api ruff check repository/tournaments_repository.py</automated>
  </verify>
  <acceptance_criteria>
    - `fetch_pending_cycle` method exists with signature `(self, category_id: int, *, conn: Connection | None = None) -> dict | None`
    - `fetch_pending_cycle` SQL JOINs `tournaments.cycles cy` to `core.maps m` and filters `cy.status = 'pending'`
    - `delete_cycle` method exists with signature `(self, cycle_id: int, *, conn: Connection | None = None) -> bool`
    - `delete_cycle` uses `DELETE ... RETURNING id` and returns boolean
    - `fetch_map_by_code` method exists with signature `(self, map_code: str, *, conn: Connection | None = None) -> dict | None`
    - `fetch_eligible_maps` now has `exclude_map_ids: list[int] | None = None` keyword parameter
    - `fetch_eligible_maps` SQL includes `AND m.id NOT IN (SELECT map_id FROM tournaments.cycles WHERE status = 'pending')` clause
    - `fetch_eligible_maps` conditionally adds `AND m.id != ALL($3::int[])` when exclude_map_ids is provided
    - ruff check passes
  </acceptance_criteria>
  <done>Repository has fetch_pending_cycle, delete_cycle, fetch_map_by_code methods. fetch_eligible_maps excludes pending cycle maps and supports exclude_map_ids. ruff passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | This plan defines types, exceptions, and SQL methods. No HTTP input crosses trust boundaries here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | fetch_map_by_code SQL query | mitigate | Uses asyncpg positional parameter $1 for map_code -- no SQL injection possible |
| T-05-02 | Tampering | fetch_eligible_maps exclude_map_ids | mitigate | Uses asyncpg positional parameter $3::int[] -- parameterized array, no injection |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
- `uv run --directory libs/sdk ruff check src/genjishimada_sdk/tournaments.py` passes
- `uv run --directory apps/api ruff check repository/tournaments_repository.py services/exceptions/tournaments.py services/exceptions/__init__.py` passes
- `just lint-sdk` passes (full SDK lint)
</verification>

<success_criteria>
- Two new SDK structs exported in __all__
- Three new domain exceptions barrel-exported
- Three new repository methods with correct signatures and docstrings
- Modified fetch_eligible_maps excludes pending maps and supports exclusion list
- All ruff checks pass
</success_criteria>

<output>
Create `.planning/phases/05-map-selection-blacklist/05-01-SUMMARY.md` when done
</output>
