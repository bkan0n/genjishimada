---
phase: 05-map-selection-blacklist
plan: 03
type: execute
wave: 3
depends_on: [05-02]
files_modified:
  - apps/api/tests/services/conftest.py
  - apps/api/tests/services/test_tournament_service.py
  - apps/api/tests/integration/test_tournaments_integration.py
autonomous: true
requirements: [CYCLE-04, CYCLE-05, CYCLE-06, CYCLE-07]

must_haves:
  truths:
    - "Service unit tests verify select_map happy path, pending-already-exists error, pool-exhausted LRU fallback, and no-eligible-maps error"
    - "Service unit tests verify reroll_map excludes old map_id from selection"
    - "Service unit tests verify choose_map validates map exists and difficulty matches"
    - "Integration tests verify full HTTP stack for all 4 endpoints including success and error status codes"
    - "All tests pass with just test-api"
  artifacts:
    - path: "apps/api/tests/services/test_tournament_service.py"
      provides: "Service unit tests for map selection business logic"
      contains: "TestSelectMap"
    - path: "apps/api/tests/services/conftest.py"
      provides: "mock_tournament_repo fixture"
      contains: "mock_tournament_repo"
    - path: "apps/api/tests/integration/test_tournaments_integration.py"
      provides: "Integration test classes for map selection endpoints"
      contains: "TestSelectMapEndpoint"
  key_links:
    - from: "apps/api/tests/services/test_tournament_service.py"
      to: "apps/api/services/tournament_service.py"
      via: "TournamentService instantiation with mocks"
      pattern: "TournamentService\\(mock_pool"
    - from: "apps/api/tests/integration/test_tournaments_integration.py"
      to: "apps/api/routes/v3/tournaments.py"
      via: "HTTP requests through test_client"
      pattern: "test_client\\.post.*select-map"
---

<objective>
Create service unit tests and integration tests for all map selection functionality.

Purpose: Validate the business logic (service layer) in isolation with mocks, and the full HTTP stack (controller -> service -> repository -> database) with real database integration tests. This ensures CYCLE-04 through CYCLE-07 are verified at both levels.
Output: New service unit test file, updated conftest with mock fixture, extended integration test file with 4 new test classes.
</objective>

<execution_context>
@/Users/nebula/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nebula/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-map-selection-blacklist/05-CONTEXT.md
@.planning/phases/05-map-selection-blacklist/05-RESEARCH.md
@.planning/phases/05-map-selection-blacklist/05-PATTERNS.md
@.planning/phases/05-map-selection-blacklist/05-01-SUMMARY.md
@.planning/phases/05-map-selection-blacklist/05-02-SUMMARY.md

<interfaces>
<!-- Executor MUST read Plan 01 and 02 SUMMARYs for actual artifacts -->

From apps/api/services/tournament_service.py (created in Plan 02):
- select_map(category_id: int) -> TournamentNextCycleResponse
- get_next_cycle(category_id: int) -> TournamentNextCycleResponse
- reroll_map(category_id: int) -> TournamentNextCycleResponse
- choose_map(category_id: int, data: TournamentChooseMapRequest) -> TournamentNextCycleResponse

From apps/api/services/exceptions/tournaments.py (created in Plan 01):
- NoEligibleMapsError(category_id: int)
- PendingCycleAlreadyExistsError(category_id: int)
- PendingCycleNotFoundError(category_id: int)
- CategoryNotFoundError(category_id: int) -- existing
- MapNotEligibleError(map_id: int, reason: str = "") -- existing

From apps/api/repository/tournaments_repository.py:
- TournamentRepository class (for AsyncMock spec)
- fetch_pending_cycle(category_id, *, conn) -> dict | None
- delete_cycle(cycle_id, *, conn) -> bool
- fetch_map_by_code(map_code, *, conn) -> dict | None
- fetch_eligible_maps(difficulties, blacklist_weeks, *, exclude_map_ids, conn) -> list[dict]
- fetch_least_recently_used_map(difficulties, *, conn) -> dict | None
- create_cycle(category_id, map_id, *, conn) -> dict
- fetch_config(*, conn) -> dict
- fetch_category(category_id, *, conn) -> dict | None

From apps/api/tests/services/conftest.py:
- Existing fixture pattern: mock_maps_repo uses mocker.AsyncMock(spec=MapsRepository)
- mock_pool fixture provides MagicMock with acquire() context manager
- mock_state fixture provides Mock with mq_channel_pool

From apps/api/tests/integration/test_tournaments_integration.py:
- BASE = "/api/v3/tournaments"
- TestCategoryLockedGuard pattern: create category via API, create_test_map, insert cycle via DB, test endpoint
- test_client, asyncpg_pool, create_test_map fixtures available
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add mock fixture and create service unit tests</name>
  <files>
    apps/api/tests/services/conftest.py,
    apps/api/tests/services/test_tournament_service.py
  </files>
  <read_first>
    apps/api/tests/services/conftest.py,
    apps/api/tests/services/test_maps_service.py,
    apps/api/services/tournament_service.py,
    apps/api/services/exceptions/tournaments.py
  </read_first>
  <action>
    **conftest.py addition:**
    Add `from repository.tournaments_repository import TournamentRepository` to imports.
    Add fixture at the end of the Repository Fixtures section:
    ```
    @pytest.fixture
    def mock_tournament_repo(mocker):
        """Mock TournamentRepository."""
        return mocker.AsyncMock(spec=TournamentRepository)
    ```

    **test_tournament_service.py (new file):**
    Create new file following the `test_maps_service.py` pattern.

    Module docstring: "Unit tests for TournamentService map selection logic."

    Imports needed: `pytest`, `TournamentService`, `TournamentChooseMapRequest` from SDK, domain exceptions (`NoEligibleMapsError`, `PendingCycleAlreadyExistsError`, `PendingCycleNotFoundError`, `CategoryNotFoundError`, `MapNotEligibleError`).

    `pytestmark = [pytest.mark.domain_tournaments]`

    Helper: define a module-level dict factory for mock return values:
    - `_config = lambda **kw: {"blacklist_weeks": 4, **kw}`
    - `_category = lambda **kw: {"id": 1, "name": "Test", "difficulties": ["Easy"], **kw}`
    - `_map = lambda **kw: {"id": 10, "code": "ABC12", "map_name": "TestMap", "difficulty": "Easy", **kw}`
    - `_pending = lambda **kw: {"id": 100, "category_id": 1, "map_id": 10, "map_code": "ABC12", "map_name": "TestMap", "map_difficulty": "Easy", "status": "pending", "started_at": None, "ended_at": None, "created_at": "2026-01-01T00:00:00", **kw}`

    **class TestSelectMap:**
    Each test instantiates `service = TournamentService(mock_pool, mock_state, mock_tournament_repo)` and sets up mock return values.

    1. `test_select_map_happy_path` -- mock: fetch_pending_cycle returns None, fetch_config returns config dict, fetch_category returns category dict, fetch_eligible_maps returns [map_dict], create_cycle returns cycle dict, second fetch_pending_cycle returns pending dict. Assert: result has map_code field, fetch_eligible_maps called with category difficulties and config blacklist_weeks.

    2. `test_select_map_pending_already_exists` -- mock: fetch_pending_cycle returns a dict (pending exists). Assert: raises `PendingCycleAlreadyExistsError`.

    3. `test_select_map_category_not_found` -- mock: fetch_pending_cycle returns None, fetch_config returns config, fetch_category returns None. Assert: raises `CategoryNotFoundError`.

    4. `test_select_map_pool_exhausted_lru_fallback` -- mock: fetch_pending_cycle None, fetch_config config, fetch_category category, fetch_eligible_maps returns [], fetch_least_recently_used_map returns map_dict, create_cycle cycle, second fetch_pending_cycle pending. Assert: result has map_code, fetch_least_recently_used_map was called.

    5. `test_select_map_no_eligible_maps` -- mock: fetch_pending_cycle None, fetch_config config, fetch_category category, fetch_eligible_maps [], fetch_least_recently_used_map None. Assert: raises `NoEligibleMapsError`.

    **class TestRerollMap:**

    6. `test_reroll_happy_path` -- mock: fetch_category returns category, fetch_pending_cycle returns pending (first call), delete_cycle True, fetch_config config, fetch_eligible_maps [map], create_cycle cycle, fetch_pending_cycle returns new pending (second call). Assert: delete_cycle called with old cycle id, fetch_eligible_maps called with `exclude_map_ids=[old_map_id]`.

    7. `test_reroll_no_pending_raises` -- mock: fetch_category returns category, fetch_pending_cycle returns None. Assert: raises `PendingCycleNotFoundError`.

    **class TestChooseMap:**

    8. `test_choose_map_happy_path` -- mock: fetch_category returns category with difficulties=["Easy"], fetch_map_by_code returns map dict with difficulty="Easy", fetch_pending_cycle returns None (no existing pending), create_cycle cycle, fetch_pending_cycle returns pending. Assert: result has map_code.

    9. `test_choose_map_not_found` -- mock: fetch_category returns category, fetch_map_by_code returns None. Assert: raises `MapNotEligibleError`.

    10. `test_choose_map_difficulty_mismatch` -- mock: fetch_category returns category with difficulties=["Hard"], fetch_map_by_code returns map with difficulty="Easy". Assert: raises `MapNotEligibleError`.

    11. `test_choose_map_replaces_existing_pending` -- mock: fetch_category returns category, fetch_map_by_code returns map, fetch_pending_cycle returns existing pending (first call), delete_cycle True, create_cycle cycle, fetch_pending_cycle returns new pending (second call). Assert: delete_cycle called with old pending id.

    **class TestGetNextCycle:**

    12. `test_get_next_cycle_happy_path` -- mock: fetch_category returns category, fetch_pending_cycle returns pending dict. Assert: result has expected fields.

    13. `test_get_next_cycle_category_not_found` -- mock: fetch_category returns None. Assert: raises `CategoryNotFoundError`.

    14. `test_get_next_cycle_no_pending` -- mock: fetch_category returns category, fetch_pending_cycle returns None. Assert: raises `PendingCycleNotFoundError`.

    Note on mock_pool: the `select_map`, `reroll_map`, and `choose_map` methods use `async with self._pool.acquire() as conn:`. The `mock_pool` fixture from conftest already handles this with `__aenter__`/`__aexit__` mocks. The returned `conn` from `mock_pool.acquire()` is a MagicMock, not used directly in unit tests since all repo calls are mocked at the AsyncMock level.
  </action>
  <verify>
    <automated>cd /Users/nebula/Documents/coding/parkour/genji/genjishimada && uv run --directory apps/api pytest tests/services/test_tournament_service.py -v -p no:xdist</automated>
  </verify>
  <acceptance_criteria>
    - `mock_tournament_repo` fixture exists in `tests/services/conftest.py` using `mocker.AsyncMock(spec=TournamentRepository)`
    - `test_tournament_service.py` has 4 test classes: TestSelectMap, TestRerollMap, TestChooseMap, TestGetNextCycle
    - At least 14 test methods covering: happy paths, error paths (pending exists, category not found, pool exhausted, no eligible maps, no pending for reroll, map not found, difficulty mismatch)
    - TestSelectMap has test for LRU fallback path verifying `fetch_least_recently_used_map` is called
    - TestRerollMap verifies `exclude_map_ids` is passed to `fetch_eligible_maps`
    - TestChooseMap verifies `delete_cycle` is called when replacing existing pending
    - All tests pass with `uv run --directory apps/api pytest tests/services/test_tournament_service.py -v -p no:xdist`
  </acceptance_criteria>
  <done>Service unit tests exist with 14+ tests covering all business logic paths. mock_tournament_repo fixture in conftest. All tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Add integration tests for map selection endpoints</name>
  <files>
    apps/api/tests/integration/test_tournaments_integration.py
  </files>
  <read_first>
    apps/api/tests/integration/test_tournaments_integration.py,
    apps/api/routes/v3/tournaments.py,
    apps/api/tests/integration/conftest.py
  </read_first>
  <action>
    Extend `test_tournaments_integration.py` by adding 4 new test classes after the existing `TestUnauthenticated` class. Follow the `TestCategoryLockedGuard` pattern for DB setup.

    **class TestSelectMapEndpoint:**
    "Tests for POST /api/v3/tournaments/categories/{id}/select-map"

    1. `test_select_map_creates_pending_cycle(self, test_client, asyncpg_pool, create_test_map)` -- Create a category via API. Create a test map with matching difficulty using `create_test_map` (check what difficulty the fixture creates; if it uses "Easy" by default, create the category with `difficulties: ["Easy"]`). POST to `{BASE}/categories/{category_id}/select-map`. Assert: 201 status, response has `map_code`, `map_name`, `map_difficulty`, `status` == "pending".

    2. `test_select_map_returns_409_when_pending_exists(self, test_client, asyncpg_pool, create_test_map)` -- Create category, create test map, insert a pending cycle directly into DB (`INSERT INTO tournaments.cycles (category_id, map_id) VALUES ($1, $2)` -- defaults to 'pending' status). POST to select-map. Assert: 409.

    3. `test_select_map_category_not_found(self, test_client)` -- POST to `{BASE}/categories/999999/select-map`. Assert: 404.

    **class TestGetNextCycleEndpoint:**
    "Tests for GET /api/v3/tournaments/categories/{id}/next-cycle"

    4. `test_preview_existing_pending(self, test_client, asyncpg_pool, create_test_map)` -- Create category, create test map, insert pending cycle via DB. GET to `{BASE}/categories/{category_id}/next-cycle`. Assert: 200, response has `map_code`, `status` == "pending".

    5. `test_preview_no_pending_returns_404(self, test_client)` -- Create category (no pending cycle). GET to next-cycle. Assert: 404.

    6. `test_preview_category_not_found(self, test_client)` -- GET `{BASE}/categories/999999/next-cycle`. Assert: 404.

    **class TestRerollEndpoint:**
    "Tests for POST /api/v3/tournaments/categories/{id}/reroll"

    7. `test_reroll_changes_map(self, test_client, asyncpg_pool, create_test_map)` -- Create category. Create TWO test maps (need at least 2 eligible maps so reroll can pick a different one). POST to select-map to create initial pending. Store original map_id from response. POST to reroll. Assert: 201, response `status` == "pending". Note: random selection means reroll MIGHT select the same map if only 2 exist, but the exclude_map_ids logic prevents it. Assert response status is 201.

    8. `test_reroll_no_pending_returns_404(self, test_client)` -- Create category. POST to reroll without first selecting. Assert: 404.

    **class TestChooseMapEndpoint:**
    "Tests for PATCH /api/v3/tournaments/categories/{id}/next-cycle"

    9. `test_choose_map_sets_specific_map(self, test_client, asyncpg_pool, create_test_map)` -- Create category with difficulties=["Easy"]. Create test map with "Easy" difficulty. Get the map code from DB. PATCH to `{BASE}/categories/{category_id}/next-cycle` with `{"map_code": map_code}`. Assert: 200, response `map_code` matches sent code.

    10. `test_choose_map_invalid_code_returns_422(self, test_client)` -- Create category. PATCH with `{"map_code": "ZZZZZ"}` (nonexistent). Assert: 422.

    11. `test_choose_map_difficulty_mismatch_returns_422(self, test_client, asyncpg_pool, create_test_map)` -- Create category with difficulties=["Very Hard"]. Create test map that is "Easy" difficulty. Get map code. PATCH with that code. Assert: 422. (The map exists but doesn't match the category's difficulty grouping.)

    **Important notes for test setup:**
    - The `create_test_map` fixture likely creates maps with a specific difficulty. Read the integration conftest to understand its parameters. If it accepts a difficulty parameter, use it to control map difficulty. If not, insert maps directly via `asyncpg_pool`.
    - Use `uuid4().hex[:5]` for unique map codes in direct DB inserts.
    - For maps created via `create_test_map`, read the returned map_id and query the DB for the code: `await conn.fetchval("SELECT code FROM core.maps WHERE id = $1", map_id)`.
    - Set `blacklist_weeks` to 0 (or a low value) via `PATCH {BASE}/config` before selection tests to avoid blacklist interference from other test-created cycles.
  </action>
  <verify>
    <automated>cd /Users/nebula/Documents/coding/parkour/genji/genjishimada && uv run --directory apps/api pytest tests/integration/test_tournaments_integration.py -v -p no:xdist</automated>
  </verify>
  <acceptance_criteria>
    - 4 new test classes exist: TestSelectMapEndpoint, TestGetNextCycleEndpoint, TestRerollEndpoint, TestChooseMapEndpoint
    - At least 11 test methods covering happy paths and error cases for all 4 endpoints
    - TestSelectMapEndpoint verifies 201 response with pending cycle data including map_code
    - TestSelectMapEndpoint verifies 409 when pending already exists
    - TestGetNextCycleEndpoint verifies 200 response with map details
    - TestRerollEndpoint verifies 201 response after reroll
    - TestChooseMapEndpoint verifies 200 response with chosen map_code matching request
    - TestChooseMapEndpoint verifies 422 for nonexistent map code
    - TestChooseMapEndpoint verifies 422 for difficulty mismatch
    - All integration tests pass (both old and new): `uv run --directory apps/api pytest tests/integration/test_tournaments_integration.py -v -p no:xdist`
    - Full test suite passes: `just test-api`
  </acceptance_criteria>
  <done>Integration tests cover all 4 map selection endpoints with 11+ tests. All tests pass including existing tests. just test-api green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Test code only -- no trust boundaries in test files |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
- `uv run --directory apps/api pytest tests/services/test_tournament_service.py -v -p no:xdist` -- all service unit tests pass
- `uv run --directory apps/api pytest tests/integration/test_tournaments_integration.py -v -p no:xdist` -- all integration tests pass (old + new)
- `just test-api` -- full test suite green
</verification>

<success_criteria>
- mock_tournament_repo fixture added to services conftest
- 14+ service unit tests covering all business logic paths (happy, error, fallback)
- 11+ integration tests covering all 4 endpoints (success + error status codes)
- Full test suite passes: just test-api
</success_criteria>

<output>
Create `.planning/phases/05-map-selection-blacklist/05-03-SUMMARY.md` when done
</output>
