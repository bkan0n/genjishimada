---
phase: 13-skill-score
plan: 03
type: execute
wave: 2
depends_on: ["13-01", "13-02"]
files_modified:
  - apps/api/repository/skill_repository.py
autonomous: true
requirements: [2, 3, 5]
must_haves:
  truths:
    - "fetch_skill_inputs returns one fastest verified non-legacy run per (user, map)"
    - "Unverified, legacy, archived, codeless, and suspicious-flagged rows are absent from the inputs"
    - "time_pct = 1.0 for the fastest run in each map's field"
    - "fetch_weights / update_weights read and write the single config row"
    - "Snapshot bulk upsert replaces the whole lean snapshot atomically"
  artifacts:
    - path: "apps/api/repository/skill_repository.py"
      provides: "input query port + snapshot read/upsert + weights read/write + provide_skill_repository"
      contains: "class SkillRepository"
      min_lines: 80
  key_links:
    - from: "SkillRepository.fetch_skill_inputs"
      to: "the spike 4-CTE input query (best→field→video_ranked→fully)"
      via: "verbatim SQL port with $1,$2 conventions"
      pattern: "video_ranked|percent_rank|raw_difficulty::float8"
---

<objective>
Create `apps/api/repository/skill_repository.py`: the data-access layer for skill.
Ports the spike's 4-CTE input query (`best → field → video_ranked → fully`) verbatim
into a repository method, plus the lean-snapshot read/bulk-upsert and the single-row
weight config read/write (SPEC req 2, 3, 5; D-04, D-07, D-09).

Purpose: This is the only place raw SQL for skill lives. The input query is the
load-bearing port — every scoring signal the service needs is computed here, and the
gotchas (FILTER-is-aggregate-only, completion-flag = verification depth, never compare
raw time across maps) are encoded in the SQL, not the Python.

Output: `apps/api/repository/skill_repository.py` (SkillRepository + provide fn).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-skill-score/13-SPEC.md
@.planning/phases/13-skill-score/13-CONTEXT.md
@.planning/phases/13-skill-score/13-PATTERNS.md

<interfaces>
From apps/api/repository/base.py — BaseRepository: `__init__(self, pool)`, `self._get_connection(conn)` returns the injected conn or acquires from pool.
Provider convention (community_repository.py:720): provide_skill_repository(state) returns SkillRepository(state.db_pool).
The skill.weight_config columns (migration 0027): diff_base, gamma, time_bonus, shrink_k, wr_bonus, partial_factor, medal_gold, medal_silver, medal_bronze.
The skill.snapshot columns (migration 0027): user_id, skill_score, maps_cleared, video_clears, hardest_raw, breakdown (jsonb), computed_at.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SkillRepository class + ported 4-CTE input query (fetch_skill_inputs)</name>
  <files>apps/api/repository/skill_repository.py</files>
  <read_first>
    - .claude/skills/spike-findings-genjishimada/sources/001-skill-input-query/query.py (lines 24-92) — THE SQL to port verbatim; copy the entire SKILL_INPUT_QUERY string (best → field → video_ranked → fully CTEs) into a module constant, discarding only the asyncpg.connect/JSON-dump harness
    - .claude/skills/spike-findings-genjishimada/references/skill-input-query.md — the eligibility filters, the completion-flag = verification-depth fact, the FILTER-is-aggregate-only gotcha, the never-compare-raw-time rule
    - apps/api/repository/completions_repository.py (lines 22-60) — BaseRepository subclass shape, the `self._get_connection(conn)` pattern, the real ranking SQL idiom and `completion` flag semantics
    - apps/api/repository/community_repository.py (lines 1-15, 720-722) — import style and the provide_* function shape
    - apps/api/repository/base.py — BaseRepository __init__ and _get_connection signature
  </read_first>
  <action>
    Create `apps/api/repository/skill_repository.py` with `from __future__ import annotations`, importing `Connection`, `Pool` from asyncpg, `State` from litestar.datastructures, and `BaseRepository` from `.base`.

    Define `SKILL_INPUT_QUERY` as a module-level triple-quoted constant — the VERBATIM port of `query.py:24-92` (the 4-CTE query). Preserve every load-bearing detail exactly:
    - The eligibility WHERE in `best` (SPEC req 3): `c.verified = TRUE AND c.legacy = FALSE AND m.archived = FALSE AND m.code IS NOT NULL`, with `DISTINCT ON (c.user_id, c.map_id) ORDER BY c.user_id, c.map_id, c.time ASC` (one fastest run per (user,map)).
    - The `video_ranked` CTE ranks ONLY `completion = FALSE` rows in its OWN CTE and LEFT JOINs back (do NOT collapse into a `rank() OVER (...) FILTER (...)` — invalid in Postgres).
    - Emit columns: `raw_difficulty::float8`, `time::float8`, `(completion = FALSE) AS fully_verified`, `field_size`, `field_rank`, `video_rank`, `round(time_pct,4)::float8` (1.0 = fastest), computed `medal` (CASE on md.gold/silver/bronze vs time), `(md.map_id IS NOT NULL) AS has_medal_thresholds`, and the `suspicious` EXISTS column.
    - `time_pct` uses `percent_rank() OVER (PARTITION BY map_id ORDER BY time DESC)` — field-relative; NEVER compare raw time across maps.

    Define `class SkillRepository(BaseRepository)`. Add an async method `fetch_skill_inputs` taking a keyword-only `conn: Connection | None = None`, that resolves the connection via `self._get_connection(conn)`, runs `SKILL_INPUT_QUERY` via `conn.fetch`, then returns the rows as dicts with suspicious rows FILTERED OUT in Python (keep only rows where `suspicious` is falsey) — mirroring the spike harness (query.py:105-106) which drops suspicious rows entirely (SPEC req 3: suspicious-flagged users absent; they contribute nothing → score 0, SPEC AC).

    At module bottom add an async `provide_skill_repository(state: State) -> SkillRepository` that returns `SkillRepository(state.db_pool)` (mirror community_repository.py:720).
  </action>
  <verify>
    <automated>cd /Users/nebula/coding/parkour/genji/genjishimada && uv run --project apps/api python -c "import ast; t=ast.parse(open('apps/api/repository/skill_repository.py').read()); names={n.name for n in ast.walk(t) if isinstance(n,(ast.AsyncFunctionDef,ast.ClassDef))}; assert {'SkillRepository','fetch_skill_inputs','provide_skill_repository'} <= names, names; src=open('apps/api/repository/skill_repository.py').read(); pre=src.split('SELECT')[0]; assert 'video_ranked' in src and 'FILTER' not in pre; assert 'percent_rank' in src and 'raw_difficulty::float8' in src; print('ok')" && just lint-api</automated>
    <human-check>If a seeded test DB with the spike fixtures is available, run fetch_skill_inputs and confirm: exactly one row per (user,map), no rows with verified=FALSE/legacy=TRUE/archived=TRUE/code IS NULL/suspicious, and time_pct=1.0 for each field's fastest run.</human-check>
  </verify>
  <acceptance_criteria>
    - Source: `SkillRepository(BaseRepository)`, `fetch_skill_inputs`, and `provide_skill_repository` all exist.
    - Source: the SQL contains a distinct `video_ranked` CTE and uses `percent_rank()` for time_pct; there is NO `rank() OVER (...) FILTER (...)` window construct (FILTER-is-aggregate-only gotcha respected).
    - Source: the SQL emits `raw_difficulty::float8` (never the text tier) and `time_pct` (never raw time compared across maps).
    - Source: the eligibility WHERE is exactly `c.verified = TRUE AND c.legacy = FALSE AND m.archived = FALSE AND m.code IS NOT NULL`; suspicious rows are dropped in the Python post-filter.
    - Behavior (with spike fixtures): one fastest run per (user,map); excluded rows absent; `time_pct = 1.0` for each field's fastest run (SPEC AC).
    - Test: `just lint-api` clean.
  </acceptance_criteria>
  <done>SkillRepository ports the spike 4-CTE input query verbatim with all gotchas preserved; fetch_skill_inputs returns one fastest eligible run per (user,map) with suspicious rows dropped; provide_skill_repository wired.</done>
</task>

<task type="auto">
  <name>Task 2: Snapshot read/bulk-upsert + weight config read/write</name>
  <files>apps/api/repository/skill_repository.py</files>
  <read_first>
    - apps/api/repository/community_repository.py (lines 61, 231-232) — `_conn = self._get_connection(conn)`, `[dict(row) for row in rows]`, $1,$2 positional params, fetch/fetchrow idioms
    - apps/api/app.py (lines 125-139) — the jsonb↔msgspec codec: writing a Python list to a jsonb column auto-serializes, reading auto-deserializes (D-06 breakdown column needs no manual JSON handling)
    - .planning/phases/13-skill-score/13-PATTERNS.md (lines 156-174) — fetch_snapshot, snapshot bulk upsert (TRUNCATE+INSERT or executemany inside one transaction), fetch_weights/update_weights shapes
    - migration 0027 (from plan 13-01) — the exact skill.snapshot and skill.weight_config column names
  </read_first>
  <action>
    Add four methods to `SkillRepository` (all taking keyword-only `conn: Connection | None = None` and resolving via `self._get_connection(conn)`):

    1. `fetch_snapshot(user_id)` → `SELECT * FROM skill.snapshot WHERE user_id = $1`; return the row as a dict or None. The `breakdown` jsonb column decodes to a Python list automatically via the app codec (D-06).

    2. `replace_snapshot(rows)` — the lean-snapshot bulk upsert (D-04/D-07): inside a SINGLE `conn.transaction()`, `TRUNCATE skill.snapshot` then bulk-insert the supplied rows (each dict has user_id, skill_score, maps_cleared, video_clears, hardest_raw, breakdown, computed_at). Use `executemany`/`copy_records_to_table` for the insert. The `breakdown` value is passed as a Python list; the `_jsonb_encoder` (app.py:132) serializes it. An empty `rows` list must leave the snapshot empty (truncate only) — do not error.

    3. `fetch_weights()` → `SELECT diff_base, gamma, time_bonus, shrink_k, wr_bonus, partial_factor, medal_gold, medal_silver, medal_bronze FROM skill.weight_config LIMIT 1`; return the row as a dict. (SPEC req 5: the ONLY source of weights — no literals in service code.)

    4. `update_weights(weights)` — UPDATE the single config row setting only the supplied keys (positional `$1,$2,...`), RETURNING the full updated row as a dict. Build the SET clause from the provided keys (allow-list to the 9 known weight column names) so a partial PATCH (D-10) updates only changed weights; guard against an empty update (return the current row unchanged).
  </action>
  <verify>
    <automated>cd /Users/nebula/coding/parkour/genji/genjishimada && uv run --project apps/api python -c "import ast; t=ast.parse(open('apps/api/repository/skill_repository.py').read()); names={n.name for n in ast.walk(t) if isinstance(n,ast.AsyncFunctionDef)}; assert {'fetch_snapshot','replace_snapshot','fetch_weights','update_weights'} <= names, names; print('ok')" && just lint-api</automated>
    <human-check>Against the migrated test DB: write a snapshot row via replace_snapshot (with a breakdown list), fetch_snapshot it back, assert breakdown decodes to a list of dicts; fetch_weights returns the 9 seeded defaults; update_weights({gamma:0.7}) returns a row with gamma=0.7 and other weights unchanged.</human-check>
  </verify>
  <acceptance_criteria>
    - Source: `fetch_snapshot`, `replace_snapshot`, `fetch_weights`, `update_weights` exist on SkillRepository.
    - Behavior: `replace_snapshot` runs inside one transaction (TRUNCATE + bulk insert); an empty list leaves the snapshot empty without error.
    - Behavior: `fetch_weights` returns a dict of exactly the 9 weight columns; with the migration seed it equals the adopted defaults.
    - Behavior: `update_weights` with a partial dict updates only those columns (allow-listed names) and returns the full updated row; round-trips through the breakdown jsonb codec with no manual JSON.
    - Test: `just lint-api` clean.
  </acceptance_criteria>
  <done>Snapshot read/bulk-upsert and weight read/write are implemented with $1,$2 conventions; breakdown rides the jsonb codec; replace_snapshot is transactional and empty-safe.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| service → repository → DB | The repository receives already-typed args from the service; SQL params are positional ($1,$2) so no string interpolation of untrusted values |
| DB rows → app | `fetch_skill_inputs` reads completion/user data; suspicious-flag and eligibility filters are the integrity gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-05 | Tampering | input query eligibility filter | mitigate | The eligibility WHERE + suspicious drop are enforced in SQL/Python, not caller-supplied — a suspicious or unverified run cannot leak into scoring inputs |
| T-13-06 | Information Disclosure | fetch_skill_inputs row contents | accept | Inputs derive from already-public completion/leaderboard data; no new disclosure surface |
| T-13-07 | Tampering | update_weights SET clause | mitigate | SET columns are an allow-list of the 9 known weight keys (not arbitrary caller column names); values bound positionally; out-of-range values gated by the gamma CHECK (migration) and service validation (plan 13-04) |
| T-13-SC | Tampering | npm/pip/cargo installs | mitigate | No new package installs; nothing to verify |
</threat_model>

<verification>
- `fetch_skill_inputs` ports the spike SQL with all gotchas; returns one eligible run per (user,map), suspicious dropped, time_pct=1.0 for field-fastest.
- Snapshot read/upsert and weight read/write operate on the 0027 columns; breakdown uses the jsonb codec.
- `just lint-api` clean.
</verification>

<success_criteria>
- [ ] SkillRepository + provide_skill_repository created
- [ ] fetch_skill_inputs is a verbatim port of the spike 4-CTE query, gotchas preserved, suspicious dropped
- [ ] fetch_snapshot / replace_snapshot / fetch_weights / update_weights implemented with $1,$2 + jsonb codec
- [ ] just lint-api clean
</success_criteria>

<output>
Create `.planning/phases/13-skill-score/13-03-SUMMARY.md` when done
</output>
