---
phase: 13-skill-score
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - libs/sdk/src/genjishimada_sdk/skill.py
  - libs/sdk/src/genjishimada_sdk/users.py
autonomous: true
requirements: [5, 6, 7]
must_haves:
  truths:
    - "A skill SDK module exports the Weights, config-update, summary, and breakdown structs"
    - "CommunityLeaderboardResponse carries a non-optional skill_score float field"
  artifacts:
    - path: "libs/sdk/src/genjishimada_sdk/skill.py"
      provides: "msgspec structs for skill endpoints (Weights, SkillConfigUpdateRequest, SkillSummaryResponse, SkillBreakdownRow)"
      contains: "class Weights"
    - path: "libs/sdk/src/genjishimada_sdk/users.py"
      provides: "skill_score field on CommunityLeaderboardResponse"
      contains: "skill_score"
  key_links:
    - from: "Weights struct"
      to: "skill.weight_config row (D-09)"
      via: "one float field per weight column, 1:1 names"
      pattern: "diff_base|gamma|partial_factor"
---

<objective>
Define the SDK msgspec structs for the skill domain and add the `skill_score`
field to `CommunityLeaderboardResponse` (SPEC req 5/6/7; D-08, D-09). This is the
interface-contract plan — every downstream plan (repository, service, routes,
leaderboard) imports these shapes, so it ships first with no DB dependency.

Purpose: Lock the wire contracts before any consumer is written so executors build
against fixed types, not a moving target.

Output: `libs/sdk/src/genjishimada_sdk/skill.py` (new) and a one-field edit to
`libs/sdk/src/genjishimada_sdk/users.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-skill-score/13-SPEC.md
@.planning/phases/13-skill-score/13-CONTEXT.md
@.planning/phases/13-skill-score/13-PATTERNS.md

<interfaces>
From libs/sdk/src/genjishimada_sdk/users.py (~line 214) — the struct to extend:
  class CommunityLeaderboardResponse(Struct):
    # ... existing fields incl. skill_rank: str (the Ninja→God label — UNCHANGED)
    # add: skill_score: float (NEW; SQL COALESCE(...,0) guarantees a value, so non-optional)

The spike breakdown dict keys (sources/002.../score.py:78-88) the JSONB array elements carry:
  map_name, difficulty, raw, fully_verified, medal, wr, raw_score, contribution, rank
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create libs/sdk/.../skill.py with the four skill structs</name>
  <files>libs/sdk/src/genjishimada_sdk/skill.py</files>
  <read_first>
    - libs/sdk/src/genjishimada_sdk/users.py (lines 214-245) — CommunityLeaderboardResponse style: Struct subclass, Attributes docstring block, field typing conventions
    - apps/api/routes/v3/content.py (lines 95-172) — the msgspec.UNSET / UnsetType pattern for optional PATCH request fields and frozen response struct style
    - .planning/phases/13-skill-score/13-PATTERNS.md (lines 286-334) — the exact struct shapes (Weights, SkillConfigUpdateRequest, SkillSummaryResponse, SkillBreakdownRow) and which keys map to the D-09 config row and the D-06 breakdown JSONB element
    - .claude/skills/spike-findings-genjishimada/sources/002-scoring-farming-resistance/score.py (lines 70-89) — player_breakdown emits the per-map dict keys the SkillBreakdownRow must mirror
  </read_first>
  <behavior>
    - Weights round-trips: msgspec.convert of a dict with all 9 keys yields a Weights with diff_base=1.44 etc.; a missing key raises (all 9 required).
    - SkillConfigUpdateRequest with NO fields set decodes with every field == UNSET (all-optional PATCH semantics).
    - SkillBreakdownRow decodes from the spike breakdown dict (map_name/raw/fully_verified/medal/wr/raw_score/contribution/rank) — medal is `str | None`.
    - SkillSummaryResponse holds user_id, skill_score, maps_cleared, video_clears, hardest_raw.
  </behavior>
  <action>
    Create `libs/sdk/src/genjishimada_sdk/skill.py` with `from __future__ import annotations` and msgspec structs (follow Genji `*Request`/`*Response` suffix + Google docstring conventions; line length 120):

    - `class Weights(Struct)` — 1:1 with the D-09 config row: `diff_base, gamma, time_bonus, shrink_k, wr_bonus, partial_factor, medal_gold, medal_silver, medal_bronze`, ALL `float`, all required. This is the type `SkillService` loads from `fetch_weights()` — no defaults here (defaults live in migration 0027 seed, NOT in code; SPEC req 5: no hardcoded weights).
    - `class SkillConfigUpdateRequest(Struct)` — one `float | UnsetType = UNSET` field per weight (all optional, PATCH partial-update semantics; mirror `content.py` UNSET pattern). Import `from msgspec import UNSET, UnsetType, Struct`.
    - `class SkillSummaryResponse(Struct)` — `user_id: int`, `skill_score: float`, `maps_cleared: int`, `video_clears: int`, `hardest_raw: float` (GET /skill/users/{id}).
    - `class SkillBreakdownRow(Struct)` — one per-map breakdown entry (the D-06 JSONB array element, decoded by the jsonb↔msgspec codec): `map_name: str`, `difficulty: str`, `raw: float`, `fully_verified: bool`, `medal: str | None`, `wr: bool`, `raw_score: float`, `contribution: float`, `rank: int`. Field names MUST match the spike `player_breakdown` dict keys (score.py:78-88) exactly so the stored JSONB decodes straight into `list[SkillBreakdownRow]`.

    If the SDK uses an `__init__.py` re-export convention for new modules, follow `libs/sdk/src/genjishimada_sdk/__init__.py` — add the skill module/structs to `__all__` only if other modules are similarly re-exported (check before editing; do not invent a registration the codebase doesn't use).
  </action>
  <verify>
    <automated>cd libs/sdk && uv run python -c "from genjishimada_sdk.skill import Weights, SkillConfigUpdateRequest, SkillSummaryResponse, SkillBreakdownRow; import msgspec; w=msgspec.convert({'diff_base':1.44,'gamma':0.68,'time_bonus':0.55,'shrink_k':10.0,'wr_bonus':0.10,'partial_factor':0.60,'medal_gold':1.12,'medal_silver':1.07,'medal_bronze':1.03}, Weights); assert w.gamma==0.68; r=msgspec.convert({'map_name':'x','difficulty':'Hell','raw':9.6,'fully_verified':True,'medal':None,'wr':True,'raw_score':12.3,'contribution':12.3,'rank':1}, SkillBreakdownRow); assert r.medal is None; u=msgspec.json.decode(msgspec.json.encode(SkillConfigUpdateRequest()), type=SkillConfigUpdateRequest); print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - Source: `libs/sdk/src/genjishimada_sdk/skill.py` defines `Weights`, `SkillConfigUpdateRequest`, `SkillSummaryResponse`, `SkillBreakdownRow`.
    - Behavior: `msgspec.convert(<full weights dict>, Weights)` succeeds and `.gamma == 0.68`; omitting a key raises.
    - Behavior: `SkillConfigUpdateRequest()` round-trips with all fields UNSET (PATCH partial semantics).
    - Behavior: `SkillBreakdownRow` decodes the spike breakdown dict; `medal` accepts `None`.
    - Source: `Weights` carries NO default values (defaults live in migration seed per SPEC req 5).
    - Test: `just lint-sdk` is clean.
  </acceptance_criteria>
  <done>The skill SDK module exports the four structs with shapes matching the D-09 config row and D-06 breakdown element; lint-sdk clean.</done>
</task>

<task type="auto">
  <name>Task 2: Add skill_score field to CommunityLeaderboardResponse</name>
  <files>libs/sdk/src/genjishimada_sdk/users.py</files>
  <read_first>
    - libs/sdk/src/genjishimada_sdk/users.py (lines 214-245) — CommunityLeaderboardResponse: the exact field list, the untouched `skill_rank: str` label field, and the trailing `total_results` field
    - .planning/phases/13-skill-score/13-PATTERNS.md (lines 407-420) — the one-field edit (skill_score non-optional float, name confirmed by D-08; the informal `score_skill` is wrong)
  </read_first>
  <action>
    Add a single field `skill_score: float` to `CommunityLeaderboardResponse` (D-08 — name is `skill_score`, NOT `score_skill`). Place it adjacent to the existing `skill_rank: str` field (which stays UNCHANGED — it is the Ninja→God label, SPEC req 6). Make `skill_score` non-optional `float` because the leaderboard SQL `COALESCE(ss.skill_score, 0)` (added in plan 13-06) guarantees a value for every row (D-07). Update the struct's `Attributes:` docstring block to document `skill_score` (numeric skill score) distinct from `skill_rank` (derived difficulty-tier label). Do NOT remove, rename, or reorder any existing field.
  </action>
  <verify>
    <automated>cd libs/sdk && uv run python -c "from genjishimada_sdk.users import CommunityLeaderboardResponse; import msgspec; fields={f.name for f in msgspec.structs.fields(CommunityLeaderboardResponse)}; assert 'skill_score' in fields and 'skill_rank' in fields, fields; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - Source: `CommunityLeaderboardResponse` contains both `skill_rank: str` (unchanged) and a new `skill_score: float`.
    - Behavior: `skill_score` is non-optional (no `| None`, no default) — SQL guarantees it via COALESCE.
    - Source: no existing field renamed/removed (skill_rank label preserved).
    - Test: `just lint-sdk` clean.
  </acceptance_criteria>
  <done>CommunityLeaderboardResponse gains a non-optional `skill_score: float` alongside the untouched `skill_rank` label.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → API (PATCH config) | SkillConfigUpdateRequest is the deserialization target for untrusted PATCH bodies; field typing is the first validation gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-03 | Tampering | SkillConfigUpdateRequest weight fields | mitigate | msgspec strict typing rejects non-float weight inputs at decode; semantic range validation (e.g. gamma ≥ 0.5) is enforced in the service/DB layers (plans 13-04/13-01 CHECK), not here |
| T-13-04 | Information Disclosure | SkillSummaryResponse / SkillBreakdownRow | accept | Skill data is public (leaderboard-visible); no PII beyond user_id, which is already public on the leaderboard |
| T-13-SC | Tampering | npm/pip/cargo installs | mitigate | No new package installs; nothing to verify |
</threat_model>

<verification>
- `skill.py` exports the four structs; all round-trip via msgspec.
- `CommunityLeaderboardResponse.skill_score` present and non-optional; `skill_rank` untouched.
- `just lint-sdk` clean.
</verification>

<success_criteria>
- [ ] `libs/sdk/src/genjishimada_sdk/skill.py` exports Weights, SkillConfigUpdateRequest, SkillSummaryResponse, SkillBreakdownRow
- [ ] Weights has no default values (SPEC req 5)
- [ ] CommunityLeaderboardResponse gains non-optional `skill_score: float`; skill_rank unchanged
- [ ] just lint-sdk clean
</success_criteria>

<output>
Create `.planning/phases/13-skill-score/13-02-SUMMARY.md` when done
</output>
