---
phase: 10-bot-slash-commands
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/bot/extensions/api_service.py
  - apps/bot/utilities/transformers.py
autonomous: true
requirements: [ADM-03]

must_haves:
  truths:
    - "The bot can call the new streak endpoint and all five reused tournament endpoints through typed APIService wrappers"
    - "D-09: A CategoryTransformer resolves a typed category_id and provides live (dynamic) autocomplete from the API list_categories endpoint"
    - "An admin invoking reroll triggers the correct API mutation (random reroll, or explicit choose-map when a code is given) via the bot wrappers"
  artifacts:
    - path: "apps/bot/extensions/api_service.py"
      provides: "get_tournament_streak, list_tournament_categories, list_tournament_cycles, get_tournament_leaderboard, reroll_next_cycle, choose_next_cycle"
      contains: "def get_tournament_streak"
    - path: "apps/bot/utilities/transformers.py"
      provides: "CategoryTransformer (transform + autocomplete)"
      contains: "class CategoryTransformer"
  key_links:
    - from: "apps/bot/utilities/transformers.py"
      to: "APIService.list_tournament_categories"
      via: "itx.client.api.list_tournament_categories()"
      pattern: "list_tournament_categories"
    - from: "apps/bot/extensions/api_service.py"
      to: "POST /tournaments/categories/{category_id}/reroll"
      via: "Route(\"POST\", ...)"
      pattern: "reroll"
---

<objective>
Add the bot-side building blocks the command Cog (Plan 10-03) composes: six thin `APIService`
wrappers (streak + the five reused tournament endpoints) and a `CategoryTransformer` that
powers the required `category` autocomplete (D-09) and resolves a typed `category_id`.

Purpose: isolate the pure HTTP-glue + transformer layer so it can be built in parallel with the
API streak endpoint (Plan 10-01) and lands BEFORE the command Cog that consumes it. The wrappers
are sync `def` methods returning `self._request(...)` (existing convention) — no `async def`.
Output: extended `api_service.py` and a new `CategoryTransformer` in `transformers.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-bot-slash-commands/10-CONTEXT.md
@.planning/phases/10-bot-slash-commands/10-RESEARCH.md
@.planning/phases/10-bot-slash-commands/10-PATTERNS.md

<interfaces>
Existing wrapper template — apps/bot/extensions/api_service.py:1667-1674:
  def get_tournament_category(self, category_id: int) -> Response[TournamentCategoryResponse]:
      r = Route("GET", "/tournaments/categories/{category_id}", category_id=category_id)
      return self._request(r, response_model=TournamentCategoryResponse)

Route/_request rules (api_service.py:160-580): path placeholders are Route(...) kwargs (url.format_map);
query filters go through params={...} (auto-skips None, flattens lists — see get_maps at :398);
request body via data=; response shape via response_model=.

Authoritative HTTP verbs (confirmed against apps/api/routes/v3/tournaments.py this phase):
  GET    /tournaments/categories                         -> list_categories
  GET    /tournaments/cycles?status=&category_id=&limit=&offset=  -> list_cycles -> TournamentCycleListResponse
  GET    /tournaments/cycles/{cycle_id}/leaderboard      -> get_leaderboard -> list[TournamentLeaderboardEntryResponse]
  POST   /tournaments/categories/{category_id}/reroll    -> reroll_map -> TournamentNextCycleResponse
  PATCH  /tournaments/categories/{category_id}/next-cycle -> choose_map -> TournamentNextCycleResponse
  GET    /tournaments/streaks/{user_id}                  -> get_streak (NEW, Plan 10-01) -> TournamentStreakResponse

SDK structs to import (libs/sdk/src/genjishimada_sdk/tournaments.py):
  TournamentStreakResponse, TournamentCategoryResponse, TournamentCycleListResponse,
  TournamentLeaderboardEntryResponse, TournamentNextCycleResponse, TournamentChooseMapRequest

UserTransformer analog — apps/bot/utilities/transformers.py:219-252 (transform digit-fast-path +
autocomplete returns list[app_commands.Choice[str]]). UserFacingError already imported at :9.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add six APIService tournament wrappers</name>
  <files>apps/bot/extensions/api_service.py</files>
  <read_first>
    - apps/bot/extensions/api_service.py:1642-1699 (get_tournament_category + neighbouring playtest wrappers: sync def returning self._request, data=/response_model= conventions)
    - apps/bot/extensions/api_service.py:340-410 (get_maps — params={...} usage for query filters that skip None)
    - apps/bot/extensions/api_service.py top import block (where SDK structs are imported for type hints)
    - .planning/phases/10-bot-slash-commands/10-PATTERNS.md (the wrappers table + Pattern 2 signatures)
  </read_first>
  <action>
    In apps/bot/extensions/api_service.py add six wrappers next to `get_tournament_category` (~:1667),
    each a sync `def` returning `self._request(...)` (NOT async — match the existing convention):
    - `get_tournament_streak(self, user_id: int) -> Response[TournamentStreakResponse]`:
      Route("GET", "/tournaments/streaks/{user_id}", user_id=user_id), response_model=TournamentStreakResponse.
    - `list_tournament_categories(self) -> Response[list[TournamentCategoryResponse]]`:
      Route("GET", "/tournaments/categories"), response_model=list[TournamentCategoryResponse].
    - `list_tournament_cycles(self, *, status: str | None = None, category_id: int | None = None, limit: int = 20, offset: int = 0) -> Response[TournamentCycleListResponse]`:
      Route("GET", "/tournaments/cycles"); pass filters via params={"status": status, "category_id": category_id, "limit": limit, "offset": offset} (params auto-skips None); response_model=TournamentCycleListResponse.
    - `get_tournament_leaderboard(self, cycle_id: int) -> Response[list[TournamentLeaderboardEntryResponse]]`:
      Route("GET", "/tournaments/cycles/{cycle_id}/leaderboard", cycle_id=cycle_id), response_model=list[TournamentLeaderboardEntryResponse].
    - `reroll_next_cycle(self, category_id: int) -> Response[TournamentNextCycleResponse]`:
      Route("POST", "/tournaments/categories/{category_id}/reroll", category_id=category_id), response_model=TournamentNextCycleResponse. VERB = POST (confirmed: tournaments.py:345).
    - `choose_next_cycle(self, category_id: int, data: TournamentChooseMapRequest) -> Response[TournamentNextCycleResponse]`:
      Route("PATCH", "/tournaments/categories/{category_id}/next-cycle", category_id=category_id), response_model=TournamentNextCycleResponse, data=data. VERB = PATCH (confirmed: tournaments.py:387).
    Add the six SDK struct names to the file's tournaments-SDK import. Google-style docstrings on each
    (D rules); full type annotations (ANN rules).
  </action>
  <verify>
    <automated>uv run --directory apps/bot ruff check extensions/api_service.py && uv run --directory apps/bot basedpyright extensions/api_service.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "def (get_tournament_streak|list_tournament_categories|list_tournament_cycles|get_tournament_leaderboard|reroll_next_cycle|choose_next_cycle)" apps/bot/extensions/api_service.py` returns six matches
    - `grep -n 'Route("POST", "/tournaments/categories/{category_id}/reroll"' apps/bot/extensions/api_service.py` matches (POST verb)
    - `grep -n 'Route("PATCH", "/tournaments/categories/{category_id}/next-cycle"' apps/bot/extensions/api_service.py` matches (PATCH verb)
    - No wrapper uses `async def` (source assertion: the six new methods return `self._request(...)`)
    - ruff + basedpyright on api_service.py pass
  </acceptance_criteria>
  <done>Six typed wrappers exist with the correct verbs/paths/response models; lint + type-check clean.</done>
</task>

<task type="auto">
  <name>Task 2: Add CategoryTransformer (autocomplete + transform)</name>
  <files>apps/bot/utilities/transformers.py</files>
  <read_first>
    - apps/bot/utilities/transformers.py:219-252 (UserTransformer — exact two-method shape to mirror)
    - apps/bot/utilities/transformers.py:1-15 (imports: app_commands, UserFacingError at :9, TYPE_CHECKING/GenjiItx)
    - apps/bot/extensions/api_service.py (the list_tournament_categories wrapper added in Task 1 — return shape)
    - .planning/phases/10-bot-slash-commands/10-PATTERNS.md (CategoryTransformer section + Pitfall 3: 25-choice slice)
  </read_first>
  <action>
    In apps/bot/utilities/transformers.py add `class CategoryTransformer(app_commands.Transformer)`
    mirroring `UserTransformer`. `autocomplete(self, itx, current) -> list[app_commands.Choice[str]]`:
    `cats = await itx.client.api.list_tournament_categories()`; case-fold substring filter on `c.name`;
    return `app_commands.Choice(name=c.name, value=str(c.id))` for matches, sliced `[:25]` (Discord hard
    limit — Pitfall 3). `transform(self, itx, value) -> int`: if `value.isdigit()` return `int(value)`
    (digit fast-path, as UserTransformer does); else re-fetch categories and resolve a case-fold name
    match → return `match.id`; if no match raise `UserFacingError(f"Unknown category: {value}")`. Per-keystroke
    API calls with no cache is consistent with UserTransformer (A4) — do not add caching. Google-style
    docstrings with Raises clause; full annotations. Import GenjiItx via the file's existing TYPE_CHECKING
    pattern if not already present.
  </action>
  <verify>
    <automated>uv run --directory apps/bot ruff check utilities/transformers.py && uv run --directory apps/bot basedpyright utilities/transformers.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "class CategoryTransformer" apps/bot/utilities/transformers.py` returns a match
    - `grep -n "list_tournament_categories" apps/bot/utilities/transformers.py` shows autocomplete calls the Task-1 wrapper
    - `grep -n "\[:25\]" apps/bot/utilities/transformers.py` shows the 25-choice slice (Pitfall 3)
    - `grep -n "Unknown category" apps/bot/utilities/transformers.py` shows the UserFacingError raise on a name miss
    - ruff + basedpyright on transformers.py pass
    - Manual-Only (VALIDATION.md): in the dev guild, typing a partial category in `/tournament info` lists live admin-created categories (no Discord interaction test harness exists)
  </acceptance_criteria>
  <done>CategoryTransformer resolves category_id (digit fast-path + name lookup), autocompletes from the live API capped at 25, raises UserFacingError on unknown name; lint + type-check clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Discord user input → CategoryTransformer | Free-typed category text; transform validates against the live API list |
| bot → API (X-API-KEY) | Bot calls the API with its single full-scope key |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-04 | Tampering | CategoryTransformer.transform free-text value | mitigate | Value is resolved against the live `list_tournament_categories` result; an unmatched name raises `UserFacingError` rather than passing an arbitrary id downstream. Digit fast-path returns an int only. |
| T-10-05 | Spoofing/Injection | category names rendered later | accept-here | Wrappers/transformer do not render names into embeds; mention-injection mitigation (numeric `<@id>`) is enforced in Plan 10-03 where rendering happens. |
| T-10-06 | Elevation of Privilege | reroll/choose wrappers (tournaments:write) | note | These wrappers can mutate, but audience restriction (admin-only) is NOT in scope here — it is enforced by the bot-side Mod/Sensei gate in Plan 10-03 (the bot key carries full scopes, so API scope alone does not restrict callers). Wrappers are inert until the gated command calls them. |
| T-10-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs (existing deps only); RESEARCH Package Legitimacy Audit confirms zero new packages. |
</threat_model>

<verification>
- `uv run --directory apps/bot ruff check extensions/api_service.py utilities/transformers.py` clean
- `uv run --directory apps/bot basedpyright extensions/api_service.py utilities/transformers.py` clean
- Wrapper verbs/paths match the live controller (grep assertions above)
</verification>

<success_criteria>
- Six typed tournament APIService wrappers exist with correct verbs (POST reroll, PATCH next-cycle) and response models
- CategoryTransformer provides live autocomplete (≤25) and resolves a typed category_id, raising UserFacingError on unknown name
- Bot lint + type-check clean for both files
</success_criteria>

<output>
Create `.planning/phases/10-bot-slash-commands/10-02-SUMMARY.md` when done
</output>
</content>
