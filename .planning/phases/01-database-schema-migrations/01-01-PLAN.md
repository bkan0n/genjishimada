---
phase: 01-database-schema-migrations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/migrations/0020_tournaments.sql
  - apps/api/tests/integration/test_tournaments_schema.py
autonomous: true
requirements:
  - "foundation -- enables all requirements"

must_haves:
  truths:
    - "D-07: The tournaments schema exists in PostgreSQL with 6 tables: config, categories, cycles, completions, streaks, pending_transitions"
    - "The migration runs cleanly against the existing database without conflicts with other schemas"
    - "D-01: Tournament completions use boolean verified/video/completion columns matching core.completions pattern"
    - "D-02: Tier-then-time ranking derived at query time via ORDER BY verified DESC, time ASC"
    - "D-03: Map cooldown is global — derived from cycle history across all categories"
    - "D-04: No blacklist table — cooldown uses blacklist_weeks config against tournaments.cycles.started_at"
    - "D-05: XP configuration columns (participation_xp, placement_xp, streak_xp) are on tournaments.categories, not tournaments.config"
    - "D-06: tournaments.config singleton holds only global settings (blacklist_weeks), no XP values"
    - "D-08: A CHECK(id = 1) singleton constraint exists on tournaments.config"
    - "D-09: core.completions has a nullable tournament_completion_id FK column referencing tournaments.completions(id) ON DELETE SET NULL"
    - "D-10: Migration file numbered 0020_tournaments.sql"
    - "Foreign key relationships correctly reference core.users(id) as bigint and core.maps(id) as int"
    - "Every FK column has a corresponding explicit index"
  artifacts:
    - path: "apps/api/migrations/0020_tournaments.sql"
      provides: "Tournament schema DDL migration"
      contains: "CREATE SCHEMA IF NOT EXISTS tournaments"
    - path: "apps/api/tests/integration/test_tournaments_schema.py"
      provides: "Schema verification integration tests"
      contains: "test_tournaments_schema_exists"
  key_links:
    - from: "tournaments.completions"
      to: "core.completions"
      via: "tournament_completion_id FK"
      pattern: "tournament_completion_id.*REFERENCES tournaments.completions"
    - from: "tournaments.cycles"
      to: "tournaments.categories"
      via: "category_id FK"
      pattern: "category_id.*REFERENCES tournaments.categories"
    - from: "tournaments.cycles"
      to: "core.maps"
      via: "map_id FK with ON DELETE RESTRICT"
      pattern: "map_id.*REFERENCES core.maps.*ON DELETE RESTRICT"
    - from: "tournaments.completions"
      to: "core.users"
      via: "user_id FK"
      pattern: "user_id.*bigint.*REFERENCES core.users"
---

<objective>
Create the `tournaments` PostgreSQL schema as a single migration file containing all tables, constraints, indexes, comments, the singleton config insert, and the ALTER TABLE on `core.completions`. Verify correctness with integration tests.

Purpose: This is the foundation phase -- every downstream layer (SDK types, repository, service, controllers, bot consumers) depends on these tables existing with the correct structure, constraints, and relationships.

Output: `apps/api/migrations/0020_tournaments.sql` (migration) and `apps/api/tests/integration/test_tournaments_schema.py` (verification)
</objective>

<execution_context>
@/Users/nebula/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nebula/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/nebula/Documents/coding/parkour/genji/genjishimada/.planning/ROADMAP.md
@/Users/nebula/Documents/coding/parkour/genji/genjishimada/.planning/STATE.md
@/Users/nebula/Documents/coding/parkour/genji/genjishimada/.planning/phases/01-database-schema-migrations/01-CONTEXT.md
@/Users/nebula/Documents/coding/parkour/genji/genjishimada/.planning/phases/01-database-schema-migrations/01-RESEARCH.md
@/Users/nebula/Documents/coding/parkour/genji/genjishimada/.planning/phases/01-database-schema-migrations/01-PATTERNS.md

<interfaces>
<!-- No application-level interfaces in this phase -- pure DDL migration -->
<!-- The conftest.py auto-applies all migrations via sorted glob of apps/api/migrations/*.sql -->
<!-- Integration tests use the pytest-databases postgres_service fixture for async connection -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the 0020_tournaments.sql migration</name>
  <files>apps/api/migrations/0020_tournaments.sql</files>
  <read_first>
    - apps/api/migrations/0018_movement_techniques.sql (structure analog: BEGIN/COMMIT wrapping, CREATE SCHEMA IF NOT EXISTS, multi-table migration, IF NOT EXISTS on tables/indexes)
    - apps/api/migrations/0013_coin_store.sql (singleton config analog: CHECK(id=1), GENERATED ALWAYS AS IDENTITY, OVERRIDING SYSTEM VALUE insert, COMMENT ON patterns)
    - apps/api/migrations/0019_release_map_code.sql (ALTER TABLE analog: ADD COLUMN on existing core table)
    - apps/api/migrations/0001_init.sql lines 447-487 (core.completions definition: column types, FK patterns, index naming, COMMENT ON)
    - .planning/phases/01-database-schema-migrations/01-RESEARCH.md (complete table definitions in Code Examples section)
    - .planning/phases/01-database-schema-migrations/01-PATTERNS.md (pattern assignments with exact SQL excerpts from analogs)
    - .planning/phases/01-database-schema-migrations/01-CONTEXT.md (locked decisions D-01 through D-10)
  </read_first>
  <action>
    Create `apps/api/migrations/0020_tournaments.sql` as a single transactional migration following the 0018 structure pattern. The file structure is:

    1. File header comment block: migration name "Add tournaments schema", description listing all tables created and the ALTER TABLE, date.

    2. `BEGIN;`

    3. `CREATE SCHEMA IF NOT EXISTS tournaments;` (per D-07)

    4. `tournaments.config` table (per D-06, D-08): singleton with `CHECK(id = 1)`, columns `id` (int GENERATED ALWAYS AS IDENTITY PRIMARY KEY), `blacklist_weeks` (int NOT NULL DEFAULT 4 CHECK > 0), `created_at` (timestamptz NOT NULL DEFAULT now()), `updated_at` (timestamptz NOT NULL DEFAULT now()). Add COMMENT ON TABLE and COMMENT ON COLUMN for `blacklist_weeks`.

    5. `tournaments.categories` table (per D-05): columns `id` (int identity PK), `name` (text NOT NULL UNIQUE), `difficulties` (text[] NOT NULL), `cycle_frequency` (text NOT NULL DEFAULT 'weekly' CHECK IN ('weekly', 'biweekly')), `participation_xp` (int NOT NULL DEFAULT 0), `placement_xp` (jsonb NOT NULL DEFAULT '[]'::jsonb), `streak_xp` (jsonb NOT NULL DEFAULT '[]'::jsonb), `champion_role_id` (bigint, nullable), `is_active` (boolean NOT NULL DEFAULT TRUE), `created_at`, `updated_at`. Add COMMENT ON TABLE and COMMENT ON COLUMNs for `difficulties`, `cycle_frequency`, `participation_xp`, `placement_xp`, `streak_xp`, `champion_role_id`.

    6. `tournaments.cycles` table: columns `id` (int identity PK), `category_id` (int NOT NULL REFERENCES tournaments.categories(id) ON DELETE CASCADE), `map_id` (int NOT NULL REFERENCES core.maps(id) ON DELETE RESTRICT), `status` (text NOT NULL DEFAULT 'pending' CHECK IN ('pending', 'active', 'finalizing', 'completed')), `started_at` (timestamptz, nullable), `ended_at` (timestamptz, nullable), `created_at` (timestamptz NOT NULL DEFAULT now()). Indexes: `idx_cycles_category_id`, `idx_cycles_map_id`, `idx_cycles_status`, `idx_cycles_category_status` (composite on category_id, status), `idx_cycles_started_at`. Add COMMENT ON TABLE and COMMENT ON COLUMNs for `status`, `started_at`, `ended_at`.

    7. `tournaments.completions` table (per D-01, D-02): columns `id` (int identity PK), `cycle_id` (int NOT NULL REFERENCES tournaments.cycles(id) ON DELETE CASCADE), `user_id` (bigint NOT NULL REFERENCES core.users(id) ON DELETE CASCADE), `map_id` (int NOT NULL REFERENCES core.maps(id) ON DELETE CASCADE), `time` (numeric(10,2) NOT NULL), `screenshot` (text NOT NULL), `video` (text, nullable), `verified` (boolean NOT NULL DEFAULT FALSE), `completion` (boolean NOT NULL DEFAULT FALSE), `inserted_at` (timestamptz NOT NULL DEFAULT now()), UNIQUE (cycle_id, user_id, inserted_at). Indexes: `idx_tournament_completions_cycle_id`, `idx_tournament_completions_user_id`, `idx_tournament_completions_map_id`, `idx_tournament_completions_cycle_user` (composite on cycle_id, user_id), `idx_tournament_completions_ranking` (composite on cycle_id, verified DESC, time ASC). Add COMMENT ON TABLE and COMMENT ON COLUMNs for `verified`, `video`, `completion`.

    8. `tournaments.streaks` table: columns `id` (int identity PK), `user_id` (bigint NOT NULL REFERENCES core.users(id) ON DELETE CASCADE), `current_streak` (int NOT NULL DEFAULT 0), `max_streak` (int NOT NULL DEFAULT 0), `last_cycle_id` (int REFERENCES tournaments.cycles(id) ON DELETE SET NULL, nullable), `updated_at` (timestamptz NOT NULL DEFAULT now()). UNIQUE INDEX `idx_streaks_user_id` on (user_id). Add COMMENT ON TABLE and COMMENT ON COLUMNs for `current_streak`, `max_streak`, `last_cycle_id`.

    9. `tournaments.pending_transitions` table: columns `id` (int identity PK), `cycle_id` (int NOT NULL REFERENCES tournaments.cycles(id) ON DELETE CASCADE), `event_type` (text NOT NULL CHECK IN ('cycle_started', 'cycle_completed')), `payload` (jsonb NOT NULL DEFAULT '{}'::jsonb), `published` (boolean NOT NULL DEFAULT FALSE), `created_at` (timestamptz NOT NULL DEFAULT now()). Indexes: `idx_pending_transitions_unpublished` (partial index on published, created_at WHERE published = FALSE), `idx_pending_transitions_cycle_id`. Add COMMENT ON TABLE and COMMENT ON COLUMNs for `published`.

    10. Singleton config INSERT following the store.config pattern exactly: `INSERT INTO tournaments.config (id, blacklist_weeks) OVERRIDING SYSTEM VALUE VALUES (1, 4) ON CONFLICT (id) DO NOTHING;`

    11. ALTER TABLE core.completions (per D-09): `ADD COLUMN tournament_completion_id int REFERENCES tournaments.completions(id) ON DELETE SET NULL`. The column MUST be nullable with no DEFAULT (metadata-only operation). Add partial index `idx_completions_tournament_completion_id` on core.completions (tournament_completion_id) WHERE tournament_completion_id IS NOT NULL. Add COMMENT ON COLUMN.

    12. `COMMIT;`

    Use `CREATE TABLE IF NOT EXISTS` and `CREATE INDEX IF NOT EXISTS` following the 0018 pattern. Use `text CHECK(... IN (...))` for status/type columns, never CREATE TYPE AS ENUM. All FK columns to core.users must be `bigint`, all FK columns to core.maps must be `int`. Every FK column must have an explicit CREATE INDEX.
  </action>
  <verify>
    <automated>cd /Users/nebula/Documents/coding/parkour/genji/genjishimada && grep -c "CREATE TABLE" apps/api/migrations/0020_tournaments.sql | grep -q "6" && grep -c "CREATE INDEX" apps/api/migrations/0020_tournaments.sql && grep -q "BEGIN;" apps/api/migrations/0020_tournaments.sql && grep -q "COMMIT;" apps/api/migrations/0020_tournaments.sql && grep -q "CHECK (id = 1)" apps/api/migrations/0020_tournaments.sql && grep -q "ALTER TABLE core.completions" apps/api/migrations/0020_tournaments.sql && grep -q "OVERRIDING SYSTEM VALUE" apps/api/migrations/0020_tournaments.sql && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - File `apps/api/migrations/0020_tournaments.sql` exists
    - File begins with a comment header block and `BEGIN;`, ends with `COMMIT;`
    - Contains exactly 6 CREATE TABLE statements: tournaments.config, tournaments.categories, tournaments.cycles, tournaments.completions, tournaments.streaks, tournaments.pending_transitions
    - tournaments.config has `CHECK (id = 1)` constraint
    - tournaments.categories has columns `participation_xp`, `placement_xp`, `streak_xp` (XP is per-category per D-05)
    - tournaments.config does NOT have any XP columns (per D-06)
    - tournaments.completions has `verified boolean`, `video text`, `completion boolean` columns matching core.completions pattern (per D-01)
    - tournaments.cycles.status uses `text CHECK(status IN (...))`, not an ENUM type
    - All FK columns to core.users use `bigint`, all FK columns to core.maps use `int`
    - Every FK column has a corresponding CREATE INDEX statement
    - ALTER TABLE core.completions ADD COLUMN tournament_completion_id is nullable with no DEFAULT
    - Singleton INSERT uses OVERRIDING SYSTEM VALUE with ON CONFLICT DO NOTHING
    - COMMENT ON statements present for all tables and significant columns
  </acceptance_criteria>
  <done>Migration file is complete with all 6 tables, constraints, indexes, comments, singleton insert, and ALTER TABLE. File follows all established codebase patterns from analog migrations.</done>
</task>

<task type="auto">
  <name>Task 2: Write integration tests for tournament schema</name>
  <files>apps/api/tests/integration/test_tournaments_schema.py</files>
  <read_first>
    - apps/api/tests/integration/test_tournaments_schema.py (will not exist yet -- this is the file being created)
    - apps/api/conftest.py (migration application mechanism: setup_test_db fixture applies all migrations via sorted glob)
    - apps/api/tests/integration/test_store_integration.py (analog: integration test pattern for another schema with singleton config)
    - apps/api/tests/integration/conftest.py (integration-specific fixtures)
    - apps/api/migrations/0020_tournaments.sql (the migration being tested -- created in Task 1)
  </read_first>
  <action>
    Create `apps/api/tests/integration/test_tournaments_schema.py` with async integration tests that verify the migration applied correctly. Use the existing `asyncpg_pool` fixture from conftest.py (session-scoped asyncpg pool connected to the test database where all migrations have already been applied by `setup_test_db`).

    Tests to write (all async, using `async with pool.acquire() as conn`):

    1. `test_tournaments_schema_exists`: Query `information_schema.schemata` WHERE `schema_name = 'tournaments'`, assert exactly 1 row returned.

    2. `test_tournaments_tables_exist`: Query `information_schema.tables` WHERE `table_schema = 'tournaments'`, collect all `table_name` values, assert the set equals `{'config', 'categories', 'cycles', 'completions', 'streaks', 'pending_transitions'}`.

    3. `test_config_singleton_constraint`: INSERT a row with `id = 2` into `tournaments.config` using `OVERRIDING SYSTEM VALUE`, expect `asyncpg.CheckViolationError`. Use try/except to catch the specific error and assert it was raised. Use a SAVEPOINT before the INSERT and ROLLBACK TO SAVEPOINT after, to avoid breaking the transaction.

    4. `test_core_completions_tournament_column_exists`: Query `information_schema.columns` WHERE `table_schema = 'core'` AND `table_name = 'completions'` AND `column_name = 'tournament_completion_id'`, assert 1 row returned. Also verify `is_nullable = 'YES'` and `column_default IS NULL`.

    5. `test_categories_has_xp_columns`: Query `information_schema.columns` WHERE `table_schema = 'tournaments'` AND `table_name = 'categories'`, collect column names, assert `participation_xp`, `placement_xp`, `streak_xp` are all present.

    6. `test_config_has_no_xp_columns`: Query `information_schema.columns` WHERE `table_schema = 'tournaments'` AND `table_name = 'config'`, collect column names, assert none of `participation_xp`, `placement_xp`, `streak_xp` are present (per D-05/D-06 boundary).

    7. `test_cycles_status_check_constraint`: INSERT a cycle row with `status = 'invalid_status'` (requires a category and map to exist first -- use existing seed data or create minimal test data), expect `asyncpg.CheckViolationError`. Use SAVEPOINT pattern.

    8. `test_config_singleton_seeded`: Query `SELECT blacklist_weeks FROM tournaments.config WHERE id = 1`, assert the row exists and `blacklist_weeks = 4`.

    9. `test_foreign_key_indexes_exist`: Query `pg_indexes` WHERE `schemaname = 'tournaments'`, collect all `indexname` values, assert the following indexes exist: `idx_cycles_category_id`, `idx_cycles_map_id`, `idx_tournament_completions_cycle_id`, `idx_tournament_completions_user_id`, `idx_tournament_completions_map_id`, `idx_streaks_user_id`, `idx_pending_transitions_cycle_id`.

    Use `import asyncpg` for exception types. Use `import pytest` with `pytestmark = pytest.mark.asyncio` at module level. Do not import from the application code -- these tests query the database schema directly via SQL.
  </action>
  <verify>
    <automated>cd /Users/nebula/Documents/coding/parkour/genji/genjishimada && uv run --directory apps/api pytest tests/integration/test_tournaments_schema.py -v -p no:xdist 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File `apps/api/tests/integration/test_tournaments_schema.py` exists
    - Contains at least 9 test functions covering: schema existence, table existence, singleton constraint, core.completions ALTER TABLE, XP column placement (on categories, not on config), status check constraint, config seed data, FK indexes
    - All tests pass when run with `uv run --directory apps/api pytest tests/integration/test_tournaments_schema.py -v -p no:xdist`
    - Tests use `asyncpg_pool` fixture (session-scoped) and `information_schema` / `pg_indexes` queries for schema introspection
    - No application code imports -- pure database introspection tests
  </acceptance_criteria>
  <done>All integration tests pass, confirming the tournament schema migration was applied correctly with all tables, constraints, indexes, singleton config, and the core.completions ALTER TABLE.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Migration file -> PostgreSQL | DDL statements are code-reviewed, not user-supplied input |
| Test assertions -> Database | Tests read schema metadata, no mutation risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | tournaments.config singleton | mitigate | CHECK(id = 1) constraint enforced at DB level prevents multi-row config tampering |
| T-01-02 | Tampering | tournaments.cycles.status | mitigate | CHECK constraint restricts status to valid values ('pending', 'active', 'finalizing', 'completed') |
| T-01-03 | Denial of Service | core.completions ALTER TABLE | mitigate | Column is nullable with no DEFAULT -- metadata-only operation, no table rewrite, near-instant |
| T-01-04 | Elevation of Privilege | FK constraint bypass | mitigate | ON DELETE RESTRICT on cycles.map_id prevents map deletion while used in tournament; ON DELETE CASCADE on user-facing FKs preserves referential integrity |
| T-01-05 | Information Disclosure | XP config placement | accept | XP values are non-sensitive game configuration; correct placement (per-category) is a data modeling concern, not a security concern |
</threat_model>

<verification>
1. Migration file exists at `apps/api/migrations/0020_tournaments.sql`
2. Migration contains BEGIN/COMMIT transaction wrapper
3. All 6 tables created in tournaments schema
4. Singleton config constraint and seed data present
5. ALTER TABLE on core.completions adds nullable FK column
6. All integration tests pass: `uv run --directory apps/api pytest tests/integration/test_tournaments_schema.py -v -p no:xdist`
7. Full test suite still passes: `just test-api`
</verification>

<success_criteria>
- The `tournaments` schema exists with 6 tables: config, categories, cycles, completions, streaks, pending_transitions
- The migration runs cleanly without conflicts with existing schemas (core, maps, store, content, etc.)
- FK relationships correctly reference core.users(id) as bigint and core.maps(id) as int
- CHECK(id = 1) singleton constraint on tournaments.config works (insert id=2 fails)
- core.completions has nullable tournament_completion_id FK with no DEFAULT
- XP columns are on categories (per D-05), not on config (per D-06)
- All FK columns have explicit indexes
- Singleton config row seeded with blacklist_weeks = 4
- Integration tests verify all of the above automatically
- Existing test suite (`just test-api`) continues to pass
</success_criteria>

<output>
Create `.planning/phases/01-database-schema-migrations/01-01-SUMMARY.md` when done
</output>
