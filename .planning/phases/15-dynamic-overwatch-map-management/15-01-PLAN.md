---
phase: 15-dynamic-overwatch-map-management
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - libs/sdk/src/genjishimada_sdk/maps.py
  - apps/api/tests/conftest.py
  - apps/api/tests/repository/maps/test_maps_repository_create_core_map.py
  - apps/api/tests/repository/maps/test_maps_repository_update_core_map.py
  - apps/api/tests/repository/maps/test_maps_repository_fetch_partial_map.py
  - apps/api/tests/repository/maps/test_maps_repository_advanced_operations.py
  - apps/api/tests/repository/maps/test_maps_repository_entity_operations.py
  - apps/api/tests/repository/maps/test_maps_repository_check_code_exists.py
  - apps/api/tests/repository/maps/test_maps_repository_guide_operations.py
  - apps/api/tests/repository/community/test_community_repository_popular.py
autonomous: true
requirements: [REQ-01, D-04]
must_haves:
  truths:
    - "msgspec accepts a map name absent from the old 70-entry Literal at the request boundary (the enum gate is gone)"
    - "The full API test suite runs after the flip — no test errors from get_args(str) returning ()"
    - "MapCategory and every other Literal still validates strictly (only the map-name Literal was relaxed)"
  artifacts:
    - path: "libs/sdk/src/genjishimada_sdk/maps.py"
      provides: "OverwatchMap aliased to str; MapCategory/Mechanics/Restrictions/Tags/Difficulty Literals untouched"
      contains: "OverwatchMap = str"
    - path: "apps/api/tests/conftest.py"
      provides: "Module-level seed-name constant replacing get_args(OverwatchMap) in the map factory"
  key_links:
    - from: "libs/sdk/src/genjishimada_sdk/maps.py"
      to: "all 27 OverwatchMap consumers"
      via: "type alias OverwatchMap = str (name kept exported in __all__)"
      pattern: "OverwatchMap = str"
    - from: "apps/api/tests fixtures"
      to: "maps.names seed rows"
      via: "_SEED_MAP_NAMES constant fed to fake.random_element"
      pattern: "_SEED_MAP_NAMES"
---

<objective>
Flip `OverwatchMap = Literal[...]` to `OverwatchMap = str` in the SDK (REQ-01) and, in the SAME wave/plan, repair every test fixture that calls `get_args(OverwatchMap)` so the suite still runs. After the flip `get_args(str)` returns `()`, which makes `fake.random_element([])` raise and blocks the WHOLE API suite — so the two changes are inseparable.

Purpose: Move the map-name validation gate off the type system and onto the database. This is the crux that lets a single `INSERT INTO maps.names` make a new map appear everywhere with no redeploy. Implements D-04's request-side requirement (`name: str` is accepted) by removing the enum constraint.

Output: A one-line SDK type-alias edit (keeping `OverwatchMap` exported so all ~27 consumers keep compiling untouched) plus a module-level seed-name constant in 8 test files + `conftest.py` (9 sites total — see Task 2 note on the RESEARCH "14" overcount).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-dynamic-overwatch-map-management/15-CONTEXT.md
@.planning/phases/15-dynamic-overwatch-map-management/15-RESEARCH.md
@.planning/phases/15-dynamic-overwatch-map-management/15-PATTERNS.md

<interfaces>
<!-- The crux edit, extracted from libs/sdk/src/genjishimada_sdk/maps.py:107-178 -->
<!-- CURRENT: OverwatchMap = Literal[ "Circuit Royal", ... 70 entries ... ] -->
<!-- TARGET:  OverwatchMap = str -->
<!-- Keep the name `OverwatchMap` defined and exported (it is in __all__ and imported by ~27 files). -->
<!-- LEAVE UNTOUCHED on the same/adjacent lines: MapCategory (maps.py:101), Mechanics, Restrictions, Tags, DifficultyAll Literals. -->

<!-- get_map_banner already takes str — no change (maps.py:1013):
def get_map_banner(map_name: str) -> str:
    _map = re.sub(r"[^a-zA-Z0-9]", "", map_name)
    sanitized_name = _map.lower().strip().replace(" ", "")
    return f"https://cdn.genji.pk/assets/map_banners/{sanitized_name}.png"
-->

<!-- Test fixture pattern to replace (conftest.py:345-346 + 8 files):
    from typing import get_args
    from genjishimada_sdk.maps import MapCategory, OverwatchMap
    ...
    "map_name": fake.random_element(elements=get_args(OverwatchMap)),
    "category": fake.random_element(elements=get_args(MapCategory)),  # <- STAYS (MapCategory is still a Literal)
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flip OverwatchMap Literal to str in the SDK</name>
  <read_first>
    - libs/sdk/src/genjishimada_sdk/maps.py (read :100-178 for the Literal block, :40-60 for __all__, :1013-1017 for get_map_banner)
    - .planning/phases/15-dynamic-overwatch-map-management/15-RESEARCH.md (Blast-Radius §A/B, Assumption A1)
  </read_first>
  <action>
    In `libs/sdk/src/genjishimada_sdk/maps.py` replace the 70-entry `OverwatchMap = Literal[ ... ]` block (lines ~107-178) with a single statement `OverwatchMap = str`. Keep the `OverwatchMap` name defined and in `__all__` so the ~27 consumer modules (SDK struct fields, API repo/route/service hints, bot hints) keep compiling with zero edits — this is the minimal-churn aliasing approach (Assumption A1).

    BEFORE editing, copy the 70 map-name string literals out of the Literal verbatim into the plan SUMMARY (or a scratch note) — plan 15-02 needs that exact list to rewrite the `0001_init.sql` seed. The 70 names include the 7 phantom maps (Arena Victoriae, Redwood Dam, Thames District, Gogadoro, Powder Keg Mine, Place Lacroix, Neon Junction) already present in the Literal.

    DO NOT touch `MapCategory` (maps.py:101), `Mechanics`, `Restrictions`, `Tags`, or `DifficultyAll` — they stay strict Literals. DO NOT touch `get_map_banner()` — it already takes `str`.

    After the edit, run `just sync` (or `just fix`) so the editable workspace SDK is reinstalled and both API and bot pick up the new type (Runtime State Inventory: SDK is a workspace package).
  </action>
  <verify>
    <automated>uv run python -c "from typing import get_args; from genjishimada_sdk.maps import OverwatchMap, MapCategory; assert OverwatchMap is str, 'OverwatchMap not str'; assert len(get_args(MapCategory)) == 3, 'MapCategory regressed'; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: `grep -n "OverwatchMap = str" libs/sdk/src/genjishimada_sdk/maps.py` returns exactly one match.
    - SOURCE: `grep -c "OverwatchMap = Literal" libs/sdk/src/genjishimada_sdk/maps.py` returns 0.
    - BEHAVIOR: `OverwatchMap is str` is True; `get_args(MapCategory)` still returns the 3 category strings (no regression).
    - SOURCE: the 70 verbatim map names are recorded in the SUMMARY for plan 15-02 to consume.
  </acceptance_criteria>
  <done>OverwatchMap resolves to str; MapCategory and the other Literals are unchanged; the 70 names are captured for the seed rewrite.</done>
</task>

<task type="auto">
  <name>Task 2: Replace get_args(OverwatchMap) with a seed-name constant in all 9 test-fixture sites</name>
  <read_first>
    - apps/api/tests/conftest.py (read :338-360 — the map factory using get_args(OverwatchMap) at :346)
    - apps/api/tests/repository/maps/test_maps_repository_create_core_map.py (the canonical fixture analog, the `get_args(OverwatchMap)` line)
    - .planning/phases/15-dynamic-overwatch-map-management/15-PATTERNS.md (the `apps/api/tests/**` assignment — _SEED_MAP_NAMES shape)
  </read_first>
  <action>
    In each of these 9 sites, replace `fake.random_element(elements=get_args(OverwatchMap))` with `fake.random_element(elements=_SEED_MAP_NAMES)`, where `_SEED_MAP_NAMES` is a module-level constant list of real seeded `maps.names` rows (e.g. `_SEED_MAP_NAMES = ["Hanamura", "Busan", "Ilios", "Nepal", "Oasis"]` — any subset known to be in the seed). Add the constant at module top in each file (or import a shared one from conftest if cleaner). Remove the now-unused `OverwatchMap` import and `get_args` import ONLY where `get_args` is no longer used for anything else.

    KEEP every `get_args(MapCategory)` / `get_args(PlaytestStatus)` call on the same or adjacent lines untouched — those remain strict Literals (PATTERNS: create_core_map.py:94,97).

    File-list provenance (resolves the RESEARCH "14" discrepancy): RESEARCH §F (line 411) says "14 test files", but a fresh live grep of the working tree — `grep -rln 'get_args(OverwatchMap)' apps/api/tests/` — returns exactly 8 files, plus `conftest.py` = **9 sites total**, and ZERO under `apps/bot/tests/` (the bot has no `get_args(OverwatchMap)` fixtures). The RESEARCH "14" was an overcount; the 9 sites below are EXHAUSTIVE. The zero-assertion grep in `<verify>` plus the full `just test-api` run are the backstop: if a site was missed, the suite errors on `random_element([])` and fails the acceptance criteria.

    The 9 sites (exhaustive per live grep):
    - apps/api/tests/conftest.py (:345-346 import + factory use)
    - apps/api/tests/repository/maps/test_maps_repository_create_core_map.py
    - apps/api/tests/repository/maps/test_maps_repository_update_core_map.py
    - apps/api/tests/repository/maps/test_maps_repository_fetch_partial_map.py
    - apps/api/tests/repository/maps/test_maps_repository_advanced_operations.py
    - apps/api/tests/repository/maps/test_maps_repository_entity_operations.py
    - apps/api/tests/repository/maps/test_maps_repository_check_code_exists.py
    - apps/api/tests/repository/maps/test_maps_repository_guide_operations.py
    - apps/api/tests/repository/community/test_community_repository_popular.py

    Note: `tests/integration/test_maps_integration.py:881` uses the string literal `"Nepal"` — no change needed there.
  </action>
  <verify>
    <automated>cd apps/api && [ "$(grep -rln 'get_args(OverwatchMap)' tests/ | wc -l)" -eq 0 ] && echo "zero get_args(OverwatchMap) sites remain" && uv run pytest tests/repository/maps/test_maps_repository_create_core_map.py tests/repository/community/test_community_repository_popular.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: `[ "$(grep -rln 'get_args(OverwatchMap)' apps/api/tests/ | wc -l)" -eq 0 ]` (zero files reference it — this is the backstop for any missed site).
    - SOURCE: `grep -rl "get_args(MapCategory)" apps/api/tests/` still returns the create_core_map test (MapCategory untouched).
    - BEHAVIOR: `uv run pytest apps/api/tests/repository/maps/ apps/api/tests/repository/community/test_community_repository_popular.py -q` runs without any `random_element([])` / IndexError / ValueError, all selected tests pass.
    - BEHAVIOR: `just test-api` completes the full API suite (exit 0) without collection or fixture errors caused by an empty get_args — this catches any file missed by the explicit 9-site list.
  </acceptance_criteria>
  <done>No test references `get_args(OverwatchMap)` (zero-assertion grep + full suite green); the full API suite runs clean post-flip; MapCategory get_args calls are preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → API request decode | The msgspec boundary that previously rejected unknown map names via the Literal enum |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Tampering / Elevation | `OverwatchMap` request fields (now `str`) | mitigate | The enum check that the Literal gave for free is replaced by a service-layer runtime check against `maps.names` (plan 15-03, REQ-02) PLUS the `core.maps.map_name` FK backstop (plan 15-02, REQ-11). This plan ONLY relaxes the type; it MUST NOT ship without the runtime gate — the dependency is encoded by Wave 2 plans consuming `maps.names`. msgspec still enforces field presence/type (a missing/non-string name → 400; Pitfall 3). |
| T-15-02 | Information disclosure | other Literals (MapCategory etc.) | accept | Only the map-name Literal is relaxed; category/mechanics/difficulty stay strict (acceptance test asserts `get_args(MapCategory)` intact). No new surface. |
</threat_model>

<verification>
- `just lint-sdk` clean (the SDK edit).
- `just test-api` runs the full suite without get_args-empty fixture failures.
- `OverwatchMap is str` and `get_args(MapCategory)` length 3 (no Literal regressed).
</verification>

<success_criteria>
- REQ-01 satisfied: the SDK no longer enforces a closed map-name set at decode; msgspec accepts any string name (presence/type still enforced).
- The 70 verbatim map names are captured for plan 15-02's seed rewrite.
- All 9 test-fixture sites use `_SEED_MAP_NAMES`; zero `get_args(OverwatchMap)` remain; MapCategory get_args preserved.
- Full API suite runs clean.
</success_criteria>

<output>
Create `.planning/phases/15-dynamic-overwatch-map-management/15-01-SUMMARY.md` when done. INCLUDE the 70 verbatim OverwatchMap names so plan 15-02 can rewrite the seed.
</output>
</output>
