---
phase: 15-dynamic-overwatch-map-management
plan: 05
type: execute
wave: 4
depends_on: ["15-04"]
files_modified:
  - apps/bot/extensions/api_service.py
  - apps/bot/extensions/moderator.py
  - apps/bot/extensions/map_editor.py
  - apps/bot/tests/test_map_name_select.py
autonomous: false
requirements: [REQ-09, REQ-10, D-02]
must_haves:
  truths:
    - "The bot has an api_service.get_all_map_names() client calling GET /utilities/map-names"
    - "MapNameSelect sources its options from a DB-fed list (fetched once in the async wizard-build context), not get_args(OverwatchMap)"
    - "A newly-added map appears in the moderator map-edit dropdown with no bot restart"
    - "get_args(MapCategory) and other Literal selects in moderator.py are unchanged"
  artifacts:
    - path: "apps/bot/extensions/api_service.py"
      provides: "get_all_map_names() -> Response[list[str]]"
      contains: "/utilities/map-names"
    - path: "apps/bot/extensions/moderator.py"
      provides: "MapNameSelect fed an all_maps list; pagination math verbatim"
      contains: "all_maps"
  key_links:
    - from: "apps/bot/extensions/moderator.py MapEditWizardView"
      to: "api_service.get_all_map_names()"
      via: "fetched once in the async command callback, threaded into __init__ -> rebuild -> MapNameSelect"
      pattern: "get_all_map_names"
    - from: "MapNameSelect.__init__"
      to: "DB-fed list"
      via: "all_maps param replaces list(get_args(OverwatchMap))"
      pattern: "all_maps"
---

<objective>
Swap the bot's moderator map-name dropdown off the (now-empty) `get_args(OverwatchMap)` and onto the DB-fed full list. Add `api_service.get_all_map_names()` (REQ-09) calling the new `GET /utilities/map-names`, and thread the fetched list through `MapEditWizardView` -> `rebuild()` -> `MapNameSelect` at BOTH construction sites (REQ-10). Keep the 25/page pagination math verbatim (Spike 008 proved it). This is the ONLY bot behavioural change in the phase.

Purpose: After the SDK flip, `get_args(str)` returns `()`, so the moderator dropdown would be empty. Sourcing it from the API/DB makes dynamically-added maps appear in the dropdown with no bot restart — completing the "appears on all three surfaces" goal.

Output: One client method; `MapNameSelect.__init__` + `MapEditWizardView.__init__`/`rebuild` updated; both async construction sites fetch the list once; one bot unit test for the pagination math. Ends with a human-verify checkpoint (no bot test harness for the live discord.py UI).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-dynamic-overwatch-map-management/15-CONTEXT.md
@.planning/phases/15-dynamic-overwatch-map-management/15-RESEARCH.md
@.planning/phases/15-dynamic-overwatch-map-management/15-PATTERNS.md
@.planning/phases/15-dynamic-overwatch-map-management/15-VALIDATION.md

<interfaces>
<!-- Client analog — api_service.py:788 get_autocomplete_map_names. ADD alongside:
def get_all_map_names(self) -> Response[list[str]]:
    r = Route("GET", "/utilities/map-names")
    return self._request(r, response_model=list[str]) -->

<!-- MapNameSelect.__init__ CURRENT (moderator.py:761-779):
def __init__(self, current: OverwatchMap | None, page: int = 0) -> None:
    all_maps = list(get_args(OverwatchMap))   # <- get_args(str) == () after the flip; THE breakage
    all_maps.sort()
    start_idx = page * _PAGINATED_SELECT_PAGE_SIZE
    end_idx = start_idx + _PAGINATED_SELECT_PAGE_SIZE
    page_maps = all_maps[start_idx:end_idx]
    options = [SelectOption(label=m, value=m, default=(m == current)) for m in page_maps]
    super().__init__(placeholder=f"Select map name (page {page + 1})...", options=options)
    self.page = page
    self.total_pages = (len(all_maps) + _PAGINATED_SELECT_PAGE_SIZE - 1) // _PAGINATED_SELECT_PAGE_SIZE

-- TARGET: add `all_maps: list[str]` param; `all_maps = sorted(all_maps)`; KEEP the slice / total_pages / SelectOption math VERBATIM. -->

<!-- MapNameSelect is constructed in rebuild() at moderator.py:1334-1337. rebuild() runs inside MapEditWizardView.
     MapEditWizardView constructed at TWO async sites:
       moderator.py:105   -> MapEditWizardView(map_data, is_mod=True)
       map_editor.py:61    -> MapEditWizardView(map_data, is_mod=False)
     At both: all_maps = await itx.client.api.get_all_map_names(); pass into __init__ -> store on self -> feed MapNameSelect. -->

<!-- MapNameSelect.__init__ is SYNC (ui.Select subclass) — DO NOT await inside it (RESEARCH Pattern 5 / anti-pattern). -->
<!-- moderator.py:743 get_args(MapCategory) STAYS a Literal — do not touch it. -->
<!-- _PAGINATED_SELECT_PAGE_SIZE = 25 (unchanged). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add api_service.get_all_map_names() client method</name>
  <read_first>
    - apps/bot/extensions/api_service.py (:788-813 — get_autocomplete_map_names + transform_map_name; the Route/_request/response_model shape)
    - .planning/phases/15-dynamic-overwatch-map-management/15-PATTERNS.md (api_service.py assignment)
  </read_first>
  <action>
    In `apps/bot/extensions/api_service.py`, add `get_all_map_names(self) -> Response[list[str]]` alongside `get_autocomplete_map_names` (~:788): `r = Route("GET", "/utilities/map-names"); return self._request(r, response_model=list[str])` — no `search`/`limit`/`params`. Add a Google-style docstring. Optionally flip the `list[OverwatchMap]` return hints in this file to `list[str]` (type-only; OverwatchMap is already str post-15-01).
  </action>
  <verify>
    <automated>cd apps/bot && grep -q "get_all_map_names" extensions/api_service.py && grep -q "utilities/map-names" extensions/api_service.py && uv run python -c "import ast; ast.parse(open('extensions/api_service.py').read()); print('parses')"</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: `get_all_map_names` exists, calls `Route("GET", "/utilities/map-names")` with `response_model=list[str]` and no params.
    - SOURCE: the file parses (no syntax error).
  </acceptance_criteria>
  <done>The bot client can fetch the full map-name list from the new endpoint.</done>
</task>

<task type="auto">
  <name>Task 2: DB-feed MapNameSelect — thread the fetched list through both construction sites</name>
  <read_first>
    - apps/bot/extensions/moderator.py (:756-789 MapNameSelect, :1273-1346 MapEditWizardView.__init__/rebuild + the MapNameSelect construction at :1334, :95-107 the moderator construction site at :105, :743 get_args(MapCategory) that STAYS)
    - apps/bot/extensions/map_editor.py (:50-63 — the second construction site at :61)
    - .planning/phases/15-dynamic-overwatch-map-management/15-RESEARCH.md (Pattern 5, the anti-pattern "don't await inside __init__")
  </read_first>
  <action>
    Change `MapNameSelect.__init__` to accept `all_maps: list[str]` (in addition to `current` and `page`); replace `all_maps = list(get_args(OverwatchMap)); all_maps.sort()` with `all_maps = sorted(all_maps)`. KEEP the `start_idx`/`end_idx`/`page_maps` slice, the `SelectOption` comprehension, the `super().__init__(...)`, `self.page`, and `self.total_pages` math BYTE-FOR-BYTE (Spike 008 proved it). Remove the now-unused `get_args`/`OverwatchMap` import from this usage ONLY if nothing else in the file needs them (note: `get_args(MapCategory)` at :743 still needs `get_args`).

    Thread the list: add an `all_maps: list[str]` param to `MapEditWizardView.__init__`, store it as `self._all_maps`, and pass it at the `MapNameSelect(...)` construction in `rebuild()` (~:1334) as `all_maps=self._all_maps`. At BOTH async construction sites — `moderator.py:105` (`MapEditWizardView(map_data, is_mod=True)`) and `map_editor.py:61` (`MapEditWizardView(map_data, is_mod=False)`) — fetch the list once with `await itx.client.api.get_all_map_names()` (use the same `.data`/value accessor the existing `get_autocomplete_map_names` callers use — confirm from a caller) and pass it into the view constructor. DO NOT `await` inside `MapNameSelect.__init__` (it is sync). DO NOT touch `get_args(MapCategory)` at moderator.py:743.
  </action>
  <verify>
    <automated>cd apps/bot && grep -q "all_maps" extensions/moderator.py && grep -q "get_all_map_names" extensions/moderator.py && grep -q "get_all_map_names" extensions/map_editor.py && grep -q "get_args(MapCategory)" extensions/moderator.py && uv run python -c "import ast; assert 'get_args(OverwatchMap)' not in open('extensions/moderator.py').read(); ast.parse(open('extensions/moderator.py').read()); ast.parse(open('extensions/map_editor.py').read()); print('parses, no get_args(OverwatchMap)')"</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: `MapNameSelect.__init__` takes an `all_maps: list[str]` param; `get_args(OverwatchMap)` no longer appears in moderator.py.
    - SOURCE: `get_args(MapCategory)` is still present in moderator.py (untouched).
    - SOURCE: both `moderator.py` and `map_editor.py` call `get_all_map_names()` in the async command callback and pass the result into `MapEditWizardView`; no `await` appears inside `MapNameSelect.__init__`.
    - SOURCE: the slice / `total_pages` / `SelectOption` math is byte-identical to the original (git diff shows only the sourcing line + signature changed).
    - BEHAVIOR: both files parse.
  </acceptance_criteria>
  <done>The moderator dropdown is DB-fed via the new endpoint at both construction sites; pagination math unchanged; MapCategory untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Bot unit test for MapNameSelect pagination with a DB-fed list</name>
  <read_first>
    - apps/bot/extensions/moderator.py (the updated MapNameSelect from Task 2)
    - apps/bot/tests/ (an existing bot unit test for the import/fixture style; pick the closest analog to a UI-component test)
    - .planning/phases/15-dynamic-overwatch-map-management/15-VALIDATION.md (REQ-10: pytest -k map_name_select)
  </read_first>
  <action>
    Create `apps/bot/tests/test_map_name_select.py` constructing `MapNameSelect` with a synthetic DB-fed `all_maps` list of, e.g., 63 strings and asserting: page 0 yields the first 25 sorted options; the last page yields the remainder; `total_pages == ceil(len / 25)`; `default=True` is set on the option matching `current`; an empty list yields 0 options and `total_pages == 0` (no crash). Name it so `pytest -k map_name_select` resolves. This unit-tests the verbatim Spike-008 math against a DB-fed list (the discord.py live UI is human-verify, Task 4).
  </action>
  <verify>
    <automated>cd apps/bot && uv run pytest tests/test_map_name_select.py -k map_name_select -x -q</automated>
  </verify>
  <acceptance_criteria>
    - BEHAVIOR: `pytest -k map_name_select -x` exits 0.
    - BEHAVIOR: page slicing (25/page), `total_pages`, `current`-default, and the empty-list case are all asserted.
    - BEHAVIOR: `just lint-bot` clean.
  </acceptance_criteria>
  <done>The pagination math is locked against a DB-fed list with a passing unit test.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The moderator map-edit wizard's map-name dropdown now sources its options from the live DB (`GET /utilities/map-names`) instead of the compiled-in Literal. A map added via `POST /api/v3/content/maps` should appear in the dropdown without restarting the bot.</what-built>
  <how-to-verify>
    1. Run the API (`just run-api`) and bot (`just run-bot`) locally against the local DB/MinIO.
    2. Add a brand-new map via the API: `POST /api/v3/content/maps` (multipart `name` + a small PNG `banner`) — or have an admin add one.
    3. In Discord, open the moderator map-edit wizard (`/map edit <code>`), advance to the Map Name field.
    4. Confirm the dropdown lists DB rows, includes the newly-added map, and paginates with the prev/next buttons (25/page). Confirm the banner for the new map resolves on the website/map read surface (`assets/map_banners/{stripped}.png`).
  </how-to-verify>
  <resume-signal>Type "approved" if the new map appears in the dropdown and paginates correctly, or describe what is wrong.</resume-signal>
</task>

</tasks>

<verification>
- `just test-api` and `just test-bot` (the bot pagination unit test) green.
- `just lint-bot` clean; both bot files parse.
- Human-verify: a newly-added map appears in the live moderator dropdown with no bot restart.
</verification>

<success_criteria>
- REQ-09: `api_service.get_all_map_names()` exists and calls the new endpoint.
- REQ-10: `MapNameSelect` is DB-fed at both construction sites; pagination math verbatim; MapCategory Literal untouched; no `await` in `__init__`.
- The phase goal closes: a one-call add appears on the bot surface (dropdown) with no redeploy.
</success_criteria>

<output>
Create `.planning/phases/15-dynamic-overwatch-map-management/15-05-SUMMARY.md` when done.
</output>
