---
phase: 02-sdk-types-domain-exceptions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - libs/sdk/src/genjishimada_sdk/tournaments.py
  - libs/sdk/src/genjishimada_sdk/__init__.py
autonomous: true
requirements: []

must_haves:
  truths:
    - "A tournaments.py SDK module exports 19 msgspec Structs and 2 Literal type aliases"
    - "API and bot can import tournament types from genjishimada_sdk.tournaments"
    - "PATCH request structs use UNSET/UnsetType with kw_only=True"
    - "JSONB columns are modeled as typed sub-structs (PlacementXpTier, StreakXpTier)"
    - "All four RabbitMQ event types are defined upfront"
  artifacts:
    - path: "libs/sdk/src/genjishimada_sdk/tournaments.py"
      provides: "Tournament SDK types"
      contains: "class TournamentConfigResponse"
    - path: "libs/sdk/src/genjishimada_sdk/__init__.py"
      provides: "SDK package exports"
      contains: "tournaments"
  key_links:
    - from: "libs/sdk/src/genjishimada_sdk/tournaments.py"
      to: "libs/sdk/src/genjishimada_sdk/difficulties.py"
      via: "from .difficulties import DifficultyTop"
      pattern: "from \\.difficulties import DifficultyTop"
    - from: "libs/sdk/src/genjishimada_sdk/__init__.py"
      to: "libs/sdk/src/genjishimada_sdk/tournaments.py"
      via: "from . import tournaments"
      pattern: "tournaments"
---

<objective>
Create the shared SDK type module for the tournament system -- all msgspec Structs (request, response, event types) and Literal type aliases that the API and bot use as their common data contract.

Purpose: Downstream phases (repository, service, controller, bot consumers) all import from this module. Having the full type contract defined upfront prevents SDK churn across phases 3-10.

Output: `libs/sdk/src/genjishimada_sdk/tournaments.py` with 19 Struct types, 2 Literal aliases, and 2 JSONB sub-structs. SDK `__init__.py` updated to export the new module.
</objective>

<execution_context>
@/Users/nebula/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nebula/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/02-sdk-types-domain-exceptions/02-CONTEXT.md
@.planning/phases/02-sdk-types-domain-exceptions/02-RESEARCH.md
@.planning/phases/02-sdk-types-domain-exceptions/02-PATTERNS.md
@.planning/phases/01-database-schema-migrations/01-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From libs/sdk/src/genjishimada_sdk/difficulties.py:
  DifficultyTop = Literal["Easy", "Medium", "Hard", "Very Hard", "Extreme", "Hell"]

From libs/sdk/src/genjishimada_sdk/__init__.py:
  Imports: auth, change_requests, completions, difficulties, internal, logs, lootbox, maps, newsfeed, notifications, rank_card, store, users, xp
  __all__ = [ ...same list as strings... ]

From apps/api/migrations/0020_tournaments.sql (DB column types):
  tournaments.config: blacklist_weeks int, created_at timestamptz, updated_at timestamptz
  tournaments.categories: id int, name text, difficulties text[], cycle_frequency text CHECK, participation_xp int, placement_xp jsonb, streak_xp jsonb, champion_role_id bigint, is_active boolean, created_at timestamptz, updated_at timestamptz
  tournaments.cycles: id int, category_id int, map_id int, status text CHECK, started_at timestamptz, ended_at timestamptz, created_at timestamptz
  tournaments.completions: id int, cycle_id int, user_id bigint, map_id int, time numeric(10,2), screenshot text, video text, verified boolean, completion boolean, inserted_at timestamptz
  tournaments.streaks: id int, user_id bigint, current_streak int, max_streak int, last_cycle_id int, updated_at timestamptz
  tournaments.pending_transitions: id int, cycle_id int, event_type text CHECK, payload jsonb, published boolean, created_at timestamptz

From libs/sdk/src/genjishimada_sdk/completions.py (pattern reference):
  import datetime as dt
  from typing import Annotated, Literal
  from msgspec import UNSET, Meta, Struct, UnsetType
  from .difficulties import DifficultyAll, DifficultyTop
  __all__ = ( "CompletionCreateRequest", ... )  # tuple, alphabetically sorted

From libs/sdk/src/genjishimada_sdk/xp.py (XP_TYPES reference):
  XP_TYPES = Literal["Map Submission", "Playtest", "Guide", "Completion", "Record", "World Record", "Quest", "Other"]
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create tournaments.py SDK module with all msgspec Structs</name>
  <files>libs/sdk/src/genjishimada_sdk/tournaments.py</files>
  <read_first>
    libs/sdk/src/genjishimada_sdk/completions.py
    libs/sdk/src/genjishimada_sdk/store.py
    libs/sdk/src/genjishimada_sdk/xp.py
    libs/sdk/src/genjishimada_sdk/difficulties.py
    libs/sdk/src/genjishimada_sdk/maps.py
    apps/api/migrations/0020_tournaments.sql
    .planning/phases/02-sdk-types-domain-exceptions/02-RESEARCH.md
    .planning/phases/02-sdk-types-domain-exceptions/02-PATTERNS.md
  </read_first>
  <action>
Create libs/sdk/src/genjishimada_sdk/tournaments.py following the completions.py module template exactly.

Imports: `import datetime as dt`, `from typing import Literal`, `from msgspec import UNSET, Struct, UnsetType`, `from .difficulties import DifficultyTop`. Do NOT import `from __future__ import annotations` (matches completions.py). Only import Meta from msgspec if using Annotated validators.

Define `__all__` as a tuple (not list), alphabetically sorted, containing all 21 public names (2 sub-structs, 2 Literal aliases, 4 events, 13 request/response types).

Type aliases after `__all__`, before class definitions:
- `CycleFrequency = Literal["weekly", "biweekly"]`
- `CycleStatus = Literal["pending", "active", "finalizing", "completed"]`

JSONB sub-structs (define first, referenced by later structs):
- `PlacementXpTier(Struct)` with fields: `place: int`, `xp: int`
- `StreakXpTier(Struct)` with fields: `threshold: int`, `xp: int`

Config types per D-04:
- `TournamentConfigResponse(Struct)` -- mirrors tournaments.config: `blacklist_weeks: int`, `created_at: dt.datetime`, `updated_at: dt.datetime`. Omit `id` (singleton, always 1).
- `TournamentConfigPatchRequest(Struct, kw_only=True)` per D-05 -- `blacklist_weeks: int | UnsetType = UNSET`

Category types per D-04:
- `TournamentCategoryResponse(Struct)` -- mirrors tournaments.categories: `id: int`, `name: str`, `difficulties: list[DifficultyTop]`, `cycle_frequency: CycleFrequency`, `participation_xp: int`, `placement_xp: list[PlacementXpTier]`, `streak_xp: list[StreakXpTier]`, `champion_role_id: int | None`, `is_active: bool`, `created_at: dt.datetime`, `updated_at: dt.datetime`
- `TournamentCategoryCreateRequest(Struct)` -- required: `name: str`, `difficulties: list[DifficultyTop]`; defaulted: `cycle_frequency: CycleFrequency = "weekly"`, `participation_xp: int = 0`, `placement_xp: list[PlacementXpTier] = []`, `streak_xp: list[StreakXpTier] = []`, `champion_role_id: int | None = None`
- `TournamentCategoryPatchRequest(Struct, kw_only=True)` per D-05 -- all fields as `FieldType | UnsetType = UNSET`: name, difficulties, cycle_frequency, participation_xp, placement_xp, streak_xp, champion_role_id (use `int | None | UnsetType = UNSET` for nullable), is_active

Cycle types per D-04:
- `TournamentCycleResponse(Struct)` -- mirrors tournaments.cycles: `id: int`, `category_id: int`, `map_id: int`, `status: CycleStatus`, `started_at: dt.datetime | None`, `ended_at: dt.datetime | None`, `created_at: dt.datetime`
- `TournamentCycleResultsResponse(Struct)` -- cycle archive with standings: `id: int`, `category_id: int`, `map_id: int`, `status: CycleStatus`, `started_at: dt.datetime | None`, `ended_at: dt.datetime | None`, `created_at: dt.datetime`, `standings: list[TournamentLeaderboardEntryResponse]` (per A1 in RESEARCH.md)

Leaderboard type per D-04:
- `TournamentLeaderboardEntryResponse(Struct)` -- per A3: `rank: int`, `user_id: int`, `name: str`, `time: float`, `verified: bool`, `completion: bool`

Completion types per D-04:
- `TournamentCompletionCreateRequest(Struct)` -- per A4, fields: `user_id: int`, `time: float`, `screenshot: str`, `video: str | None = None`
- `TournamentCompletionResponse(Struct)` -- mirrors tournaments.completions: `id: int`, `cycle_id: int`, `user_id: int`, `map_id: int`, `time: float` (numeric -> float per project codec), `screenshot: str`, `video: str | None`, `verified: bool`, `completion: bool`, `inserted_at: dt.datetime`

Streak type per D-04:
- `TournamentStreakResponse(Struct)` -- mirrors tournaments.streaks: `user_id: int`, `current_streak: int`, `max_streak: int`, `last_cycle_id: int | None`, `updated_at: dt.datetime`

Event types per D-03:
- `TournamentCycleStartedEvent(Struct)` -- minimal: `cycle_id: int`, `category_id: int`, `map_id: int`
- `TournamentCycleCompletedEvent(Struct)` -- minimal: `cycle_id: int`, `category_id: int`
- `TournamentCompletionCreatedEvent(Struct)` -- minimal: `completion_id: int`, `cycle_id: int`
- `TournamentXpGrantEvent(Struct)` -- per A2: `user_id: int`, `amount: int`, `cycle_id: int`, `category_id: int`, `grant_reason: str`

Every public class MUST have a Google-style docstring with summary line + Attributes section (indented `name: Description.` with period). Required fields first, optional/defaulted last. Follow completions.py field ordering pattern exactly.
  </action>
  <verify>
    <automated>cd /Users/nebula/Documents/coding/parkour/genji/genjishimada && just lint-sdk</automated>
  </verify>
  <acceptance_criteria>
    - File exists at libs/sdk/src/genjishimada_sdk/tournaments.py
    - __all__ tuple contains exactly 21 entries (2 sub-structs + 2 Literal aliases + 4 events + 13 req/resp types)
    - CycleFrequency and CycleStatus are Literal type aliases
    - PlacementXpTier has fields place: int, xp: int
    - StreakXpTier has fields threshold: int, xp: int
    - TournamentConfigPatchRequest uses kw_only=True and UNSET defaults
    - TournamentCategoryPatchRequest uses kw_only=True and UNSET defaults
    - TournamentCompletionResponse.time is float (not int or Decimal)
    - All four event types defined: TournamentCycleStartedEvent, TournamentCycleCompletedEvent, TournamentCompletionCreatedEvent, TournamentXpGrantEvent
    - `just lint-sdk` passes with zero errors
  </acceptance_criteria>
  <done>tournaments.py SDK module exists with 19 Struct classes, 2 Literal aliases, and passes lint-sdk cleanly</done>
</task>

<task type="auto">
  <name>Task 2: Register tournaments module in SDK __init__.py</name>
  <files>libs/sdk/src/genjishimada_sdk/__init__.py</files>
  <read_first>
    libs/sdk/src/genjishimada_sdk/__init__.py
  </read_first>
  <action>
Edit libs/sdk/src/genjishimada_sdk/__init__.py per D-02:

In the `from . import (...)` block, add `tournaments,` in alphabetical position (between `store,` and `users,`).

In the `__all__ = [...]` list, add `"tournaments",` in alphabetical position (between `"store",` and `"users",`).

No other changes to this file.
  </action>
  <verify>
    <automated>cd /Users/nebula/Documents/coding/parkour/genji/genjishimada && uv run --directory libs/sdk python -c "from genjishimada_sdk import tournaments; print(f'Imported {len(tournaments.__all__)} exports')"</automated>
  </verify>
  <acceptance_criteria>
    - libs/sdk/src/genjishimada_sdk/__init__.py contains "tournaments" in both the import block and __all__ list
    - `from genjishimada_sdk import tournaments` succeeds without ImportError
    - `from genjishimada_sdk.tournaments import TournamentConfigResponse` succeeds without ImportError
  </acceptance_criteria>
  <done>SDK __init__.py exports the tournaments module and imports resolve cleanly</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | No runtime behavior -- pure type definitions and data class declarations |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | SDK Struct field types | mitigate | Use Literal[...] for CycleFrequency and CycleStatus to constrain valid values at decode time; use float for numeric(10,2) per project codec |
| T-02-02 | Tampering | JSONB sub-struct typing | mitigate | Use typed PlacementXpTier/StreakXpTier sub-structs instead of list[dict] -- msgspec validates structure on decode |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed -- all dependencies already in libs/sdk/pyproject.toml |

No runtime behavior exists in this plan. Types are validated by msgspec at decode time in downstream phases.
</threat_model>

<verification>
1. `just lint-sdk` passes with zero errors
2. `from genjishimada_sdk.tournaments import TournamentConfigResponse` resolves
3. All 21 public names are in __all__ tuple
4. No new packages added to pyproject.toml
</verification>

<success_criteria>
- tournaments.py SDK module exists with 19 Struct types, 2 Literal aliases, 2 JSONB sub-structs
- SDK __init__.py exports the tournaments module
- `just lint-sdk` passes cleanly
- All types match database column types from 0020_tournaments.sql (float for numeric, dt.datetime for timestamptz, int for bigint)
</success_criteria>

<output>
Create `.planning/phases/02-sdk-types-domain-exceptions/02-01-SUMMARY.md` when done
</output>
