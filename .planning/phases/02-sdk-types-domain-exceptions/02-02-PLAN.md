---
phase: 02-sdk-types-domain-exceptions
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/services/exceptions/tournaments.py
  - apps/api/services/exceptions/__init__.py
autonomous: true
requirements: []

must_haves:
  truths:
    - "A TournamentsError base exception class exists extending DomainError"
    - "Eight specific tournament exception subclasses exist with context parameters"
    - "CategoryNotFoundError is aliased as TournamentsCategoryNotFoundError in the barrel __init__.py to avoid collision with content.CategoryNotFoundError"
    - "All tournament exceptions can be imported from services.exceptions"
  artifacts:
    - path: "apps/api/services/exceptions/tournaments.py"
      provides: "Tournament domain exception hierarchy"
      contains: "class TournamentsError"
    - path: "apps/api/services/exceptions/__init__.py"
      provides: "Barrel re-exports for tournament exceptions"
      contains: "TournamentsCategoryNotFoundError"
  key_links:
    - from: "apps/api/services/exceptions/tournaments.py"
      to: "apps/api/utilities/errors.py"
      via: "from utilities.errors import DomainError"
      pattern: "from utilities\\.errors import DomainError"
    - from: "apps/api/services/exceptions/__init__.py"
      to: "apps/api/services/exceptions/tournaments.py"
      via: "from .tournaments import"
      pattern: "from \\.tournaments import"
---

<objective>
Create the domain exception hierarchy for the tournament system -- a base TournamentsError and eight specific business rule violation exceptions. Update the barrel __init__.py with imports and aliasing for the CategoryNotFoundError name collision.

Purpose: The service layer (Phase 4+) catches repository exceptions and raises these domain exceptions. Controllers catch them and return appropriate HTTP errors. Having the exception classes ready prevents service/controller phases from defining inline errors.

Output: `apps/api/services/exceptions/tournaments.py` with 9 exception classes. `apps/api/services/exceptions/__init__.py` updated with imports and __all__ entries.
</objective>

<execution_context>
@/Users/nebula/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nebula/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/02-sdk-types-domain-exceptions/02-CONTEXT.md
@.planning/phases/02-sdk-types-domain-exceptions/02-RESEARCH.md
@.planning/phases/02-sdk-types-domain-exceptions/02-PATTERNS.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From apps/api/utilities/errors.py (lines 150-169):
  class DomainError(Exception):
      def __init__(self, message: str, **context: typing.Any) -> None:
          super().__init__(message)
          self.message = message
          self.context = context

From apps/api/services/exceptions/completions.py (full pattern):
  """Completions domain exceptions."""
  from utilities.errors import DomainError
  class CompletionsError(DomainError): """Base for completions domain errors."""
  class MapNotFoundError(CompletionsError):
      def __init__(self, code: str) -> None:
          super().__init__("This map code does not exist or has been archived.", code=code)
  class DuplicateCompletionError(CompletionsError):
      def __init__(self, user_id: int, map_code: str) -> None:
          super().__init__("You already have a completion for this map.", user_id=user_id, map_code=map_code)

From apps/api/services/exceptions/__init__.py (aliasing pattern):
  from .change_requests import (MapNotFoundError as ChangeRequestsMapNotFoundError,)
  from .completions import (MapNotFoundError as CompletionsMapNotFoundError,)
  from .users import (UserNotFoundError as UsersUserNotFoundError,)
  from .content import (CategoryNotFoundError, ...)  # <-- existing, will collide
  __all__ = [ ...alphabetically sorted list... ]

Existing content.CategoryNotFoundError at line 38 of __init__.py:
  from .content import (CategoryNotFoundError, ...)
  Already in __all__ at position ~line 96
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create tournaments.py domain exception module</name>
  <files>apps/api/services/exceptions/tournaments.py</files>
  <read_first>
    apps/api/services/exceptions/completions.py
    apps/api/services/exceptions/store.py
    apps/api/services/exceptions/maps.py
    apps/api/utilities/errors.py
    .planning/phases/02-sdk-types-domain-exceptions/02-PATTERNS.md
  </read_first>
  <action>
Create apps/api/services/exceptions/tournaments.py per D-06 and D-09, following the completions.py exception module template exactly.

Module docstring: """Tournaments domain exceptions.\n\nThese exceptions represent business rule violations in the tournaments domain.\nThey are raised by TournamentsService and caught by controllers.\n"""

Single import: `from utilities.errors import DomainError`

Define 9 exception classes in this order (base first, then alphabetical):

1. `TournamentsError(DomainError)` -- base, docstring only, no __init__
2. `CategoryLockedError(TournamentsError)` -- params: `category_id: int`, `cycle_id: int`. Message: "Category cannot be modified while a cycle is active."
3. `CategoryNotFoundError(TournamentsError)` -- param: `category_id: int`. Message: "Tournament category not found." (Note: uses unqualified name in-module per D-06; aliased in __init__.py per RESEARCH.md Pitfall 1)
4. `CycleAlreadyActiveError(TournamentsError)` -- param: `category_id: int`. Message: "A cycle is already active for this category."
5. `CycleNotActiveError(TournamentsError)` -- params: `cycle_id: int`, `status: str`. Message: f-string including current status, e.g., f"Cycle is not active (current status: {status})."
6. `CycleNotFoundError(TournamentsError)` -- param: `cycle_id: int`. Message: "Cycle not found."
7. `DuplicateTournamentCompletionError(TournamentsError)` -- params: `user_id: int`, `cycle_id: int`. Message: "User has already submitted a completion for this cycle."
8. `MapNotEligibleError(TournamentsError)` -- param: `map_id: int`. Message: "Map is not eligible for tournament selection." Add optional `reason: str = ""` param; if provided, append to message.
9. `NoCycleActiveError(TournamentsError)` -- param: `category_id: int`. Message: "No active cycle exists for this category."

Every exception subclass has:
- Class docstring (one-line summary, no Attributes section needed for exceptions)
- `def __init__(self, ...) -> None:` with typed params
- `super().__init__("message", key=value, ...)` passing all params as context kwargs
- No __init__ docstring (D107 is ignored; matches completions.py pattern)

Do NOT use the `handle_db_exceptions` decorator pattern. Do NOT import or reference repository exceptions. Per D-07, the service layer will translate repository exceptions to these domain exceptions.
  </action>
  <verify>
    <automated>cd /Users/nebula/Documents/coding/parkour/genji/genjishimada && uv run --directory apps/api ruff check services/exceptions/tournaments.py && uv run --directory apps/api basedpyright services/exceptions/tournaments.py</automated>
  </verify>
  <acceptance_criteria>
    - File exists at apps/api/services/exceptions/tournaments.py
    - Module docstring follows Google convention: "Tournaments domain exceptions."
    - TournamentsError extends DomainError
    - Exactly 8 specific exception subclasses exist (CategoryLockedError, CategoryNotFoundError, CycleAlreadyActiveError, CycleNotActiveError, CycleNotFoundError, DuplicateTournamentCompletionError, MapNotEligibleError, NoCycleActiveError)
    - Every subclass has `def __init__(self, ...) -> None:` with typed parameters
    - Every __init__ calls `super().__init__("message", key=value)` with all params as kwargs
    - CycleNotActiveError uses f-string to include status in message
    - ruff check and basedpyright pass with zero errors
  </acceptance_criteria>
  <done>Tournament domain exception module with 1 base + 8 specific exception classes, passing lint and type-check</done>
</task>

<task type="auto">
  <name>Task 2: Update exceptions __init__.py with tournament imports and aliasing</name>
  <files>apps/api/services/exceptions/__init__.py</files>
  <read_first>
    apps/api/services/exceptions/__init__.py
    apps/api/services/exceptions/tournaments.py
  </read_first>
  <action>
Edit apps/api/services/exceptions/__init__.py to add tournament exception imports per the established pattern.

Add two import blocks for the tournaments module. Position them alphabetically among existing domain import blocks (after the `.store` block, before the `.users` block):

Block 1 -- standard imports (non-colliding names):
```
from .tournaments import (
    CategoryLockedError,
    CycleAlreadyActiveError,
    CycleNotActiveError,
    CycleNotFoundError,
    DuplicateTournamentCompletionError,
    MapNotEligibleError,
    NoCycleActiveError,
    TournamentsError,
)
```

Block 2 -- aliased import (collision with content.CategoryNotFoundError):
```
from .tournaments import (
    CategoryNotFoundError as TournamentsCategoryNotFoundError,
)
```

This matches the established aliasing pattern: ChangeRequestsMapNotFoundError, CompletionsMapNotFoundError, UsersUserNotFoundError.

Update the `__all__` list to include all new names in correct alphabetical positions:
- "CategoryLockedError" (after "CategoryNotFoundError")
- "CycleAlreadyActiveError" (after "CreatorNotFoundError")
- "CycleNotActiveError" (after "CycleAlreadyActiveError")
- "CycleNotFoundError" (after "CycleNotActiveError")
- "DuplicateTournamentCompletionError" (after "DuplicateTagsError")
- "MapNotEligibleError" (after "MapCodeExistsError")
- "NoCycleActiveError" (after "MasteryUpdateFailedError")
- "TournamentsCategoryNotFoundError" (after "TokenInvalidError")
- "TournamentsError" (after "TournamentsCategoryNotFoundError")

Total: 9 new entries in __all__ (8 direct + 1 alias).
  </action>
  <verify>
    <automated>cd /Users/nebula/Documents/coding/parkour/genji/genjishimada && uv run --directory apps/api python -c "from services.exceptions import TournamentsError, TournamentsCategoryNotFoundError, CycleNotActiveError; print('All tournament exceptions importable')"</automated>
  </verify>
  <acceptance_criteria>
    - apps/api/services/exceptions/__init__.py has two import blocks from .tournaments
    - CategoryNotFoundError is aliased as TournamentsCategoryNotFoundError (not conflicting with content.CategoryNotFoundError)
    - __all__ list contains exactly 9 new entries for tournament exceptions
    - `from services.exceptions import TournamentsError` works without ImportError
    - `from services.exceptions import TournamentsCategoryNotFoundError` works without ImportError
    - `from services.exceptions import CategoryNotFoundError` still imports the content domain version (not overwritten)
  </acceptance_criteria>
  <done>Barrel __init__.py exports all tournament exceptions with proper aliasing, no naming collisions</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | No runtime behavior -- exception class definitions only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Information Disclosure | Exception messages | mitigate | Exception messages contain only domain context (IDs, status strings), no internal DB details, no stack traces in user-facing output |
| T-02-04 | Tampering | Exception context kwargs | accept | Context kwargs are internal (logged/Sentry), never serialized to HTTP response body; low risk |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed |

No runtime behavior in this plan. Exceptions are raised by services (Phase 4+) and caught by controllers to produce HTTP responses.
</threat_model>

<verification>
1. `ruff check apps/api/services/exceptions/tournaments.py` passes
2. `basedpyright apps/api/services/exceptions/tournaments.py` passes
3. All tournament exceptions importable from `services.exceptions`
4. `CategoryNotFoundError` still resolves to content domain version (no collision)
5. `TournamentsCategoryNotFoundError` resolves to tournament domain version
</verification>

<success_criteria>
- tournaments.py exception module exists with TournamentsError base + 8 specific subclasses
- All exceptions follow the three-tier pattern (extend DomainError, context kwargs)
- Barrel __init__.py updated with imports, aliasing, and __all__ entries
- No naming collision between content.CategoryNotFoundError and tournaments.CategoryNotFoundError
- ruff check + basedpyright pass for the exception module
</success_criteria>

<output>
Create `.planning/phases/02-sdk-types-domain-exceptions/02-02-SUMMARY.md` when done
</output>
