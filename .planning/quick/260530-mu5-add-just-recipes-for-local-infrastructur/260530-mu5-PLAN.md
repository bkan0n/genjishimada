---
phase: 260530-mu5
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [justfile]
autonomous: true
requirements: [QUICK-mu5]
must_haves:
  truths:
    - "Running `just infra-up` starts the local Docker Compose stack detached"
    - "Running `just infra-down` stops and removes the local Docker Compose stack"
    - "Running `just infra-logs` follows logs from the local Docker Compose stack"
    - "The three recipes live in the existing `# Convenience` section"
  artifacts:
    - path: "justfile"
      provides: "infra-up, infra-down, infra-logs recipes"
      contains: "docker-compose.local.yml"
  key_links:
    - from: "justfile infra-* recipes"
      to: "docker-compose.local.yml"
      via: "docker compose -f flag"
      pattern: "docker compose -f docker-compose.local.yml"
---

<objective>
Add three convenience `just` recipes to the repo-root `justfile` for managing the local
infrastructure stack defined in `docker-compose.local.yml` (postgres-local, rabbitmq-local,
minio-local).

Purpose: Replace the manually-typed `docker compose -f docker-compose.local.yml ...` commands
documented in CLAUDE.md with short, memorable `just` targets matching the existing task-runner
workflow.
Output: Updated `justfile` with `infra-up`, `infra-down`, `infra-logs` recipes in the
`# Convenience` section.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

@justfile
@docker-compose.local.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add infra-up, infra-down, infra-logs recipes to Convenience section</name>
  <files>justfile</files>
  <action>
    In `justfile`, locate the `# Convenience` section (currently containing `lint-all`,
    `test-all`, and `ci`, between the `# Convenience` header at line 71-73 and the
    `# Documentation (MkDocs)` header at line 87). After the `ci:` recipe (ending line 85)
    and before the `# ----------------------------` line for the Documentation section,
    append three recipes, each preceded by a single descriptive comment line.

    Match the EXACT style of the existing file: a `# ...` comment line describing the recipe,
    the recipe name followed by a colon, then a recipe body indented with FOUR spaces (the file
    uses 4-space indentation throughout — verify against existing recipes like `lint-all` and
    `docs-serve`; do NOT use 2-space indentation).

    Add these three recipes (all targeting `docker-compose.local.yml` per the local-dev workflow
    in CLAUDE.md):
    - `infra-up` — comment "Start local infrastructure (postgres, rabbitmq, minio) in Docker"; body: `docker compose -f docker-compose.local.yml up -d`
    - `infra-down` — comment "Stop and remove local infrastructure containers"; body: `docker compose -f docker-compose.local.yml down`
    - `infra-logs` — comment "Follow logs from local infrastructure"; body: `docker compose -f docker-compose.local.yml logs -f`

    Use `docker compose` (space form, v2 plugin) to stay consistent with CLAUDE.md, not the
    legacy `docker-compose` hyphenated binary. Do not modify any other recipe or section.
    Preserve the blank-line spacing pattern used between adjacent recipes (one blank line
    separating each comment+recipe block).
  </action>
  <verify>
    <automated>just --list 2>/dev/null | grep -Eq 'infra-up' && just --list 2>/dev/null | grep -Eq 'infra-down' && just --list 2>/dev/null | grep -Eq 'infra-logs' && echo OK</automated>
  </verify>
  <done>
    `just --list` parses without error and shows `infra-up`, `infra-down`, and `infra-logs`.
    Each recipe body invokes `docker compose -f docker-compose.local.yml` with the correct
    subcommand (`up -d`, `down`, `logs -f`). Recipes appear in the `# Convenience` section with
    4-space-indented bodies and a descriptive comment line above each. No other recipes changed.
  </done>
</task>

</tasks>

<verification>
- `just --list` runs without a parse error (confirms valid justfile syntax)
- `infra-up`, `infra-down`, `infra-logs` all appear in `just --list` output
- `grep -c 'docker compose -f docker-compose.local.yml' justfile` returns 3
</verification>

<success_criteria>
- Three new recipes (`infra-up`, `infra-down`, `infra-logs`) exist in the `# Convenience` section
- Each uses `docker compose -f docker-compose.local.yml` with the correct subcommand
- Style matches existing recipes (descriptive comment line, 4-space-indented body)
- `just --list` parses cleanly and lists all three new recipes
</success_criteria>

<output>
Create `.planning/quick/260530-mu5-add-just-recipes-for-local-infrastructur/260530-mu5-SUMMARY.md` when done
</output>
